<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Cabbage74</title><description>Yo!</description><link>https://cabbage74.github.io/</link><language>en</language><item><title>Decoder-Only Transformer</title><link>https://cabbage74.github.io/posts/decoder-only-transformer/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/decoder-only-transformer/</guid><pubDate>Sat, 22 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;目标&lt;/h2&gt;
&lt;p&gt;实现一个Mini的Decoder-Only Transformer复习一下大致架构。&lt;/p&gt;
&lt;p&gt;不会使用BPE，Rope，AdamW等，理解网络结构为主。&lt;/p&gt;
&lt;p&gt;将Pytorch视为一个自动算梯度的库。&lt;/p&gt;
&lt;h2&gt;网络&lt;/h2&gt;
</content:encoded></item><item><title>LibKVDB</title><link>https://cabbage74.github.io/posts/libkvdb/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/libkvdb/</guid><description>JYY OS 2026 M9</description><pubDate>Sun, 16 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;理论&lt;/h2&gt;
&lt;p&gt;最后两节JYY广度优先地讲了计算机安全和虚拟化的一些东西。&lt;/p&gt;
&lt;p&gt;让“一套服务感觉自己在独占机器”有两条路线。&lt;/p&gt;
&lt;p&gt;一条是SOSP1997论文Disco为起点，VMWare为代表的思路。&lt;/p&gt;
&lt;p&gt;一条是利用Linux自带的Namespace和Cgroup做隔离，Docker把这套做成了一个标准化又易用的产品。&lt;/p&gt;
&lt;h2&gt;实验&lt;/h2&gt;
&lt;p&gt;实现一个Crash Safe的简易KV DB。&lt;/p&gt;
&lt;p&gt;不要求性能，一把大锁加Write Ahead Log即可。&lt;/p&gt;
</content:encoded></item><item><title>日拱两卒（十）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E5%8D%81/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E5%8D%81/</guid><description>1 - 10</description><pubDate>Fri, 14 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;10&lt;/h2&gt;
&lt;p&gt;和为K的子数组。&lt;/p&gt;
&lt;p&gt;枚举左端点，维护潜在目标右端点的数量。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def subarraySum(self, nums: List[int], k: int) -&amp;gt; int:
        n = len(nums)
        s = [0 for _ in range(n + 1)]
        cnt = defaultdict(int)
        for i in range(n):
            s[i + 1] = s[i] + nums[i]
            cnt[s[i + 1]] += 1
        
        ans = 0
        for l in range(1, n + 1):
            ans += cnt[s[l - 1] + k]
            cnt[s[l]] -= 1
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;9&lt;/h2&gt;
&lt;p&gt;找到字符串中所有字母异位词。&lt;/p&gt;
&lt;p&gt;字母的种类和个数一样，顺序不要求一样就是异位词。维护定长滑动窗口里的字母种类和个数。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def findAnagrams(self, s: str, p: str) -&amp;gt; List[int]:
        n, m = len(s), len(p)
        
        target = defaultdict(int)
        for i in range(m):
            target[p[i]] += 1
        
        cnt = defaultdict(int)
        l = 0
        ans = []

        def check() -&amp;gt; bool:
            for c in cnt:
                if target[c] != cnt[c]:
                    return False
            return True

        for r in range(n):
            cnt[s[r]] += 1
            if r - l + 1 &amp;gt; m:
                cnt[s[l]] -= 1
                l += 1
            if r - l + 1 == m and check():
                ans.append(l)
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;8&lt;/h2&gt;
&lt;p&gt;无重复字符的最长子串。&lt;/p&gt;
&lt;p&gt;还是滑动窗口。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def lengthOfLongestSubstring(self, s: str) -&amp;gt; int:
        n = len(s)
        cnt = defaultdict(int)

        ans = 0
        l = 0
        for r in range(n):
            cnt[s[r]] += 1
            
            for c in cnt:
                while cnt[c] &amp;gt; 1:
                    cnt[s[l]] -= 1
                    l += 1
            
            ans = max(ans, r - l + 1)

        return ans
            
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;7&lt;/h2&gt;
&lt;p&gt;接雨水。&lt;/p&gt;
&lt;p&gt;每一个位置的贡献是&lt;code&gt;min(左侧最高木板，右侧最高木板) - 该位置模板高度&lt;/code&gt;。维护前后缀最大值很容易算。&lt;/p&gt;
&lt;p&gt;题解的优化是把空间降到&lt;code&gt;O(1)&lt;/code&gt;，相向双指针，原理是木板短的那一侧都能确定贡献，所以能移动这一侧的指针。不预计算数组而是动态维护两个变量。&lt;/p&gt;
&lt;p&gt;没啥必要。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def trap(self, height: List[int]) -&amp;gt; int:
        n = len(height)
        pre_max = [-1 for _ in range(n)]
        suf_max = [-1 for _ in range(n)]

        pre_max[0] = height[0]
        for i in range(1, n):
            pre_max[i] = max(pre_max[i - 1], height[i])

        suf_max[n - 1] = height[n - 1]
        for i in range(n - 2, 0, -1):
            suf_max[i] = max(suf_max[i + 1], height[i])

        ans = 0
        for i in range(1, n - 1):
            ans += max(0, min(pre_max[i - 1], suf_max[i + 1]) - height[i])
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;6&lt;/h2&gt;
&lt;p&gt;三数之和。&lt;/p&gt;
&lt;p&gt;固定一位，剩下两位靠排序带来的单调性做双指针。&lt;/p&gt;
&lt;p&gt;如果不用&lt;code&gt;set()&lt;/code&gt;去重的话要跳过重复元素。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def threeSum(self, nums: list[int]) -&amp;gt; list[list[int]]:
        ans = []
        n = len(nums)
        nums.sort()

        for i in range(n):
            if i &amp;gt; 0 and nums[i] == nums[i - 1]:
                continue

            l, r = i + 1, n - 1

            while l &amp;lt; r:
                s = nums[i] + nums[l] + nums[r]

                if s == 0:
                    ans.append([nums[i], nums[l], nums[r]])

                    while l &amp;lt; r and nums[l + 1] == nums[l]:
                        l += 1

                    while l &amp;lt; r and nums[r - 1] == nums[r]:
                        r -= 1

                    l += 1
                    r -= 1

                elif s &amp;gt; 0:
                    r -= 1

                else:
                    l += 1

        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;5&lt;/h2&gt;
&lt;p&gt;盛最多水的容器。&lt;/p&gt;
&lt;p&gt;相向双指针，动长的那根木板一定没收益，所以每次动短的那根木板。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def maxArea(self, height: List[int]) -&amp;gt; int:
        n = len(height)
        l, r = 0, n - 1
        ans = (n - 1) * min(height[0], height[n - 1])

        while l &amp;lt; r:
            if height[l] &amp;lt; height[r]:
                l += 1
            else:
                r -= 1
            ans = max(ans, (r - l) * min(height[l], height[r]))
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;4&lt;/h2&gt;
&lt;p&gt;移动零。&lt;/p&gt;
&lt;p&gt;维护不变量，把非零元素都换到前面来。&lt;/p&gt;
&lt;p&gt;写成把零元素都换到后面去的逻辑会导致非零元素的原本顺序被打乱。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def moveZeroes(self, nums: List[int]) -&amp;gt; None:
        end = 0
        n = len(nums)

        for i in range(n):
            if nums[i] != 0:
                nums[end], nums[i] = nums[i], nums[end]
                end += 1
        
        return
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;3&lt;/h2&gt;
&lt;p&gt;最长连续序列。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;101 5 1 2 3 9 4&lt;/code&gt;的最长连续序列指&lt;code&gt;1 2 3 4 5&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;维护个数，记忆化，每个元素最多被遍历一次。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def longestConsecutive(self, nums: List[int]) -&amp;gt; int:
        n = len(nums)
        cnt = defaultdict(int)

        for i in range(n):
            cnt[nums[i]] += 1

        @cache
        def dfs(u) -&amp;gt; int:
            if not cnt.get(u - 1):
                return 1
            return 1 + dfs(u - 1)

        ans = 0
        for c in cnt:
            ans = max(ans, dfs(c))
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;2&lt;/h2&gt;
&lt;p&gt;字母异位词分组。&lt;/p&gt;
&lt;p&gt;把字符串按照字符种类和个数分类。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def groupAnagrams(self, strs: List[str]) -&amp;gt; List[List[str]]:
        ans = []
        tot = 0
        mp = {}

        for s in strs:
            key = &apos;&apos;.join(sorted(s))
            if key not in mp:
                ans.append([s])
                mp[key] = tot
                tot += 1
            else:
                ans[mp[key]].append(s)
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;1&lt;/h2&gt;
&lt;p&gt;两数之和。&lt;/p&gt;
&lt;p&gt;注意要求了一个数不能用两次。&lt;/p&gt;
&lt;p&gt;终于！结束了！&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def twoSum(self, nums: List[int], target: int) -&amp;gt; List[int]:
        n = len(nums)
        mp = {}

        for i in range(n):
            if target - nums[i] in mp:
                return [i, mp[target - nums[i]]]
            mp[nums[i]] = i
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>FSRecov</title><link>https://cabbage74.github.io/posts/fsrecov/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/fsrecov/</guid><description>JYY OS 2026 M8</description><pubDate>Thu, 13 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;理论&lt;/h2&gt;
&lt;p&gt;文件系统本质是将块设备按照某种方法（如ext4）维护成数据结构（目录树）。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;mount&lt;/code&gt;这个syscall可以把一个块设备挂载（解析块设备）到指定目录。比如插U盘这个例子，桌面上或者某个文件夹里会多一个U盘文件夹。&lt;/p&gt;
&lt;p&gt;目录树这个数据结构在Linux表现为每个节点是&lt;code&gt;inode&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;把文件名想象成边，硬链接&lt;code&gt;ln a.txt b.txt&lt;/code&gt;表现为从目录节点新建一条边指向&lt;code&gt;a.txt&lt;/code&gt;那条边指向的节点。所以&lt;code&gt;rm&lt;/code&gt;这个操作本质调的syscall是&lt;code&gt;unlink&lt;/code&gt;，引用计数减一，不管是&lt;code&gt;rm a.txt&lt;/code&gt;或是&lt;code&gt;rm b.txt&lt;/code&gt;都不会删这个节点。&lt;/p&gt;
&lt;p&gt;软链接/符号链接&lt;code&gt;ln -s a.txt b.txt&lt;/code&gt;表现为从目录节点新建一条边指向一个新节点，这个新节点可以看作什么都没有就是个路径字符串&lt;code&gt;a.txt&lt;/code&gt;。内核解析时看到了会跳到目标路径的文件。&lt;/p&gt;
&lt;p&gt;文件有很多属性。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;-rw-rw-r--   1   ubuntu   ubuntu   44   Aug 5 19:09   add.c
│            │     │        │      │        │          │
│            │     │        │      │        │          └─ 文件名
│            │     │        │      │        └─ 最后修改时间 mtime
│            │     │        │      └─ 文件大小，单位字节
│            │     │        └─ 所属组 group
│            │     └─ 所有者 owner
│            └─ 硬链接数量
└─ 文件类型 + 权限

-   rw-   rw-   r--
│    │     │     │
│    │     │     └─ others：其他用户
│    │     └─ group：同组用户
│    └─ user：文件所有者
└─ 文件类型

-    普通文件
d    directory，目录
l    symbolic link，符号链接
c    character device，字符设备
b    block device，块设备
p    pipe/FIFO
s    socket
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;除了这些自带的，文件可以通过&lt;code&gt;xattr&lt;/code&gt;添加任何想要的属性。比如&lt;code&gt;embedding&lt;/code&gt;（VectorFS）。&lt;/p&gt;
&lt;p&gt;文件系统还提供了很多强力的功能。&lt;/p&gt;
&lt;p&gt;如果有监控文件改变的需求（比如写Web的时候热更新，Vscode监控文件变化），Linux提供&lt;code&gt;inotify&lt;/code&gt;这组syscall。通过注册文件描述符，关心的事件（比如修改），就能和&lt;code&gt;epoll&lt;/code&gt;这种机制联动了。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;inotify&lt;/code&gt;本质是内核提供给了我们一套hook机制，修改&lt;code&gt;inode&lt;/code&gt;的时候发现正好有人watch它时就通知。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;eBPF&lt;/code&gt;是一个更强的机制，正好也能做这件事。&lt;code&gt;eBPF&lt;/code&gt;是一个Linux提供的机制，允许把用户态代码注入到内核提供的特定hook里（当然Linux会检查必须是安全的代码）。那其实可以自己实现更丰富的监控逻辑。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;eBPF&lt;/code&gt;还能做很多事。&lt;code&gt;BPF(Berkeley Packet Filter)&lt;/code&gt;原本是一种针对网络包的技术，如果想过滤一些网络包，比如来自某个IP的网络包，那过滤的逻辑得写在用户态代码里，然后才能丢掉。但是如果过滤代码能在内核运行可以快很多，省去很多上下文切换和数据拷贝。这个技术从只针对网络包到针对所有，变成了&lt;code&gt;extended BPF&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;除了监控还有个&lt;code&gt;overlay&lt;/code&gt;的机制很有趣。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;mount&lt;/code&gt;除了挂载，可以把两个目录拼凑成一个虚拟的目录。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;          upper/
             +
          lower/
             │
             ▼
         merged/
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这个目录满足：所有的写入都写进upper（如果是删除lower里有的，在upper打tombstone，如果是修改，从lower里拷贝一份到upper再修改），对于重名文件优先看到upper里的。&lt;/p&gt;
&lt;p&gt;这个功能能让我们实现“网吧管理”这种功能，给每个顾客一个upper就行了，下一个顾客来就换一个upper。&lt;/p&gt;
&lt;p&gt;Docker大量用了这个。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;FROM ubuntu
RUN apt install python3
COPY app /app
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;一开始，lower是ubuntu镜像，然后在upper里装python环境。然后把这当成一个整体lower，在upper里拷贝宿主机的目录。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;┌────────────────────────┐
│ container writable     │ ← upper
├────────────────────────┤
│ app layer              │ ← 这里以及下面都是镜像层（lower）
├────────────────────────┤
│ python layer           │
├────────────────────────┤
│ ubuntu base            │
└────────────────────────┘
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;最后再加一层容器可写层，一个Docker容器就可以跑了。&lt;/p&gt;
&lt;p&gt;如果在容器里&lt;code&gt;echo hello &amp;gt;&amp;gt; /app/main.py&lt;/code&gt;并不会写镜像层的&lt;code&gt;main.py&lt;/code&gt;，而是把这个文件拷贝一份到可写层。所以容器的写入不会影响镜像层，而同一个镜像的多个容器能共享这个镜像，节省空间。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/GPTSOL.png&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;当我问GPT SOL这个机制背后的原理时...&lt;/p&gt;
&lt;h2&gt;实验&lt;/h2&gt;
&lt;p&gt;实验目标是写一个命令行工具&lt;code&gt;fsrecov&lt;/code&gt;尽可能多地从被快速格式化的FAT32镜像里恢复出BMP图像。&lt;/p&gt;
&lt;p&gt;FAT32文件系统的格式化不会真的把块设备里的数据全部清空，而是会清空FAT32里的一个File Allocation Table。相当于只是擦掉了定位真实数据的指针的信息。&lt;/p&gt;
&lt;p&gt;既然数据区没被破坏，只是元数据区被破坏了，那就有一定的办法恢复出一些文件。&lt;/p&gt;
&lt;p&gt;比如实验给的文件都是BMP文件，根据这些文件的特征可以硬编码一些规则。&lt;/p&gt;
</content:encoded></item><item><title>Httpd</title><link>https://cabbage74.github.io/posts/httpd/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/httpd/</guid><description>JYY OS 2026 M7</description><pubDate>Wed, 12 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;理论&lt;/h2&gt;
&lt;p&gt;线程虽然能并发，但线程的创建与切换本身也有代价（8MB的栈，上下文切换），创建和切换线程的代价有可能比函数调用还要大。&lt;/p&gt;
&lt;p&gt;问题是：线程虽然比进程轻，但还是太重，怎么更轻，还能支持大量并发任务？&lt;/p&gt;
&lt;p&gt;对于这种情况，一种思路是设计出更轻量的线程，一种思路是事件驱动异步编程模型。&lt;/p&gt;
&lt;p&gt;先看第一种思路。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import random

THREADS = 1_000_000


def T_worker(name):
    i = 0
    while (i := i + 1):
        yield f&quot;[{name}] i = {i}&quot;


threads = [T_worker(i) for i in range(THREADS)]

count = 0

while count &amp;lt; 10_000_000:
    current = random.choice(threads)
    res = current.send(None) # 等价于 res = next(current)
    print(res)
    count += 1
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python的&lt;code&gt;yield&lt;/code&gt;语法就像是一个生产用户态的简易协作式线程的工具（含有&lt;code&gt;yield&lt;/code&gt;的函数是一个&lt;code&gt;generator&lt;/code&gt;），当&lt;code&gt;generator&lt;/code&gt;运行到&lt;code&gt;yield&lt;/code&gt;时就暂存当前函数的上下文，然后主动让出时间片，这里的&lt;code&gt;random&lt;/code&gt;就像是一个调度器。&lt;/p&gt;
&lt;p&gt;但这本质是在用户态模拟“并发”，由第三方线程库的调度逻辑或者编程语言自己的运行时来调度，内核是感知不到这些用户线程的，所以没有物理并行，内核眼里还是只有一个Python线程在跑，对于CPU密集的任务作用不大。&lt;/p&gt;
&lt;p&gt;但对于I/O密集的任务，这种用户态线程配合非阻塞的I/O API是很有作用的。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;task A： 
CPU 1ms 
read syscall
CPU 1ms

taskB:
...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;假设有这样一个A任务，如果用默认的阻塞式&lt;code&gt;read&lt;/code&gt;，没有东西读就等着，那用户线程跑到这里的时候已经陷入内核开始等待，也无法&lt;code&gt;yield&lt;/code&gt;以后让实际的内核线程去跑B任务。&lt;/p&gt;
&lt;p&gt;但&lt;code&gt;read&lt;/code&gt;其实可以开启&lt;code&gt;O_NONBLOCK&lt;/code&gt;非阻塞模式（OS提供了），没读到会立即返回一个特殊值&lt;code&gt;EAGAIN&lt;/code&gt;。那代码逻辑里其实就可以写好出现这种情况就&lt;code&gt;yield&lt;/code&gt;，先让B任务跑起来。&lt;/p&gt;
&lt;p&gt;:::tip
那么对于非阻塞的API，到底什么时候才该回来读呢？
如果只是过一会就回来读，重复&lt;code&gt;read → EAGAIN → yield&lt;/code&gt;的这个过程，那就还是轮询，并且这里的“一会”是多久也很难确定。
如果能把I/O对象设计成fd，就可以用epoll来监听了！
所以流程应该是&lt;code&gt;read → EAGAIN → 注册fd到epoll → yield → 调度器去跑任务B ... 某一刻fd变成ready → epoll_wait()返回这个fd → 调度器去唤醒任务A&lt;/code&gt;
:::&lt;/p&gt;
&lt;p&gt;这种需要自己写调度器，控制用户线程间如何&lt;code&gt;yield&lt;/code&gt;的模型对程序员的心智负担还是有点大的。&lt;/p&gt;
&lt;p&gt;如果能让程序员仍然按照传统同步阻塞的线程模型写代码，而由语言运行时负责轻量线程的创建、调度、阻塞与唤醒，就可以进一步降低并发编程的负担。Go 的 goroutine 基本就是这条路线。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;func worker() {
    data := readSomething()
    result := process(data)
}

go worker()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这看起来完全是同步阻塞代码，但Go的运行时不会让这个goroutine在&lt;code&gt;read&lt;/code&gt;处阻塞，而是让它睡去，等有消息了再唤醒，背后和&lt;code&gt;epoll&lt;/code&gt;那套是一样的。&lt;/p&gt;
&lt;p&gt;并且Go的运行时会维护若干内核线程与若干goroutine之间的映射，实现物理上的并行。&lt;/p&gt;
&lt;p&gt;Go隐藏了用户线程调度的细节，隐藏了异步I/O的实现机制，但程序员还是需要处理很多并行流，此时就需要用上互斥与同步。&lt;/p&gt;
&lt;p&gt;再看第二种事件驱动的异步编程模型思路。&lt;/p&gt;
&lt;p&gt;JavaScript是这种思路的实践者，因为JavaScript服务于网页，而网页会面临很多事件，比如鼠标不知道什么时候会被点击，网页请求不知道什么时候会结束。&lt;/p&gt;
&lt;p&gt;浏览器需要协调用户输入、脚本执行、网络、定时器和渲染等大量不同时到来的事件，因此使用 event loop 组织这些任务。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;read(fd, callback);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;在JavaScript里，一般都使用非阻塞的I/O API。&lt;/p&gt;
&lt;p&gt;这样的一个&lt;code&gt;read&lt;/code&gt;实际上的行为如下。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;1. 我要读 fd
2. 现在数据还没准备好
3. js runtime 记住：
       fd 123 ready 之后
       要执行 callback
4. 当前 JS 函数返回
5. JS 继续做别的事情
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;在单线程event loop中，一个同步callback一旦开始执行，会run-to-completion，其他callback不会从中间插入，因此普通callback之间通常不需要mutex来防止线程抢占。&lt;/p&gt;
&lt;p&gt;:::tip
什么是event loop？
event loop 不是 JS 语法的一部分，而是浏览器/Node 这样的 runtime 提供的调度机制。
可以粗略理解为：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;while (true) {
    从队列拿一个事件
    执行它对应的 JS callback
    callback 执行完
    再拿下一个
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;传统 JavaScript 的主执行模型是单线程的。大概是这样的模型：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;              event queue
       ┌────────────────────┐
       │ callback A         │
       │ callback B         │
       │ callback C         │
       └──────────┬─────────┘
                  ↓
             event loop
                  ↓
          一个 JS 执行线程
                  ↓
                CPU
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;:::&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;readFile(&quot;a.txt&quot;, function (err, data) {
    if (err) {
        console.error(err);
        return;
    }

    console.log(data);

    // 读完以后要做的后续事情
    process(data);
});

console.log(&quot;我不用等 read 完成&quot;);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;本质上回调函数里就是&lt;code&gt;read&lt;/code&gt;结束以后应该继续做什么。&lt;/p&gt;
&lt;p&gt;所以其实原始的事件驱动的心智模型很简单，由语言的运行时去决定什么时候回来执行这段I/O之后的逻辑。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;readA(function (a) {
    readB(a, function (b) {
        readC(b, function (c) {
            process(c);
        });
    });
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;但这样的写法会容易造成回调地狱，写出不好阅读的代码。&lt;/p&gt;
&lt;p&gt;“未来的结果”和“结果出来以后做什么”现在全部通过函数嵌套表达，控制流越来越难读。&lt;/p&gt;
&lt;p&gt;Promise/Future其实是把“未来的结果”抽象成对象。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;readA(function(a) {
    process(a);
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;变成&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const p = readA();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;p&lt;/code&gt;并不是&lt;code&gt;a&lt;/code&gt;，而是一个未来可能会产生结果的对象，目前是&lt;code&gt;pending&lt;/code&gt;，未来可能变成&lt;code&gt;fulfilled(a)&lt;/code&gt;或&lt;code&gt;rejected(error)&lt;/code&gt;。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;readA()
    .then(function(a) {
        return readB(a);
    })
    .then(function(b) {
        return readC(b);
    })
    .then(function(c) {
        process(c);
    });
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;代码变成这样。&lt;/p&gt;
&lt;p&gt;但程序员还是想像写同步程序一样写异步程序。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const a = readA();
const b = readB(a);
const c = readC(b);
process(c);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;类似这种东西。&lt;/p&gt;
&lt;p&gt;于是就有了async/await语法糖：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;async function work() {
    const a = await readA();
    const b = await readB(a);
    const c = await readC(b);

    process(c);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;看起来像同步代码，但实际上语义是：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;启动 readA
↓
结果没好
↓
暂停当前 async function
↓
把控制权还给 event loop
↓
event loop 跑其他任务
↓
Promise 完成
↓
恢复这个 async function（从上次暂停那一行开始）
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python的async/await也是类似的东西。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import asyncio

async def worker():
    print(&quot;A&quot;)

    data = await read_something()

    print(&quot;B&quot;)
    process(data)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;执行流大概如下。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;worker 运行
↓
print(&quot;A&quot;)
↓
await read_something()
↓
结果还没好
↓
暂停 worker coroutine
↓
控制权回到 event loop
↓
event loop 去运行别的 coroutine
↓
read 完成
↓
event loop 再恢复 worker
↓
data = 结果
↓
print(&quot;B&quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;和JavaScript的区别是Python里这个event loop由asyncio这个库提供，而不是语言运行时提供。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;while True:
    找一个 ready 的 coroutine
    恢复它运行

    它：
        return
        或者 await

    回到 event loop
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这个event loop大概可以这么理解。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import asyncio


async def worker(name):
    print(name, &quot;start&quot;)

    await asyncio.sleep(1)

    print(name, &quot;end&quot;)


async def main():
    await asyncio.gather( # 一下子丢多个coroutine给event loop
        worker(&quot;A&quot;),
        worker(&quot;B&quot;),
        worker(&quot;C&quot;),
    )
    # await worker(&quot;A&quot;)
    # await worker(&quot;B&quot;)
    # await worker(&quot;C&quot;)
    # 这样就是3秒


asyncio.run(main())
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这个例子程序的输出是&lt;code&gt;A start B start C start 1秒 A end B end C end&lt;/code&gt;。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;async def
    → 定义 coroutine

await X
    → 当前 coroutine 等 X，期间可以让 event loop 跑别人

create_task(X)
    → 把 coroutine X 注册成可以独立调度的 Task

gather(A, B, C)
    → 一起调度 A/B/C，并等待它们全部结束

asyncio.run(main())
    → 创建/运行 event loop，从 main 开始跑
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python asyncio的几个语义。&lt;/p&gt;
&lt;p&gt;此时有一种每个语言的并发模型区别很大的感觉，学得会有点累。&lt;/p&gt;
&lt;p&gt;着重点要关注四点，所有并发模型都在回答这四个问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;并发单元是什么——线程?协程?进程?回调?&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;谁来调度——内核?用户态运行时?事件循环?&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;它们怎么通信——共享内存+锁?消息传递?异步返回值?&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;等待时谁被阻塞——阻塞整个内核线程?还是只挂起一个轻量单元?&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;实验&lt;/h2&gt;
&lt;p&gt;根据骨架代码实现一个http服务器，支持并发。&lt;/p&gt;
&lt;p&gt;CGI协议是做这个实验才知道的，Common Gateway Interface（通用网关接口），规定HTTP服务器怎么调用外部程序。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;GET /cgi-bin/hello.py?name=wang HTTP/1.1
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;如果url以&lt;code&gt;cgi-bin&lt;/code&gt;开头，服务器应该启动对应的程序，并把参数通过环境变量和标准输入传进去。程序的输出再包装成http response返回。&lt;/p&gt;
&lt;p&gt;本质是每个请求fork一个新进程：&lt;code&gt;请求进来 → fork + exec 一个解释器跑脚本 → 脚本 stdout 变成响应 → 进程销毁&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;不过CGI已经过时了，现代的开发方法至少是像Go net/http包那样，服务器和应用程序是同一个进程，或者像Nginx那样转发请求，而不是fork新进程。&lt;/p&gt;
</content:encoded></item><item><title>GPT.c</title><link>https://cabbage74.github.io/posts/gptc/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/gptc/</guid><description>JYY OS 2026 M6</description><pubDate>Mon, 10 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;理论&lt;/h2&gt;
&lt;p&gt;互斥只解决了“谁做无所谓，但不能同时做”。&lt;/p&gt;
&lt;p&gt;但有些并发问题对谁来做的先后顺序有要求，比如生产者-消费者问题。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;while (1) {
    lock(&amp;amp;mutex);

    if (!queue_empty()) {
        item = pop();
        unlock(&amp;amp;mutex);
        break;
    }

    unlock(&amp;amp;mutex);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;当然消费者也可以写成这个样子，不停地去看队列里有没有消息。但这又显然在&lt;code&gt;busy waiting&lt;/code&gt;浪费资源。&lt;/p&gt;
&lt;p&gt;于是可以发明条件变量，条件不满足的时候就去睡觉，条件满足了再被唤醒（类似优化自旋锁的思路）。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;pthread_mutex_lock(&amp;amp;mutex);

while (queue_empty()) {
    pthread_cond_wait(&amp;amp;cond, &amp;amp;mutex); // 释放mutex，然后睡觉
}

item = pop();

pthread_mutex_unlock(&amp;amp;mutex);
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;pthread_mutex_lock(&amp;amp;mutex);

push(item);

pthread_mutex_unlock(&amp;amp;mutex);

pthread_cond_signal(&amp;amp;cond);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;条件变量的模版：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;等待条件满足： lock(lk); while(!cond) wait(cv, lk); unlock(lk);
条件可能满足： broadcast(cv); signal(cv); // 唤醒所有or唤醒一个
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;除了条件变量，信号量也可以用来同步，信号量就像是带一个计数器的互斥锁。&lt;/p&gt;
&lt;p&gt;信号量和互斥锁的区别是信号量没有&lt;code&gt;owner&lt;/code&gt;，互斥锁原则上要由同一个线程来上锁解锁，不然就是UB。&lt;/p&gt;
&lt;p&gt;信号量看上去比条件变量好用很多，但是对于复杂条件，比如一些带有“或”的条件，用信号量实现理解起来就没有那么显然，但是用条件变量的话还是直接套模版放在&lt;code&gt;cond&lt;/code&gt;那个位置就行了。&lt;/p&gt;
&lt;h2&gt;实验&lt;/h2&gt;
&lt;p&gt;给了现成的&lt;code&gt;gpt.c&lt;/code&gt;是一个神经网络推理的实现。但是实现是串行的，找到能并行化的部分，改造成并行推理。&lt;/p&gt;
&lt;p&gt;神经网络层内都能并行，是一个&lt;code&gt;spawn join&lt;/code&gt;的简单运用，连锁都不需要。&lt;/p&gt;
</content:encoded></item><item><title>日拱两卒（九）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%B9%9D/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%B9%9D/</guid><description>11 - 20</description><pubDate>Mon, 10 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;20&lt;/h2&gt;
&lt;p&gt;旋转图像。&lt;/p&gt;
&lt;p&gt;顺时针旋转90度。等价于沿副对角线对折，然后上下对折。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def rotate(self, matrix: List[List[int]]) -&amp;gt; None:
        n, m = len(matrix), len(matrix[0])
        for i in range(n):
            for j in range(n - 1 - i):
                matrix[i][j], matrix[n - 1 - j][n - 1 - i] = matrix[n - 1 - j][n - 1 - i], matrix[i][j]

        for i in range(n // 2):
            for j in range(n):
                matrix[i][j], matrix[n - 1 - i][j] = matrix[n - 1 - i][j], matrix[i][j]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;19&lt;/h2&gt;
&lt;p&gt;螺旋矩阵。&lt;/p&gt;
&lt;p&gt;螺旋顺序输出。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def spiralOrder(self, matrix: List[List[int]]) -&amp;gt; List[int]:
        ans = []

        dx = [0, 1, 0, -1]
        dy = [1, 0, -1, 0]
        d = 0

        x, y = 0, 0
        cnt = 0
        n, m = len(matrix), len(matrix[0])
        visited = [[False for _ in range(m)] for _ in range(n)]

        while cnt &amp;lt; n * m:
            ans.append(matrix[x][y])
            cnt += 1
            visited[x][y] = True
            tx, ty = x + dx[d], y + dy[d]
            if tx &amp;gt;= n or tx &amp;lt; 0 or ty &amp;gt;= m or ty &amp;lt; 0 or visited[tx][ty]:
                d = (d + 1) % 4
            tx, ty = x + dx[d], y + dy[d]
            x, y = tx, ty
            

        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;18&lt;/h2&gt;
&lt;p&gt;矩阵置零。&lt;/p&gt;
&lt;p&gt;如果一个元素为零，把它所在的一行和一列都变成零。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;O(n + m)&lt;/code&gt;空间的做法很容易想到，记录每一行和每一列里是否有零。&lt;/p&gt;
&lt;p&gt;这题的考察点很莫名其妙，题干说有无常数空间做法。&lt;/p&gt;
&lt;p&gt;以为会有很巧妙的做法。&lt;/p&gt;
&lt;p&gt;题解的方法也感觉没有很巧妙，就是把记录的东西放在每一行和每一列的第一个元素，再特殊处理第一行与第一列。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def setZeroes(self, matrix: List[List[int]]) -&amp;gt; None:
        n, m = len(matrix), len(matrix[0])
        first_row_zero, first_col_zero = 0, 0
        for j in range(m):
            first_row_zero = first_row_zero or not matrix[0][j]
        for i in range(n):
            first_col_zero = first_col_zero or not matrix[i][0]
        for i in range(1, n):
            for j in range(1, m):
                if not matrix[i][j]:
                    matrix[0][j], matrix[i][0] = 0, 0
        for i in range(1, n):
            for j in range(1, m):
                if not matrix[0][j] or not matrix[i][0]:
                    matrix[i][j] = 0
        if first_row_zero:
            for j in range(m):
                matrix[0][j] = 0
        if first_col_zero:
            for row in matrix:
                row[0] = 0
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;17&lt;/h2&gt;
&lt;p&gt;缺失的第一个正数。&lt;/p&gt;
&lt;p&gt;把每个数换到对应的位置，最多&lt;code&gt;n&lt;/code&gt;个位置。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def firstMissingPositive(self, nums: List[int]) -&amp;gt; int:
        n = len(nums)
        for i in range(n):
            while 1 &amp;lt;= nums[i] &amp;lt;= n and nums[nums[i] - 1] != nums[i]:
                j = nums[i] - 1
                nums[i], nums[j] = nums[j], nums[i]
        
        for i in range(n):
            if nums[i] != i + 1:
                return i + 1
        
        return n + 1
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;16&lt;/h2&gt;
&lt;p&gt;除了自身以外数组的乘积。&lt;/p&gt;
&lt;p&gt;前缀积和后缀积。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def productExceptSelf(self, nums: List[int]) -&amp;gt; List[int]:
        n = len(nums)
        pre = [1 for _ in range(n + 2)]
        suf = [1 for _ in range(n + 2)]
        for i in range(n):
            pre[i + 1] = pre[i] * nums[i]
        for i in range(n, 1, -1):
            suf[i] = suf[i + 1] * nums[i - 1]
        ans = []
        for i in range(1, n + 1, 1):
            ans.append(pre[i - 1] * suf[i + 1])
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;15&lt;/h2&gt;
&lt;p&gt;轮转数组。&lt;/p&gt;
&lt;p&gt;向右K位。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def rotate(self, nums: List[int], k: int) -&amp;gt; None:
        def reverse(l, r):
            i, j = l, r
            while i &amp;lt; j:
                nums[i], nums[j] = nums[j], nums[i]
                i += 1
                j -= 1
        n = len(nums)
        k %= n
        reverse(0, n - 1 - k)
        reverse(n - k, n - 1)
        reverse(0, n - 1)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;14&lt;/h2&gt;
&lt;p&gt;合并区间。&lt;/p&gt;
&lt;p&gt;左端点排序完贪心。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def merge(self, intervals: List[List[int]]) -&amp;gt; List[List[int]]:
        intervals.sort(key=lambda x : (x[0], x[1]))
        
        ans = []
        l, r = intervals[0][0], intervals[0][1]
        i = 1
        n = len(intervals)
        while i &amp;lt; n:
            if intervals[i][0] &amp;gt; r:
                ans.append([l, r])
                l, r = intervals[i][0], intervals[i][1]
            else:
                r = max(r, intervals[i][1])
            i += 1
        ans.append([l, r])
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;13&lt;/h2&gt;
&lt;p&gt;最大子数组和。&lt;/p&gt;
&lt;p&gt;数组里可能有负数。&lt;/p&gt;
&lt;p&gt;把&lt;code&gt;j&lt;/code&gt;那层循环优化成维护一个最小值。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def maxSubArray(self, nums: List[int]) -&amp;gt; int:
        n = len(nums)
        s = [0 for _ in range(n + 1)]
        for i in range(n):
            s[i + 1] = s[i] + nums[i]
        pre_min = [1000000 for _ in range(n + 1)]
        pre_min[0] = s[0]
        ans = -1000000
        for i in range(1, n + 1):
            ans = max(ans, s[i] - pre_min[i - 1])
            pre_min[i] = min(pre_min[i - 1], s[i])
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;12&lt;/h2&gt;
&lt;p&gt;最小覆盖子串。&lt;/p&gt;
&lt;p&gt;返回最短的子串，包含目标串的所有字母。&lt;/p&gt;
&lt;p&gt;滑动窗口维护每个字母的出现次数，右端点固定了以后左端点就可以贪心往右走。&lt;/p&gt;
&lt;p&gt;Python要求字典里的Key得初始化才能用，很麻烦。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;for i in range(26):
    cnt[i] = 0
for i in range(32, 32 + 26):
    cnt[i] = 0
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;如果直接用&lt;code&gt;cnt = {}&lt;/code&gt;需要写这种初始化代码，用&lt;code&gt;defaultdict(int)&lt;/code&gt;会默认有初始值&lt;code&gt;0&lt;/code&gt;。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def minWindow(self, s: str, t: str) -&amp;gt; str:
        ans = &quot;&quot;
        n = len(s)
        m = len(t)

        target = defaultdict(int)
        for i in range(m):
            target[ord(t[i]) - ord(&apos;A&apos;)] += 1
        
        # [l, r)
        l, r = 0, 0 
        cnt = defaultdict(int)

        def check() -&amp;gt; bool:
            for c in target:
                if target[c] &amp;gt; cnt[c]:
                    return False
            return True

        while True:
            if r &amp;lt;= n - 1:
                r += 1
                cnt[ord(s[r - 1]) - ord(&apos;A&apos;)] += 1
            while check():
                if r - l &amp;lt; len(ans) or ans == &quot;&quot;:
                    ans = s[l:r]
                cnt[ord(s[l]) - ord(&apos;A&apos;)] -= 1
                l += 1
            if r == n:
                break
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;11&lt;/h2&gt;
&lt;p&gt;滑动窗口最大值。&lt;/p&gt;
&lt;p&gt;单调队列。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def maxSlidingWindow(self, nums: List[int], k: int) -&amp;gt; List[int]:
        n = len(nums)

        ans = []
        q = deque()
        for r in range(n):
            l = r - k + 1
            while q and q[0] &amp;lt; l:
                q.popleft()
            while q and nums[q[-1]] &amp;lt;= nums[r]:
                q.pop()
            q.append(r)

            if l &amp;gt;= 0:
                ans.append(nums[q[0]])
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>日拱两卒（八）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E5%85%AB/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E5%85%AB/</guid><description>21 - 30</description><pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;30&lt;/h2&gt;
&lt;p&gt;两两交换链表中的节点。&lt;/p&gt;
&lt;p&gt;和K个一组翻转没区别。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def reverse(self, head: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        cur = head
        pre = None

        while cur:
            nxt = cur.next
            cur.next = pre
            pre = cur
            cur = nxt
        
        return pre

    def swapPairs(self, head: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        dummy = ListNode(0, head)

        cur = dummy.next
        cur_head = cur
        pre_cur_head = dummy
        cnt = 0

        while cur:
            cnt += 1
            nxt = cur.next
            if cnt &amp;gt;= 2:
                cur.next = None
                new_head = self.reverse(cur_head)
                cur_head.next = nxt
                pre_cur_head.next = new_head

                pre_cur_head = cur_head
                cur_head = nxt
                cnt = 0

            cur = nxt                
        
        return dummy.next
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;29&lt;/h2&gt;
&lt;p&gt;删除链表的倒数第N个节点。&lt;/p&gt;
&lt;p&gt;尺子一开始左端点在链表头，右端点在正数第N个，那么尺子右端点在链表尾时，左端点就在倒数第N个。&lt;/p&gt;
&lt;p&gt;删除其实是要找第N+1个。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -&amp;gt; Optional[ListNode]:
        left = right = dummy = ListNode(0, head)

        for _ in range(n):
            right = right.next
        while right.next:
            left = left.next
            right = right.next
        left.next = left.next.next

        return dummy.next
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;28&lt;/h2&gt;
&lt;p&gt;两数相加。&lt;/p&gt;
&lt;p&gt;简单模拟。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        dummy = ListNode(0, None)
        
        carry = 0
        cur = dummy
        while l1 or l2:
            val = carry
            val += l1.val if l1 else 0
            val += l2.val if l2 else 0
            new_node = ListNode(val % 10, None)
            cur.next = new_node
            carry = val // 10
            
            cur = cur.next
            l1 = l1.next if l1 else l1
            l2 = l2.next if l2 else l2

        if carry:
            new_node = ListNode(carry, None)
            cur.next = new_node
        
        return dummy.next
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;27&lt;/h2&gt;
&lt;p&gt;合并两个有序链表。&lt;/p&gt;
&lt;p&gt;之前合并K个也做过了。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        dummy = ListNode(0, None)

        cur = dummy
        while list1 and list2:
            if list1.val &amp;lt; list2.val:
                cur.next = ListNode(list1.val, None)
                list1 = list1.next
            else:
                cur.next = ListNode(list2.val, None)
                list2 = list2.next
            cur = cur.next
        
        if list1:
            cur.next = list1
        else:
            cur.next = list2
        
        return dummy.next
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;26&lt;/h2&gt;
&lt;p&gt;环形链表II。&lt;/p&gt;
&lt;p&gt;找环的第一个节点。题解太诡异了，完全不如直接哈希。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def detectCycle(self, head: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        mp = {}

        while head:
            if mp.get(head):
                return head
            mp[head] = True
            head = head.next

        return None
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;25&lt;/h2&gt;
&lt;p&gt;环形链表。&lt;/p&gt;
&lt;p&gt;快慢指针看是否相遇以判环。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def hasCycle(self, head: Optional[ListNode]) -&amp;gt; bool:
        slow = head
        fast = head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            if slow == fast:
                return True
        return False   
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;24&lt;/h2&gt;
&lt;p&gt;回文链表。&lt;/p&gt;
&lt;p&gt;把后半段翻转，逐位比对。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def middle(self, head: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        slow = head
        fast = head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
        return slow
    
    def reverse(self, head: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        cur = head
        pre = None

        while cur:
            nxt = cur.next
            cur.next = pre
            pre = cur
            cur = nxt
        
        return pre


    def isPalindrome(self, head: Optional[ListNode]) -&amp;gt; bool:
        if head.next is None:
            return True
        head2 = self.reverse(self.middle(head))
        while head and head2:
            if head.val != head2.val:
                return False
            head = head.next
            head2 = head2.next
        return True
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;23&lt;/h2&gt;
&lt;p&gt;反转链表。&lt;/p&gt;
&lt;p&gt;因为倒着做，前面用过很多次了。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def reverseList(self, head: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        pre = None
        cur = head
        while cur:
            nxt = cur.next
            cur.next = pre
            pre = cur
            cur = nxt        
        return pre
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;22&lt;/h2&gt;
&lt;p&gt;相交链表。&lt;/p&gt;
&lt;p&gt;没想到题解这种思路，相当于两条路都走两遍的话肯定能找到。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def getIntersectionNode(self, headA: ListNode, headB: ListNode) -&amp;gt; Optional[ListNode]:
        p = headA
        q = headB
        while p is not q:
            p = p.next if p else headB
            q = q.next if q else headA
        return p 
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;21&lt;/h2&gt;
&lt;p&gt;搜索二维矩阵II。&lt;/p&gt;
&lt;p&gt;矩阵满足每行递增，每列递增，搜有无目标值。&lt;/p&gt;
&lt;p&gt;每次看右上角能杀掉一行或一列。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -&amp;gt; bool:
        n, m = len(matrix), len(matrix[0])
        i, j = 0, m - 1
        while i &amp;lt;= n - 1 and j &amp;gt;= 0:
            if matrix[i][j] == target:
                return True
            if matrix[i][j] &amp;gt; target:
                j -= 1
            if matrix[i][j] &amp;lt; target:
                i += 1

        return False
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>C-REPL</title><link>https://cabbage74.github.io/posts/c-repl/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/c-repl/</guid><description>JYY OS 2026 M4</description><pubDate>Tue, 04 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;理论&lt;/h2&gt;
&lt;p&gt;可执行文件到底是什么？&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;┌─────────┐ 高地址
│  stack  │ &amp;lt;- OS
├─────────┤
│    ↓    │ 
│   mmap  │ &amp;lt;- OS
│    ↑    │
├─────────┤ 
│  heap   │ &amp;lt;- OS
├─────────┤
│  bss    │
│  data   │ &amp;lt;- ELF
│  text   │
└─────────┘ 低地址
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这样一个简化的进程地址空间不是全在ELF里的。&lt;/p&gt;
&lt;p&gt;ELF只描述程序初始需要加载哪些东西。如代码段、全局变量。&lt;/p&gt;
&lt;p&gt;而OS负责把它变成一个运行中的进程。&lt;/p&gt;
&lt;p&gt;比如栈是由OS创建的，然后OS把&lt;code&gt;argc, argv, envp&lt;/code&gt;放在初始的栈上。ELF的入口执行点是&lt;code&gt;_start&lt;/code&gt;的地址，CRT相关的代码负责从这个初始栈里取出参数，喂给真正的&lt;code&gt;main&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;ELF(Executable and Linkable Format)不等价于可执行文件，实际上在Linux里有三种。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;.o&lt;/code&gt;可重定位文件，可执行文件，&lt;code&gt;.so&lt;/code&gt;共享目标文件。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#include &amp;lt;stdio.h&amp;gt;

int add(int a, int b);

int main() {
    int result = add(10, 20);

    printf(&quot;result = %d\n&quot;, result);

    return 0;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;int add(int a, int b) {
    return a + b;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;如果&lt;code&gt;gcc main.c&lt;/code&gt;或者&lt;code&gt;gcc add.c&lt;/code&gt;都是不会通过的，一个报找不到&lt;code&gt;add&lt;/code&gt;，一个报找不到&lt;code&gt;main&lt;/code&gt;(&lt;code&gt;_start&lt;/code&gt;函数里调了&lt;code&gt;main&lt;/code&gt;)。&lt;/p&gt;
&lt;p&gt;我们需要把两份代码给链接到一起。链接器会通过符号解析、重定位两步做这件事。&lt;/p&gt;
&lt;p&gt;通过&lt;code&gt;gcc -c xxx&lt;/code&gt;得到&lt;code&gt;.o&lt;/code&gt;的可重定位文件。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ubuntu@VM-0-9-ubuntu:~/elf-lab$ readelf -s main.o

Symbol table &apos;.symtab&apos; contains 7 entries:
   Num:    Value          Size Type    Bind   Vis      Ndx Name
     0: 0000000000000000     0 NOTYPE  LOCAL  DEFAULT  UND
     1: 0000000000000000     0 FILE    LOCAL  DEFAULT  ABS main.c
     2: 0000000000000000     0 SECTION LOCAL  DEFAULT    1 .text
     3: 0000000000000000     0 SECTION LOCAL  DEFAULT    5 .rodata
     4: 0000000000000000    62 FUNC    GLOBAL DEFAULT    1 main
     5: 0000000000000000     0 NOTYPE  GLOBAL DEFAULT  UND add
     6: 0000000000000000     0 NOTYPE  GLOBAL DEFAULT  UND printf
ubuntu@VM-0-9-ubuntu:~/elf-lab$ readelf -s add.o

Symbol table &apos;.symtab&apos; contains 4 entries:
   Num:    Value          Size Type    Bind   Vis      Ndx Name
     0: 0000000000000000     0 NOTYPE  LOCAL  DEFAULT  UND
     1: 0000000000000000     0 FILE    LOCAL  DEFAULT  ABS add.c
     2: 0000000000000000     0 SECTION LOCAL  DEFAULT    1 .text
     3: 0000000000000000    24 FUNC    GLOBAL DEFAULT    1 add
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;能看到&lt;code&gt;main.o&lt;/code&gt;里的&lt;code&gt;add&lt;/code&gt;是&lt;code&gt;UNDEFINED&lt;/code&gt;，而&lt;code&gt;add.o&lt;/code&gt;的符号表确实提供了这个符号，而且是&lt;code&gt;GLOBAL&lt;/code&gt;的。&lt;/p&gt;
&lt;p&gt;链接器是能抓到这个关系的，根据两者的&lt;code&gt;.symtab&lt;/code&gt;能建立起符号如何对应。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ubuntu@VM-0-9-ubuntu:~/elf-lab$ objdump -d main.o

main.o:     file format elf64-x86-64


Disassembly of section .text:

0000000000000000 &amp;lt;main&amp;gt;:
   0:	f3 0f 1e fa          	endbr64
   4:	55                   	push   %rbp
   5:	48 89 e5             	mov    %rsp,%rbp
   8:	48 83 ec 10          	sub    $0x10,%rsp
   c:	be 14 00 00 00       	mov    $0x14,%esi
  11:	bf 0a 00 00 00       	mov    $0xa,%edi
  16:	e8 00 00 00 00       	call   1b &amp;lt;main+0x1b&amp;gt;
  1b:	89 45 fc             	mov    %eax,-0x4(%rbp)
  1e:	8b 45 fc             	mov    -0x4(%rbp),%eax
  21:	89 c6                	mov    %eax,%esi
  23:	48 8d 05 00 00 00 00 	lea    0x0(%rip),%rax        # 2a &amp;lt;main+0x2a&amp;gt;
  2a:	48 89 c7             	mov    %rax,%rdi
  2d:	b8 00 00 00 00       	mov    $0x0,%eax
  32:	e8 00 00 00 00       	call   37 &amp;lt;main+0x37&amp;gt;
  37:	b8 00 00 00 00       	mov    $0x0,%eax
  3c:	c9                   	leave
  3d:	c3                   	ret
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;main.o&lt;/code&gt;不知道&lt;code&gt;add&lt;/code&gt;函数在哪，所以&lt;code&gt;call add&lt;/code&gt;的时候，那个地址是全零。&lt;/p&gt;
&lt;p&gt;上一步符号解析只解决这个&lt;code&gt;add&lt;/code&gt;到底是哪里定义的，还需要重定位过程解决这里地址的填写。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ubuntu@VM-0-9-ubuntu:~/elf-lab$ readelf -r main.o

Relocation section &apos;.rela.text&apos; at offset 0x1d8 contains 3 entries:
  Offset          Info           Type           Sym. Value    Sym. Name + Addend
000000000017  000500000004 R_X86_64_PLT32    0000000000000000 add - 4
000000000026  000300000002 R_X86_64_PC32     0000000000000000 .rodata - 4
000000000033  000600000004 R_X86_64_PLT32    0000000000000000 printf - 4

Relocation section &apos;.rela.eh_frame&apos; at offset 0x220 contains 1 entry:
  Offset          Info           Type           Sym. Value    Sym. Name + Addend
000000000020  000200000002 R_X86_64_PC32     0000000000000000 .text + 0
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;.rela.*&lt;/code&gt;这类section告诉链接器哪里需要修改。&lt;/p&gt;
&lt;p&gt;看&lt;code&gt;objdump&lt;/code&gt;的输出，&lt;code&gt;.text&lt;/code&gt;段的&lt;code&gt;0x16&lt;/code&gt;偏移处是&lt;code&gt;call add&lt;/code&gt;，其中&lt;code&gt;e8&lt;/code&gt;是&lt;code&gt;call&lt;/code&gt;，&lt;code&gt;0x17&lt;/code&gt;就是实际的需要填的&lt;code&gt;add&lt;/code&gt;地址，对应了&lt;code&gt;.rela.text&lt;/code&gt;的第一条。&lt;/p&gt;
&lt;p&gt;当&lt;code&gt;gcc main.o add.o -o main&lt;/code&gt;让链接器同时看到&lt;code&gt;main.o&lt;/code&gt;和&lt;code&gt;add.o&lt;/code&gt;时，它的视角大概是这样。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;main.o

.text
    main

add.o

.text
    add
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;链接器会决定最终的布局。这里的决定方法是由一个默认链接脚本决定的。&lt;code&gt;ld --verbose&lt;/code&gt;能看。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;.text

0x401000:
    main

0x401050:
    add
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;有了这个地址，链接器再填回&lt;code&gt;.rela.*&lt;/code&gt;里告诉链接器需要填的地方。&lt;/p&gt;
&lt;p&gt;总结步骤，链接器看各个&lt;code&gt;.o&lt;/code&gt;文件的&lt;code&gt;.symtab&lt;/code&gt;符号表，解析应该怎么依赖。合并符号表，最终布局决定以后，去把&lt;code&gt;.rela.text&lt;/code&gt;里的需要填的地址填好。符号解析+重定位地址，这就是静态链接。&lt;/p&gt;
&lt;p&gt;静态链接会把代码都打包进一个文件里，但是&lt;code&gt;libc&lt;/code&gt;这种库如果让每个C程序都完整持有一份，显然不合理。&lt;/p&gt;
&lt;p&gt;动态链接让磁盘甚至内存里都只有一份&lt;code&gt;libc&lt;/code&gt;的代码。依赖&lt;code&gt;libc&lt;/code&gt;的进程都链接这一份代码。&lt;/p&gt;
&lt;p&gt;动态链接的过程仍然是类似符号解析加重定位的宗旨。&lt;/p&gt;
&lt;p&gt;但动态链接的定位发生在运行时。不过进程启动时会根据ELF把相关动态库放在&lt;code&gt;mmap&lt;/code&gt;区域（动态库的物理页在系统里还是只有一份），PC在程序运行中会跳到&lt;code&gt;mmap&lt;/code&gt;区域。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;main

.text
    main:
        xxx
    add:
        xxx

.dynamic
    动态库链接需要什么东西（需要什么动态库，动态符号表在哪，重定位表在哪等等）

.plt
    printf跳板

.got
    保存printf真实地址

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;静态链接的&lt;code&gt;add&lt;/code&gt;最终就会在&lt;code&gt;ELF&lt;/code&gt;里，所以重定位的时候很方便拿到一个相对地址填到&lt;code&gt;main&lt;/code&gt;里全是&lt;code&gt;0&lt;/code&gt;的地方。&lt;/p&gt;
&lt;p&gt;但是&lt;code&gt;printf&lt;/code&gt;的代码段不会进&lt;code&gt;ELF&lt;/code&gt;。这里会填&lt;code&gt;call printf@plt&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;execve&lt;/code&gt;加载一个ELF的时候，如果发现需要动态链接，会先启动动态链接器&lt;code&gt;ld.so&lt;/code&gt;再跳到&lt;code&gt;_start&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;动态链接器会看&lt;code&gt;.dynamic&lt;/code&gt;看需要什么。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;plt(Procedure Linkage Table)&lt;/code&gt;可以理解为一个中转站，会去查&lt;code&gt;got(Global Offset Table)&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;got&lt;/code&gt;是一个简单的表，可能就长下面这样。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;GOT:
+----------------+
| printf地址     |
+----------------+
| malloc地址     |
+----------------+
| xxx地址        |
+----------------+
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;但一开始&lt;code&gt;got&lt;/code&gt;里面的地址是通向&lt;code&gt;ld.so&lt;/code&gt;的。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;printf@plt&lt;/code&gt;里的代码会跳到&lt;code&gt;got&lt;/code&gt;表，然后如果是第一次，通向&lt;code&gt;ld.so&lt;/code&gt;，由它找到真的&lt;code&gt;printf&lt;/code&gt;，填到&lt;code&gt;got&lt;/code&gt;里，后续就不需要再走&lt;code&gt;ld.so&lt;/code&gt;了。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;got&lt;/code&gt;在这里有点像缓存，之所以不让&lt;code&gt;ld.so&lt;/code&gt;一次性全部查完，而是有需要的时候一个一个查，是因为有些函数不一定需要查，这样更合理。&lt;/p&gt;
&lt;p&gt;其实这里也解释了为什么程序运行的时候不要手动移动动态库位置了，因为&lt;code&gt;got&lt;/code&gt;里的地址就失效了。除非重启进程。&lt;/p&gt;
&lt;p&gt;:::tip
我其实没太理解&lt;code&gt;plt&lt;/code&gt;有什么用，感觉直接&lt;code&gt;call printf@got&lt;/code&gt;也可以。
GPT说其实也可以，&lt;code&gt;plt&lt;/code&gt;的存在有历史原因，有些系统没有&lt;code&gt;plt&lt;/code&gt;。
:::&lt;/p&gt;
&lt;h2&gt;实验&lt;/h2&gt;
&lt;p&gt;这是一个Python REPL。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# cabbage @ cabbage-Mac in ~/blog on git:main x [19:44:36] C:127
$ python3        
Python 3.13.2 (main, Feb  4 2025, 14:51:09) [Clang 16.0.0 (clang-1600.0.26.6)] on darwin
Type &quot;help&quot;, &quot;copyright&quot;, &quot;credits&quot; or &quot;license&quot; for more information.
Cmd click to launch VS Code Native REPL
&amp;gt;&amp;gt;&amp;gt; 1 + 1
2
&amp;gt;&amp;gt;&amp;gt; 
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;本次实验实现一个有约束的简易C-REPL。&lt;/p&gt;
&lt;p&gt;只支持两个功能：能算表达式，能定义一行返回值为&lt;code&gt;int&lt;/code&gt;，参数类型都为&lt;code&gt;int&lt;/code&gt;的函数。&lt;/p&gt;
&lt;p&gt;我之前一直以为Python这种解释型语言才能这样，长见识ing...&lt;/p&gt;
&lt;p&gt;做法是把每一行输入都放进一个临时的&lt;code&gt;c&lt;/code&gt;文件，编译成共享库加载执行。&lt;/p&gt;
&lt;p&gt;如果是表达式就把它包装成函数（直接返回那种函数），同样编译成共享库加载执行。&lt;/p&gt;
&lt;p&gt;本质就是运用&lt;code&gt;dlopen, dlsym, dlclose&lt;/code&gt;这几个函数。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ubuntu@VM-0-9-ubuntu:~/os2026/crepl$ ./crepl 
C REPL - 输入表达式或函数定义

&amp;gt; 1 + 1
2

&amp;gt; int gcd(int a, int b) { return b ? gcd(b, a % b) : a; }
OK: gcd() defined.

&amp;gt; gcd(2, 4)
2

&amp;gt; f()
compile error
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>日拱两卒（七）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%B8%83/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%B8%83/</guid><description>31 - 40</description><pubDate>Sun, 02 Aug 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;40&lt;/h2&gt;
&lt;p&gt;二叉树的直径。&lt;/p&gt;
&lt;p&gt;这里的坑是长度是按边计算的，不是节点数。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -&amp;gt; int:
        ans = 0

        def dfs(cur) -&amp;gt; int:
            nonlocal ans
            if cur is None:
                return 0
            l = dfs(cur.left) + 1
            r = dfs(cur.right) + 1
            nonlocal ans
            ans = max(ans, l + r - 2)
            return max(l, r)

        dfs(root)   
        return ans     
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;39&lt;/h2&gt;
&lt;p&gt;对称二叉树。&lt;/p&gt;
&lt;p&gt;不能拍成中序遍历看回文，是有反例的。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def isSame(self, p, q) -&amp;gt; bool:
        if p is None or q is None:
            return p is q
        if p.val != q.val:
            return False
        return self.isSame(p.left, q.right) and self.isSame(p.right, q.left)
        
    def isSymmetric(self, root: Optional[TreeNode]) -&amp;gt; bool:
        return self.isSame(root.left, root.right)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;38&lt;/h2&gt;
&lt;p&gt;翻转二叉树。&lt;/p&gt;
&lt;p&gt;简单递归。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def invertTree(self, root: Optional[TreeNode]) -&amp;gt; Optional[TreeNode]:
        if root is None:
            return None
        tmp = deepcopy(root.right)
        root.right = self.invertTree(root.left)
        root.left = self.invertTree(tmp)
        return root
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;37&lt;/h2&gt;
&lt;p&gt;二叉树的最大深度。&lt;/p&gt;
&lt;p&gt;简单递归。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -&amp;gt; int:
        if root is None:
            return 0
        return max(self.maxDepth(root.left) + 1, self.maxDepth(root.right) + 1)        
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;36&lt;/h2&gt;
&lt;p&gt;二叉树的中序遍历。&lt;/p&gt;
&lt;p&gt;简单递归。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def inorderTraversal(self, root: Optional[TreeNode]) -&amp;gt; List[int]:
        ans = []

        def dfs(cur):
            if cur is None:
                return
            dfs(cur.left)
            ans.append(cur.val)
            dfs(cur.right)

        dfs(root)

        return ans        
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;35&lt;/h2&gt;
&lt;p&gt;LRU缓存。&lt;/p&gt;
&lt;p&gt;维护一个每个节点含有KV信息的双向链表+一个用于快速定位的哈希表。&lt;/p&gt;
&lt;p&gt;Python竟然有现成的数据结构&lt;code&gt;OrderedDict&lt;/code&gt;。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class LRUCache:

    def __init__(self, capacity: int):
        self.capacity = capacity
        self.cache = OrderedDict()

    def get(self, key: int) -&amp;gt; int:
        if key not in self.cache:
            return -1
        self.cache.move_to_end(key, last=False)
        return self.cache[key]

    def put(self, key: int, value: int) -&amp;gt; None:
        self.cache[key] = value
        self.cache.move_to_end(key, last=False)
        if len(self.cache) &amp;gt; self.capacity:
            self.cache.popitem()
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;34&lt;/h2&gt;
&lt;p&gt;合并K个升序链表。&lt;/p&gt;
&lt;p&gt;递归左右两部分再合二为一。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def mergeTwo(self, left: Optional[ListNode], right: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        dummy = ListNode()
        cur = dummy
        
        while left and right:
            if left.val &amp;lt; right.val:
                cur.next = left
                left = left.next
            else:
                cur.next = right
                right = right.next
            cur = cur.next
        cur.next = left if left else right

        return dummy.next
    
    def mergeKLists(self, lists: List[Optional[ListNode]]) -&amp;gt; Optional[ListNode]:
        k = len(lists)
        if k == 0:
            return None
        if k == 1:
            return lists[0]
        left = self.mergeKLists(lists[: k // 2])
        right = self.mergeKLists(lists[k // 2 :])
        return self.mergeTwo(left, right)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;33&lt;/h2&gt;
&lt;p&gt;排序链表。&lt;/p&gt;
&lt;p&gt;感觉写起来很烦，一看题解确实要写这么烦。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def get_middle(self, head: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        if head is None:
            return None

        slow = head
        fast = head
        while fast.next and fast.next.next:
            fast = fast.next.next
            slow = slow.next
        
        res = slow.next
        slow.next = None
        return res
        
    def mergeTwo(self, left: Optional[ListNode], right: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        dummy = ListNode()
        cur = dummy
        
        while left and right:
            if left.val &amp;lt; right.val:
                cur.next = left
                left = left.next
            else:
                cur.next = right
                right = right.next
            cur = cur.next
        cur.next = left if left else right

        return dummy.next


    def sortList(self, head: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        if head is None or head.next is None:
            return head
        
        middle = self.get_middle(head)

        left = self.sortList(head)
        right = self.sortList(middle)

        return self.mergeTwo(left, right)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;32&lt;/h2&gt;
&lt;p&gt;随机链表的复制。&lt;/p&gt;
&lt;p&gt;目前题干最莫名其妙的一题。&lt;/p&gt;
&lt;p&gt;深拷贝每个节点多一个随机指针的链表，可以用哈希表解决。题解做法也是目前最出乎意料，每个节点原地复制，然后拆出最后的结果。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def copyRandomList(self, head: &apos;Optional[Node]&apos;) -&amp;gt; &apos;Optional[Node]&apos;:
        cur = head
        while cur:
            cur.next = Node(cur.val, cur.next)
            cur = cur.next.next

        cur = head
        while cur:
            if cur.random:
                cur.next.random = cur.random.next
            cur = cur.next.next

        cur = dummy = Node(0, head)
        while cur.next:
            cur.next = cur.next.next
            cur = cur.next

        return dummy.next      
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;31&lt;/h2&gt;
&lt;p&gt;K个一组翻转链表。&lt;/p&gt;
&lt;p&gt;有些繁琐。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def reverse(self, head: Optional[ListNode]) -&amp;gt; Optional[ListNode]:
        prev = None
        cur = head
        while cur:
            nxt = cur.next
            cur.next = prev
            prev = cur
            cur = nxt
        return prev

    def reverseKGroup(self, head: Optional[ListNode], k: int) -&amp;gt; Optional[ListNode]:
        dummy = ListNode(0, head)

        cur = dummy.next
        cnt = 0
        cur_head = cur
        pre_cur_head = dummy
        while cur:
            cnt += 1
            nxt = cur.next
            if cnt &amp;gt;= 2:
                cur.next = None
                new_head = self.reverse(cur_head)
                cur_head.next = nxt
                pre_cur_head.next = new_head

                pre_cur_head = cur_head
                cur_head = nxt
                cnt = 0

            cur = nxt

        return dummy.next
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>SPerf</title><link>https://cabbage74.github.io/posts/sperf/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/sperf/</guid><description>JYY OS 2026 M3</description><pubDate>Tue, 28 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;理论&lt;/h2&gt;
&lt;p&gt;ABI是规定已经编译好的程序如何交互的规范。&lt;/p&gt;
&lt;p&gt;它包含但不限于以下方面：调用函数时参数放哪里？结构体内存布局如何Padding？函数名如何修饰？&lt;/p&gt;
&lt;p&gt;类似CSAPP的书在教学的时候一般都是这样的心智模型：调用函数时，参数、局部变量、返回地址都扔到栈上。&lt;/p&gt;
&lt;p&gt;这其实就是一种具体的ABI。&lt;/p&gt;
&lt;p&gt;在x86-64的系统上其实是另一种规范，第一个参数会放到rdi上，第二个参数会放到rsi上...&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;int add(int a, int b);

int main()
{
    int x = add(10, 20);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;假设&lt;code&gt;add&lt;/code&gt;是我们写的一个汇编函数，为什么C语言能正确调用汇编呢？&lt;/p&gt;
&lt;p&gt;因为Linux x86-64的ABI规定了第一个参数会放到rdi上，第二个参数会放到rsi上。&lt;/p&gt;
&lt;p&gt;所以只要我们写的汇编函数确实是这样就能调用。这里的寄存器e开头指的是只拿低32位。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;add:
    mov eax, edi
    add eax, esi
    ret
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;也就是说如果需要裸写汇编，对于Linux和Windows两个平台就得维护两份代码，对应两种ABI。&lt;/p&gt;
&lt;p&gt;但如果用C语法写&lt;code&gt;add&lt;/code&gt;函数，编译器会根据ABI生成出正确的汇编。&lt;/p&gt;
&lt;p&gt;C的ABI是事实上的通用接口，只要符合C的ABI就可以调用C函数，比如&lt;code&gt;libc&lt;/code&gt;库里的函数。&lt;/p&gt;
&lt;h2&gt;实验&lt;/h2&gt;
&lt;p&gt;实现一个命令行工具，能启动另一个程序，然后统计这个程序里每个系统调用的时间。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;strace -T&lt;/code&gt;其实有这个功能。JYY给的方法是父进程启动子进程跑&lt;code&gt;strace&lt;/code&gt;，用管道拿到以后直接解析。&lt;/p&gt;
&lt;p&gt;Linux还提供了&lt;code&gt;ptrace&lt;/code&gt;这个系统调用，允许一个进程观察和控制另一个进程的执行。&lt;code&gt;strace, gdb&lt;/code&gt;这些都基于这个。&lt;/p&gt;
</content:encoded></item><item><title>日拱两卒（六）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E5%85%AD/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E5%85%AD/</guid><description>41 - 50</description><pubDate>Tue, 28 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;50&lt;/h2&gt;
&lt;p&gt;二叉树中的最大路径和。&lt;/p&gt;
&lt;p&gt;维护每个节点的左右两条链。&lt;/p&gt;
&lt;p&gt;因为有负数，注意和零比较。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def maxPathSum(self, root: Optional[TreeNode]) -&amp;gt; int:
        if root == None:
            return 0
        
        N = 3 * 10000 + 10
        ans = -N * 1000

        s = dict()
        
        def dfs(u):
            nonlocal ans
            if u.left is None:
                s[(u, 0)] = 0
            if u.right is None:
                s[(u, 1)] = 0
            if u.left:
                dfs(u.left)
                s[(u, 0)] = max(0, max(s[(u.left, 0)], s[(u.left, 1)]) + u.left.val)
            if u.right:
                dfs(u.right)
                s[(u, 1)] = max(0, max(s[(u.right, 0)], s[(u.right, 1)]) + u.right.val)
            ans = max(ans, u.val + s[(u, 0)] + s[(u, 1)])

        dfs(root)

        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;49&lt;/h2&gt;
&lt;p&gt;二叉树的最近公共祖先。&lt;/p&gt;
&lt;p&gt;简单递归。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def lowestCommonAncestor(self, root: &apos;TreeNode&apos;, p: &apos;TreeNode&apos;, q: &apos;TreeNode&apos;) -&amp;gt; &apos;TreeNode&apos;:
        if root is None or root == p or root == q:
            return root
        
        l = self.lowestCommonAncestor(root.left, p, q)
        r = self.lowestCommonAncestor(root.right, p, q)
        
        if l and r:
            return root
        if l:
            return self.lowestCommonAncestor(l, p, q)
        if r:
            return self.lowestCommonAncestor(r, p, q)
        return None
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;48&lt;/h2&gt;
&lt;p&gt;路径总和III。&lt;/p&gt;
&lt;p&gt;求路径和等于Target的路径数。这里的路径只向下。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def pathSum(self, root: Optional[TreeNode], targetSum: int) -&amp;gt; int:
        def getSum(root, target) -&amp;gt; int:
            if root is None:
                return 0
            res = 0
            if root.val == target:
                res += 1
            res += getSum(root.left, target - root.val)
            res += getSum(root.right, target - root.val)
            return res
        
        if root is None:
            return 0
        return getSum(root, targetSum) + self.pathSum(root.left, targetSum) + self.pathSum(root.right, targetSum)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;47&lt;/h2&gt;
&lt;p&gt;从前序与中序遍历序列构造二叉树。&lt;/p&gt;
&lt;p&gt;这个初见不会，甚至完全不知道怎么做。&lt;/p&gt;
&lt;p&gt;通过看前序的第一个元素能找到根，在中序里找到这个元素，那么就知道了左子树和右子树的大小。&lt;/p&gt;
&lt;p&gt;然后就去递归解子问题。&lt;/p&gt;
&lt;p&gt;好巧妙，我的脑子好僵化。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -&amp;gt; Optional[TreeNode]:
        if not preorder:
            return None
        left_cnt = inorder.index(preorder[0])
        left = self.buildTree(preorder[1: 1 + left_cnt], inorder[:left_cnt])
        right = self.buildTree(preorder[1 + left_cnt:], inorder[1 + left_cnt:])
        return TreeNode(preorder[0], left, right)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;46&lt;/h2&gt;
&lt;p&gt;二叉树展开为链表。&lt;/p&gt;
&lt;p&gt;右左根遍历从后往前构造。不要忘记把左指针置空。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def flatten(self, root: Optional[TreeNode]) -&amp;gt; None:
        if root is None:
            return

        end = None

        def dfs(cur):
            nonlocal end
            if cur.right:
                dfs(cur.right)
            if cur.left:
                dfs(cur.left)
            cur.right = end
            cur.left = None
            end = cur

        dfs(root)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;45&lt;/h2&gt;
&lt;p&gt;二叉树的右视图。&lt;/p&gt;
&lt;p&gt;按深度遍历，每一层的最右边是答案。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def rightSideView(self, root: Optional[TreeNode]) -&amp;gt; List[int]:
        ans = []
        
        def dfs(root, depth):
            if root is None:
                return
                
            if len(ans) &amp;gt; depth:
                ans[depth] = root.val
            else:
                ans.append(root.val)

            dfs(root.left, depth + 1)
            dfs(root.right, depth + 1)
            

        dfs(root, 0)
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;44&lt;/h2&gt;
&lt;p&gt;二叉搜索树中第K小的元素。&lt;/p&gt;
&lt;p&gt;按子树大小来筛。&lt;/p&gt;
&lt;p&gt;题解更简单，二叉搜索树的中序遍历是一个递增序列，其实就是求中序遍历的第K个元素。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def kthSmallest(self, root: Optional[TreeNode], k: int) -&amp;gt; int:
        @cache
        def sz(cur) -&amp;gt; int:
            if cur is None:
                return 0
            sz_left = sz(cur.left)
            sz_right = sz(cur.right)
            return 1 + sz_left + sz_right

        sz(root)

        def dfs(cur, k) -&amp;gt; int:
            sz_left = sz(cur.left)
            if k == sz_left + 1:
                return cur.val
            elif k &amp;lt;= sz_left:
                return dfs(cur.left, k)
            elif k &amp;gt; sz_left + 1:
                return dfs(cur.right, k - sz_left - 1)

        return dfs(root, k)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;43&lt;/h2&gt;
&lt;p&gt;验证二叉搜索树。&lt;/p&gt;
&lt;p&gt;昨晚上面那道题就知道这里可以检查中序遍历。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def isValidBST(self, root: Optional[TreeNode]) -&amp;gt; bool:
        check = []

        def dfs(cur):
            if cur is None:
                return
            dfs(cur.left)
            check.append(cur.val)
            dfs(cur.right)

        dfs(root)

        for i in range(len(check) - 1):
            if check[i] &amp;gt;= check[i + 1]:
                return False
        return True
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;42&lt;/h2&gt;
&lt;p&gt;将有序数组转为二叉平衡搜索树。&lt;/p&gt;
&lt;p&gt;挑出中间的数，简单递归。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def sortedArrayToBST(self, nums: List[int]) -&amp;gt; Optional[TreeNode]:
        if len(nums) == 0:
            return None
        return TreeNode(val=nums[len(nums) // 2], left=self.sortedArrayToBST(nums[:len(nums) // 2]), right=self.sortedArrayToBST(nums[len(nums) // 2 + 1:]))
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;41&lt;/h2&gt;
&lt;p&gt;二叉树的层序遍历。&lt;/p&gt;
&lt;p&gt;按层级输出。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -&amp;gt; List[List[int]]:
        ans = []
        def dfs(cur, dep):
            if cur is None:
                return
            if dep &amp;gt;= len(ans):
                ans.append([cur.val])
            else:
                ans[dep].append(cur.val)
            dfs(cur.left, dep + 1)
            dfs(cur.right, dep + 1)
            
        dfs(root, 0)
        return ans
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>PSTree</title><link>https://cabbage74.github.io/posts/pstree/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/pstree/</guid><description>JYY OS 2026 M2</description><pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;理论&lt;/h2&gt;
&lt;p&gt;应用程序需要请求操作系统来做一件事情时，本质上都是要访问“操作系统中的某个对象”。&lt;/p&gt;
&lt;p&gt;Unix的哲学是尽可能将这些对象以文件形式暴露出来，比如&lt;code&gt;/proc&lt;/code&gt;目录。&lt;/p&gt;
&lt;p&gt;文件描述符是这个“一切皆文件”哲学的基础。它的本质是一个可替换的I/O通道编号。&lt;/p&gt;
&lt;p&gt;“一切皆文件”更准确的描述是Unix将大量资源抽象为可通过文件描述符访问的对象。&lt;/p&gt;
&lt;p&gt;假设没有文件描述符，当程序通过&lt;code&gt;open&lt;/code&gt;打开一个文件时，内核需要返回内核里文件的地址（或者其他类似的东西），但这既不安全，也破坏了抽象（程序不应该需要知道磁盘是怎么组织的）。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;int fd = open(&quot;a.txt&quot;, O_RDONLY); // fd = 3
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这个3是如何与实际文件关联起来的呢？&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;用户空间:
fd = 3

内核空间:
+----------------+
| fd table       |
+----------------+
|0 | stdin       |
|1 | stdout      |
|2 | stderr      |
|3 | a.txt       |
+----------------+
        |
        v
... 很多层
        |
        v
struct file
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这个&lt;code&gt;fd table&lt;/code&gt;在Linux内核里位于&lt;code&gt;task_struct&lt;/code&gt;里的&lt;code&gt;files_struct&lt;/code&gt;里面。每个进程都有&lt;code&gt;task_struct&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;由于文件描述符只是一个数字索引，可以很方便地统一所有资源。除了&lt;code&gt;file&lt;/code&gt;，其他&lt;code&gt;socket&lt;/code&gt;、&lt;code&gt;pipe&lt;/code&gt;之类的东西也可以用&lt;code&gt;int&lt;/code&gt;管理了。&lt;/p&gt;
&lt;p&gt;每个Unix进程启动的时候&lt;code&gt;stdin, stdout, stderr&lt;/code&gt;默认占三个文件描述符。&lt;/p&gt;
&lt;p&gt;文件描述符是Unix管道的核心。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ubuntu@VM-0-9-ubuntu:~$ ls | grep txt
a.txt
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;下面研究这条命令背后到底发生了什么。&lt;/p&gt;
&lt;p&gt;在最开始，&lt;code&gt;shell&lt;/code&gt;的&lt;code&gt;fd table&lt;/code&gt;如下。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;fd:
0 stdin
1 stdout
2 stderr
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;shell&lt;/code&gt;进程接收到这条命令以后，发现命令涉及管道，调用&lt;code&gt;pipe&lt;/code&gt;这个syscall。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;pipe&lt;/code&gt;这个syscall会创建一块内核缓冲区，并返回两个fd，一个读端一个写端。&lt;/p&gt;
&lt;p&gt;此时&lt;code&gt;shell&lt;/code&gt;的&lt;code&gt;fd table&lt;/code&gt;如下。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;fd:
0 stdin
1 stdout
2 stderr
3 pipe读端
4 pipe写端
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;现在正式调用&lt;code&gt;fork&lt;/code&gt;召唤出&lt;code&gt;ls&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;fork&lt;/code&gt;会原样复制&lt;code&gt;fd table&lt;/code&gt;。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;fd:
0 stdin
1 stdout
2 stderr
3 pipe读端
4 pipe写端
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;此时&lt;code&gt;ls&lt;/code&gt;实际上和&lt;code&gt;shell&lt;/code&gt;没区别，在这个时候&lt;code&gt;execve&lt;/code&gt;的话&lt;code&gt;ls&lt;/code&gt;的输出还是会去&lt;code&gt;stdout&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;我们想让&lt;code&gt;ls&lt;/code&gt;往&lt;code&gt;pipe&lt;/code&gt;的写端里写，而不是往&lt;code&gt;stdout&lt;/code&gt;里写。通过&lt;code&gt;dup2(4,1)&lt;/code&gt;这个syscall把4号&lt;code&gt;fd&lt;/code&gt;复制到1号。再&lt;code&gt;close(3), close(4)&lt;/code&gt;把无关端口关掉。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;fd:
0 stdin
1 pipe写端
2 stderr
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;此时再&lt;code&gt;execve&lt;/code&gt;加载&lt;code&gt;ls&lt;/code&gt;。&lt;code&gt;ls&lt;/code&gt;的代码完全没有改动，但输出时&lt;code&gt;printf&lt;/code&gt;里面的&lt;code&gt;write(1, ..)&lt;/code&gt;这个系统调用无感知地往&lt;code&gt;pipe&lt;/code&gt;的写端里写了。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;grep&lt;/code&gt;同理，会经历类似上面的过程。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;fd:
0 pipe读端
1 stdout
2 stderr
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;总的来说，管道的实现思想是修改程序看到的&lt;code&gt;fd&lt;/code&gt;表。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;stty -a&lt;/code&gt;可以看当前终端的绑定键。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;stty -a | claude &apos;这几个快捷键都有什么用&apos;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Unix也有类似Windows的任务管理。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Ctrl-z 放后台
jobs 就像是Windows的任务管理器
fg/bg %n 把第n个job移到前台和后台
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;实验&lt;/h2&gt;
&lt;p&gt;扫描&lt;code&gt;/proc&lt;/code&gt;里每一个数字进程的&lt;code&gt;status&lt;/code&gt;，拿&lt;code&gt;ppid&lt;/code&gt;。扫描&lt;code&gt;comm&lt;/code&gt;拿名字。&lt;/p&gt;
&lt;p&gt;建图，打印。&lt;/p&gt;
&lt;p&gt;和Linux的实现的区别包括但不限于：Linux的实现还会打印出线程，是用花括号包裹的。同名的默认会被折叠成&lt;code&gt;n*[xxx]&lt;/code&gt;的形式。&lt;/p&gt;
</content:encoded></item><item><title>Labyrinth</title><link>https://cabbage74.github.io/posts/labyrinth/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/labyrinth/</guid><description>JYY OS 2026 M1</description><pubDate>Thu, 23 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;理论&lt;/h2&gt;
&lt;p&gt;进程自身只看得到寄存器和内存里的内容，如果想获取其他自身的信息需要和OS交互。&lt;/p&gt;
&lt;p&gt;一种是可以通过&lt;code&gt;getpid&lt;/code&gt;之类的的syscalls。&lt;/p&gt;
&lt;p&gt;一种是&lt;code&gt;/proc&lt;/code&gt;目录下的文件，&lt;code&gt;/proc&lt;/code&gt;是一个伪文件系统，挂载在内存里不落盘。&lt;/p&gt;
&lt;p&gt;Unix通过&lt;code&gt;fork&lt;/code&gt;和&lt;code&gt;execve&lt;/code&gt;两个API来复制和复位状态机。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;pid_t pid = fork();
if (pid == 0) {
    // 子进程：我现在是父进程的完整克隆
    // 但我可以用自己的系统调用&quot;重新配置自己&quot;
    
    close(0);                    // 关掉继承的 stdin
    open(&quot;/dev/null&quot;, O_RDONLY); // 重定向到 /dev/null
    dup2(pipe_fd, 1);            // stdout 重定向到管道
    chdir(&quot;/some/dir&quot;);          // 切换工作目录
    setenv(&quot;PATH&quot;, &quot;...&quot;, 1);   // 修改环境变量
    setgid(new_gid);            // 降权
    setuid(new_uid);            // 降权
    // ... 任何你能想到的状态修改
    
    execve(&quot;/bin/myprog&quot;, argv, envp);  // 最后才加载新程序
} else {
    ...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;上面的代码创建了一个进程，在&lt;code&gt;execve&lt;/code&gt;之前用代码自由的执行了降权之类的操作。&lt;/p&gt;
&lt;p&gt;这体现了Unix的&quot;机制而非策略&quot;思想：内核提供 fork和 execve两个正交的机制，组合方式由用户态决定。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;BOOL CreateProcess(
  LPCTSTR               lpApplicationName,
  LPTSTR                lpCommandLine,
  LPSECURITY_ATTRIBUTES lpProcessAttributes,
  LPSECURITY_ATTRIBUTES lpThreadAttributes,
  BOOL                  bInheritHandles,
  DWORD                 dwCreationFlags,
  LPVOID                lpEnvironment,
  LPCTSTR               lpCurrentDirectory,
  LPSTARTUPINFO         lpStartupInfo,
  LPPROCESS_INFORMATION lpProcessInformation
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;相对地，Windows设计&lt;code&gt;spawn&lt;/code&gt;这类API需要维护非常多参数来尽量满足创建不同种类进程需要的能力。&lt;/p&gt;
&lt;p&gt;教科书上一个简化的进程地址空间如下。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;┌─────────┐ 高地址
│  stack  │
├─────────┤
│    ↓    │
│   mmap  │
│    ↑    │
├─────────┤ ← break
│  heap   │
├─────────┤
│  bss    │
│  data   │
│  text   │
└─────────┘ 低地址
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;可以通过&lt;code&gt;brk&lt;/code&gt;系统调用设置&lt;code&gt;break&lt;/code&gt;的位置来控制堆区大小。历史上C语言里有一个&lt;code&gt;sbrk&lt;/code&gt;函数是更高级的封装，现在被废弃了。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;mmap&lt;/code&gt;操纵内存映射区。&lt;/p&gt;
&lt;p&gt;想要看一个Linux进程具体的进程地址空间信息，可以用下面的命令。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ubuntu@VM-0-9-ubuntu:~/os2026$ cat /proc/self/maps
5647336d4000-5647336d6000 r--p 00000000 fc:02 926                        /usr/bin/cat
5647336d6000-5647336da000 r-xp 00002000 fc:02 926                        /usr/bin/cat
5647336da000-5647336dc000 r--p 00006000 fc:02 926                        /usr/bin/cat
5647336dc000-5647336dd000 r--p 00007000 fc:02 926                        /usr/bin/cat
5647336dd000-5647336de000 rw-p 00008000 fc:02 926                        /usr/bin/cat
56476abe5000-56476ac06000 rw-p 00000000 00:00 0                          [heap]
7f76c0218000-7f76c023a000 rw-p 00000000 00:00 0 
7f76c023a000-7f76c0523000 r--p 00000000 fc:02 2694                       /usr/lib/locale/locale-archive
7f76c0523000-7f76c0526000 rw-p 00000000 00:00 0 
7f76c0526000-7f76c054e000 r--p 00000000 fc:02 11781                      /usr/lib/x86_64-linux-gnu/libc.so.6
7f76c054e000-7f76c06e3000 r-xp 00028000 fc:02 11781                      /usr/lib/x86_64-linux-gnu/libc.so.6
7f76c06e3000-7f76c073b000 r--p 001bd000 fc:02 11781                      /usr/lib/x86_64-linux-gnu/libc.so.6
7f76c073b000-7f76c073c000 ---p 00215000 fc:02 11781                      /usr/lib/x86_64-linux-gnu/libc.so.6
7f76c073c000-7f76c0740000 r--p 00215000 fc:02 11781                      /usr/lib/x86_64-linux-gnu/libc.so.6
7f76c0740000-7f76c0742000 rw-p 00219000 fc:02 11781                      /usr/lib/x86_64-linux-gnu/libc.so.6
7f76c0742000-7f76c074f000 rw-p 00000000 00:00 0 
7f76c0758000-7f76c075a000 rw-p 00000000 00:00 0 
7f76c075a000-7f76c075c000 r--p 00000000 fc:02 600                        /usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2
7f76c075c000-7f76c0786000 r-xp 00002000 fc:02 600                        /usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2
7f76c0786000-7f76c0791000 r--p 0002c000 fc:02 600                        /usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2
7f76c0792000-7f76c0794000 r--p 00037000 fc:02 600                        /usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2
7f76c0794000-7f76c0796000 rw-p 00039000 fc:02 600                        /usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2
7fffd6eb6000-7fffd6ed8000 rw-p 00000000 00:00 0                          [stack]
7fffd6f00000-7fffd6f04000 r--p 00000000 00:00 0                          [vvar]
7fffd6f04000-7fffd6f06000 r-xp 00000000 00:00 0                          [vdso]
ffffffffff600000-ffffffffff601000 --xp 00000000 00:00 0                  [vsyscall]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;/proc/self&lt;/code&gt;是一个符号链接，指向&quot;正在读它的那个进程&quot;的PID目录。&lt;/p&gt;
&lt;p&gt;所以上面的命令返回的是&lt;code&gt;cat&lt;/code&gt;这个进程的地址空间映射信息。&lt;/p&gt;
&lt;h2&gt;实验&lt;/h2&gt;
&lt;p&gt;简单的模拟，热身实验。&lt;/p&gt;
</content:encoded></item><item><title>日拱两卒（五）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%BA%94/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%BA%94/</guid><description>51 - 60</description><pubDate>Thu, 23 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;60&lt;/h2&gt;
&lt;p&gt;单词搜索。&lt;/p&gt;
&lt;p&gt;在一个二维矩阵里找单词，简单搜索。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def exist(self, board: List[List[str]], word: str) -&amp;gt; bool:
        n = len(board)
        m = len(board[0])
        st = [[False for _ in range (m)] for _ in range (n)]
        dx = [-1, 1, 0, 0]
        dy = [0, 0, -1, 1]
        def dfs(x, y, u) -&amp;gt; bool:
            if word[u] != board[x][y]:
                return False
            if u == len(word) - 1:
                return True
            st[x][y] = True
            ok = False
            for i in range(4):
                tx = x + dx[i]
                ty = y + dy[i]
                if tx &amp;lt; 0 or tx &amp;gt;= n or ty &amp;lt; 0 or ty &amp;gt;= m:
                    continue
                if st[tx][ty]:
                    continue
                ok |= dfs(tx, ty, u + 1)
            st[x][y] = False
            return ok
            
            
        for i in range(n):
            for j in range(m):
                if dfs(i, j, 0):
                    return True
        return False
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;59&lt;/h2&gt;
&lt;p&gt;括号生成。&lt;/p&gt;
&lt;p&gt;生成合法括号序列，简单搜索。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def generateParenthesis(self, n: int) -&amp;gt; List[str]:
        ans = []
        s = [&apos;.&apos; for _ in range (2 * n)]
        def dfs(u, left, right):
            if u == 2 * n:
                ans.append(&apos;&apos;.join(s))
                return

            if left &amp;lt; n:
                s[u] = &apos;(&apos;
                dfs(u + 1, left + 1, right)
                s[u] = &apos;.&apos;

            if right &amp;lt; left:
                s[u] = &apos;)&apos;
                dfs(u + 1, left, right + 1)
                s[u] = &apos;.&apos;
            
        dfs(0, 0, 0)
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;58&lt;/h2&gt;
&lt;p&gt;组合总和。&lt;/p&gt;
&lt;p&gt;找出列表里所有能组成目标数字的组合。每个数字能用无数次。&lt;/p&gt;
&lt;p&gt;简单搜索。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def combinationSum(self, candidates: List[int], target: int) -&amp;gt; List[List[int]]:
        ans = []
        tmp = []

        def dfs(u, cur):
            if cur &amp;gt; target:
                return
            if cur == target:
                ans.append(tmp[:])
                return
            for i in range(u, len(candidates)):
                tmp.append(candidates[i])
                dfs(i, cur + candidates[i])
                tmp.pop()

        dfs(0, 0)
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;57&lt;/h2&gt;
&lt;p&gt;电话号码的字母组合。&lt;/p&gt;
&lt;p&gt;简单搜索。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def letterCombinations(self, digits: str) -&amp;gt; List[str]:
        mapping = {2: &apos;abc&apos;, 3: &apos;def&apos;, 4: &apos;ghi&apos;, 5: &apos;jkl&apos;, 6: &apos;mno&apos;, 7: &apos;pqrs&apos;, 8: &apos;tuv&apos;, 9: &apos;wxyz&apos;}
        ans = []
        n = len(digits)
        tmp = []

        def dfs(u):
            if u &amp;gt;= n:
                ans.append(&apos;&apos;.join(tmp[:]))
                return
            letters = mapping[int(digits[u])]
            for letter in letters:
                tmp.append(letter)
                dfs(u + 1)
                tmp.pop()

        dfs(0)
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;56&lt;/h2&gt;
&lt;p&gt;子集。&lt;/p&gt;
&lt;p&gt;返回所有可能的子集。简单搜索，每个元素选或不选。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def subsets(self, nums: List[int]) -&amp;gt; List[List[int]]:
        ans = []

        n = len(nums)
        tmp = []
        
        def dfs(u):
            if u &amp;gt;= n:
                ans.append(tmp[:])
                return
            
            dfs(u + 1)

            tmp.append(nums[u])
            dfs(u + 1)
            tmp.pop()

        dfs(0)
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;55&lt;/h2&gt;
&lt;p&gt;全排列。&lt;/p&gt;
&lt;p&gt;简单搜索。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def permute(self, nums: List[int]) -&amp;gt; List[List[int]]:
        ans = []
        n = len(nums)

        tmp = []
        st = [False for _ in range(n)]
        def dfs(u):
            if u &amp;gt;= n:
                ans.append(tmp[:])
                return
            for i in range(n):
                if st[i]:
                    continue

                st[i] = True
                tmp.append(nums[i])
                
                dfs(u + 1)

                tmp.pop()
                st[i] = False

        dfs(0)
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;54&lt;/h2&gt;
&lt;p&gt;实现Trie。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Node:
    __slots__ = &apos;son&apos;, &apos;end&apos;

    def __init__(self):
        self.son = [None] * 26
        self.end = False

class Trie:

    def __init__(self):
        self.root = Node()

    def insert(self, word: str) -&amp;gt; None:
        cur = self.root
        for c in word:
            c = ord(c) - ord(&apos;a&apos;)
            if cur.son[c] is None:
                cur.son[c] = Node()
            cur = cur.son[c]
        cur.end = True

    def search(self, word: str) -&amp;gt; bool:
        cur = self.root
        for c in word:
            c = ord(c) - ord(&apos;a&apos;)
            if cur.son[c] is None:
                return False
            cur = cur.son[c]
        return cur.end

    def startsWith(self, prefix: str) -&amp;gt; bool:
        cur = self.root
        for c in prefix:
            c = ord(c) - ord(&apos;a&apos;)
            if cur.son[c] is None:
                return False
            cur = cur.son[c]
        return True
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;53&lt;/h2&gt;
&lt;p&gt;课程表。&lt;/p&gt;
&lt;p&gt;课程之间有依赖关系，拓扑排序判有没有可能全部选上。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def canFinish(self, numCourses: int, prerequisites: List[List[int]]) -&amp;gt; bool:
        e = [[] for _ in range(numCourses)]
        in_degree = [0 for _ in range(numCourses)]
        
        for edge in prerequisites:
            e[edge[1]].append(edge[0])
            in_degree[edge[0]] += 1
        
        q = deque([i for i in range(numCourses) if in_degree[i] == 0])
        visited = 0

        while q:
            u = q.popleft()
            visited += 1
            for v in e[u]:
                in_degree[v] -= 1
                if in_degree[v] == 0:
                    q.append(v)
        
        return visited == numCourses
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;52&lt;/h2&gt;
&lt;p&gt;腐烂的橘子。&lt;/p&gt;
&lt;p&gt;多源的往外腐蚀，问最快的腐蚀所有橘子的时间。每个源都BFS一遍。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def orangesRotting(self, grid: List[List[int]]) -&amp;gt; int:
        n, m = len(grid), len(grid[0])
        
        for i in range(n):
            for j in range(m):
                if grid[i][j] == 0:
                    grid[i][j] = -1
                elif grid[i][j] == 1:
                    grid[i][j] = float(&quot;inf&quot;)
                else:
                    grid[i][j] = 0
        
        dx = [-1, 1, 0, 0]
        dy = [0, 0, -1, 1]
        q = deque()

        for i in range(n):
            for j in range(m):
                if grid[i][j] == 0:
                    q.append((i, j))

        while q:
            x, y = q.popleft()
            for i in range(4):
                tx = x + dx[i]
                ty = y + dy[i]
                if tx &amp;lt; 0 or tx &amp;gt;= n or ty &amp;lt; 0 or ty &amp;gt;= m:
                    continue
                if grid[tx][ty] &amp;lt; grid[x][y] + 1:
                    continue
                grid[tx][ty] = grid[x][y] + 1
                q.append((tx, ty))

        ans = 0
        for i in range(n):
            for j in range(m):
                if grid[i][j] == float(&quot;inf&quot;):
                    return -1
                if grid[i][j] != -1:
                    ans = max(ans, grid[i][j])
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;51&lt;/h2&gt;
&lt;p&gt;岛屿数量。&lt;/p&gt;
&lt;p&gt;经典搜索。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def numIslands(self, grid: List[List[str]]) -&amp;gt; int:
        n, m = len(grid), len(grid[0])
        
        dx = [-1, 1, 0, 0]
        dy = [0, 0, -1, 1]
        def dfs(x, y):
            grid[x][y] = &apos;0&apos;
            for i in range(4):
                tx = x + dx[i]
                ty = y + dy[i]
                if tx &amp;lt; 0 or tx &amp;gt;= n or ty &amp;lt; 0 or ty &amp;gt;= m:
                    continue
                if grid[tx][ty] == &apos;0&apos;:
                    continue
                dfs(tx, ty)

        ans = 0
        for i in range(n):
            for j in range(m):
                if grid[i][j] == &apos;1&apos;:
                    ans += 1
                    dfs(i, j)
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>日拱两卒（四）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E5%9B%9B/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E5%9B%9B/</guid><description>61 - 70</description><pubDate>Sat, 18 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;70&lt;/h2&gt;
&lt;p&gt;最小栈。让栈能额外支持一个操作，返回栈内最小值。&lt;/p&gt;
&lt;p&gt;栈内多维护一个“前缀最小值”即可。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class MinStack:

    def __init__(self):
        self.stack = [(0, float(&quot;inf&quot;))]

    def push(self, value: int) -&amp;gt; None:
        self.stack.append((value, min(self.stack[-1][1], value)))

    def pop(self) -&amp;gt; None:
        self.stack.pop()

    def top(self) -&amp;gt; int:
        return self.stack[-1][0]

    def getMin(self) -&amp;gt; int:
        return self.stack[-1][1]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;69&lt;/h2&gt;
&lt;p&gt;有效的括号。&lt;/p&gt;
&lt;p&gt;给一个含有三种括号的序列，判断是否合法。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def isValid(self, s: str) -&amp;gt; bool:
        stack = []
        need = {
            &quot;}&quot; : &quot;{&quot;,
            &quot;]&quot; : &quot;[&quot;,
            &quot;)&quot; : &quot;(&quot;
        }
        for c in s:
            if c == &apos;(&apos; or c == &apos;[&apos; or c == &apos;{&apos;:
                stack.append(c)
            else:
                if not stack:
                    return False
                if stack[-1] != need[c]:
                    return False
                stack.pop()
        return not stack
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;68&lt;/h2&gt;
&lt;p&gt;寻找两个正序数组的中位数。&lt;/p&gt;
&lt;p&gt;类似快速选择每次丢一半的思想。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def findMedianSortedArrays(self, nums1: List[int], nums2: List[int]) -&amp;gt; float:
        def findkth(k):
            start1 = 0
            start2 = 0
            while True:
                if start1 == len(nums1):
                    return nums2[start2 + k - 1]
                if start2 == len(nums2):
                    return nums1[start1 + k - 1]
                if k == 1:
                    return min(nums1[start1], nums2[start2])
                half = k // 2
                idx1 = min(start1 + half, len(nums1)) - 1
                idx2 = min(start2 + half, len(nums2)) - 1
                if nums1[idx1] &amp;lt; nums2[idx2]:
                    k -= idx1 - start1 + 1
                    start1 = idx1 + 1
                else:
                    k -= idx2 - start2 + 1
                    start2 = idx2 + 1

        
        total = len(nums1) + len(nums2)
        if total &amp;amp; 1:
            return findkth((1 + total) &amp;gt;&amp;gt; 1)
        else:
            left = findkth(total &amp;gt;&amp;gt; 1)
            right = findkth((total &amp;gt;&amp;gt; 1) + 1)
            return (left + right) / 2.0
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;67&lt;/h2&gt;
&lt;p&gt;寻找旋转排序数组中的最小值。&lt;/p&gt;
&lt;p&gt;这里旋转一次的定义是把一个末尾元素放到头部。&lt;/p&gt;
&lt;p&gt;问题是一开始有一个排序数组，现在被旋转了未知的次数，要找到最小值。&lt;/p&gt;
&lt;p&gt;被旋转后的数组的“形状”大概是先升再掉到最低点，再升。&lt;/p&gt;
&lt;p&gt;所以可以每次二分的时候和最后一个元素比较。&lt;/p&gt;
&lt;p&gt;如果当前位置更大，说明最低点在右边。&lt;/p&gt;
&lt;p&gt;如果当前位置更小，说明最低点在左边。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def findMin(self, nums: List[int]) -&amp;gt; int:
        l = 0
        r = len(nums)

        while l &amp;lt; r:
            mid = (l + r) &amp;gt;&amp;gt; 1
            if nums[mid] &amp;lt;= nums[-1]:
                r = mid
            else:
                l = mid + 1
        
        return nums[l]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;66&lt;/h2&gt;
&lt;p&gt;搜索旋转排序数组。&lt;/p&gt;
&lt;p&gt;数组和上一题一样，但这次不是找最小值而是找目标值。&lt;/p&gt;
&lt;p&gt;思路是先找出最小值，分两段做简单二分。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def findMin(self, nums: List[int]) -&amp;gt; int:
        l = 0
        r = len(nums)

        while l &amp;lt; r:
            mid = (l + r) &amp;gt;&amp;gt; 1
            if nums[mid] &amp;lt;= nums[-1]:
                r = mid
            else:
                l = mid + 1
        
        return l

    def search(self, nums: List[int], target: int) -&amp;gt; int:
        min_idx = self.findMin(nums)
        min_v = nums[min_idx]
        if target &amp;lt;= nums[-1]:
            l = min_idx
            r = len(nums)
            while l &amp;lt; r:
                mid = (l + r) &amp;gt;&amp;gt; 1
                if nums[mid] &amp;gt;= target:
                    r = mid
                else:
                    l = mid + 1
            if l &amp;lt; len(nums) and nums[l] == target:
                return l
        else:
            l = 0
            r = min_idx - 1
            while l &amp;lt; r:
                mid = (l + r) &amp;gt;&amp;gt; 1
                if nums[mid] &amp;gt;= target:
                    r = mid
                else:
                    l = mid + 1
            if l &amp;lt; len(nums) and nums[l] == target:
                return l

        return -1
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;65&lt;/h2&gt;
&lt;p&gt;在排序数组中查找元素的第一个和最后一个位置。&lt;/p&gt;
&lt;p&gt;简单二分。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def searchRange(self, nums: List[int], target: int) -&amp;gt; List[int]:
        l, r = 0, len(nums)
        while l &amp;lt; r:
            mid = (l + r) &amp;gt;&amp;gt; 1
            if nums[mid] &amp;gt;= target:
                r = mid
            else:
                l = mid + 1
        if l &amp;gt;= len(nums) or nums[l] != target:
            return [-1, -1]
        ans1 = l
        l, r = 0, len(nums)
        while l &amp;lt; r:
            mid = (l + r) &amp;gt;&amp;gt; 1
            if nums[mid] &amp;gt; target:
                r = mid
            else:
                l = mid + 1
        return [ans1, l - 1]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;64&lt;/h2&gt;
&lt;p&gt;搜索二维矩阵。&lt;/p&gt;
&lt;p&gt;有一个排序的二维数组，问有没有目标值。&lt;/p&gt;
&lt;p&gt;每次和右上角比对，排除一行或一列。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -&amp;gt; bool:
        i, j = 0, len(matrix[0]) - 1
        while i &amp;lt; len(matrix) and j &amp;gt;= 0:
            if matrix[i][j] == target:
                return True
            if matrix[i][j] &amp;lt; target:
                i += 1
            elif matrix[i][j] &amp;gt; target:
                j -= 1
        return False
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;63&lt;/h2&gt;
&lt;p&gt;搜索插入位置。&lt;/p&gt;
&lt;p&gt;最最简单的二分。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def searchInsert(self, nums: List[int], target: int) -&amp;gt; int:
        l, r = 0, len(nums)
        while l &amp;lt; r:
            mid = (l + r) &amp;gt;&amp;gt; 1
            if nums[mid] &amp;gt;= target:
                r = mid
            else:
                l = mid + 1
        return l
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;62&lt;/h2&gt;
&lt;p&gt;N皇后。&lt;/p&gt;
&lt;p&gt;经典搜索。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def solveNQueens(self, n: int) -&amp;gt; List[List[str]]:
        board = [[&apos;.&apos; for _ in range(n)] for _ in range(n)]
        ans = []

        col = [False] * n
        dia1 = [False] * (2 * n - 1)
        dia2 = [False] * (2 * n - 1)

        def dfs(u):
            if u == n:
                ans.append([&apos;&apos;.join(row) for row in board])
                return
            for i in range(n):
                if col[i] or dia1[i - u + n - 1] or dia2[i + u]:
                    continue

                board[u][i] = &apos;Q&apos;
                col[i] = True
                dia1[i - u + n - 1] = True
                dia2[i + u] = True
                
                dfs(u + 1)
                
                board[u][i] = &apos;.&apos;
                col[i] = False
                dia1[i - u + n - 1] = False
                dia2[i + u] = False
            
        dfs(0)
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;61&lt;/h2&gt;
&lt;p&gt;分割回文串。&lt;/p&gt;
&lt;p&gt;经典搜索。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def partition(self, s: str) -&amp;gt; List[List[str]]:
        ans = []
        tmp = []
        n = len(s)
        
        def check(s) -&amp;gt; bool:
            for i in range(len(s) // 2):
                if s[i] != s[len(s) - 1 - i]:
                    return False
            return True

        def dfs(u):
            if u &amp;gt;= n:
                ans.append(tmp[:])
            for length in range(1, n - u + 1):
                if check(s[u:u + length]):
                    tmp.append(s[u:u + length])
                    dfs(u + length)
                    tmp.pop()

        dfs(0)
        return ans
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>日拱两卒（三）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%B8%89/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%B8%89/</guid><description>71 - 80</description><pubDate>Mon, 13 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;80&lt;/h2&gt;
&lt;p&gt;划分字母区间。&lt;/p&gt;
&lt;p&gt;把一个字符串分成尽可能多的区间，同一种字母必须出现在同一个区间。&lt;/p&gt;
&lt;p&gt;预处理出来每个字母的最左和最右。&lt;/p&gt;
&lt;p&gt;从前往后扫，维护必须延伸到的最右边。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def partitionLabels(self, s: str) -&amp;gt; List[int]:
        n = len(s)
        seg = [[-1] * 2 for _ in range(26)]

        for i, ch in enumerate(s):
            idx = ord(ch) - ord(&apos;a&apos;)
            if seg[idx][0] == -1:
                seg[idx][0] = i
            seg[idx][1] = i

        ans = []
        l = 0
        r = seg[ord(s[0]) - ord(&apos;a&apos;)][1]
        i = l
        while True:
            if r &amp;gt;= n - 1:
                ans.append(n - 1 - l + 1)
                break
            if i &amp;gt; r:
                ans.append(r - l + 1)
                l = i
                r = seg[ord(s[i]) - ord(&apos;a&apos;)][1]

            r = max(r, seg[ord(s[i]) - ord(&apos;a&apos;)][1])
            i += 1            
            
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;79&lt;/h2&gt;
&lt;p&gt;跳跃游戏II。&lt;/p&gt;
&lt;p&gt;问在&lt;code&gt;nums&lt;/code&gt;上从&lt;code&gt;0&lt;/code&gt;跳到&lt;code&gt;n - 1&lt;/code&gt;的最小次数。每一次可以跳&lt;code&gt;0&lt;/code&gt;到&lt;code&gt;nums[i]&lt;/code&gt;之间任意远。&lt;/p&gt;
&lt;p&gt;记忆化做法在Leetcode上没被卡。&lt;/p&gt;
&lt;p&gt;但本质上和&lt;code&gt;80 划分字母区间&lt;/code&gt;是同一道题。&lt;/p&gt;
&lt;p&gt;维护当前区间的最远延申位置，每当指针越过这个边界，就完成一次切割或者跳跃。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def jump(self, nums: List[int]) -&amp;gt; int:
        n = len(nums)
        if n == 1:
            return 0

        right = [i + nums[i] for i in range(n)]

        ans = 0
        r = right[0]
        max_r = r
        i = 0

        while True:
            if r &amp;gt;= n - 1:
                ans += 1
                break

            if i &amp;gt; r:
                ans += 1
                r = max_r
                continue

            max_r = max(max_r, right[i])
            i += 1

        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;78&lt;/h2&gt;
&lt;p&gt;跳跃游戏。&lt;/p&gt;
&lt;p&gt;上一题的Easy版，只问能否跳到，而不问最小跳跃次数。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def canJump(self, nums: List[int]) -&amp;gt; bool:
        n = len(nums)
        r = 0
        for i in range(n):
            r = max(r, i + nums[i]) 
            if i &amp;gt;= r and r != n - 1:
                return False
        return True
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;77&lt;/h2&gt;
&lt;p&gt;买卖股票的最佳时机。&lt;/p&gt;
&lt;p&gt;知道每一天的股票价格，问最大可能利润。简单贪心。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def maxProfit(self, prices: List[int]) -&amp;gt; int:
        n = len(prices)

        rmax = [0] * (n + 1)
        rmax[n] = 0
        for i in range(n - 1, -1, -1):
            rmax[i] = max(rmax[i + 1], prices[i])
        
        ans = 0
        for i in range(n):
            ans = max(ans, rmax[i] - prices[i])
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;76&lt;/h2&gt;
&lt;p&gt;数据流的中位数。&lt;/p&gt;
&lt;p&gt;往列表里不断添加元素，每添加一个元素就打印当前列表的中位数。&lt;/p&gt;
&lt;p&gt;维护对顶堆，一个最大堆存前一半元素，一个最小堆存后一半元素，中位数只会与两个堆顶相关。&lt;/p&gt;
&lt;p&gt;Python里有内置的堆相关内置函数，&lt;code&gt;heappush&lt;/code&gt;是按堆的方式插元素，&lt;code&gt;heappushpop&lt;/code&gt;是插完一个后再弹出堆顶。&lt;/p&gt;
&lt;p&gt;默认是最小堆，想用最大堆就存相反数。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class MedianFinder:

    def __init__(self):
        self.left = []
        self.right = []

    def addNum(self, num: int) -&amp;gt; None:
        if len(self.left) == len(self.right):
            add_to_left = heappushpop(self.right, num)
            heappush(self.left, -add_to_left)
        else:
            add_to_right = heappushpop(self.left, -num)
            heappush(self.right, -add_to_right)

    def findMedian(self) -&amp;gt; float:
        if len(self.left) &amp;gt; len(self.right):
            return -self.left[0]
        return (-self.left[0] + self.right[0]) / 2
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;75&lt;/h2&gt;
&lt;p&gt;前 K 个高频元素。&lt;/p&gt;
&lt;p&gt;哈希加桶排序。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def topKFrequent(self, nums: List[int], k: int) -&amp;gt; List[int]:
        cnt = {}
        for num in nums:
            cnt[num] = cnt.get(num, 0) + 1
        max_cnt = max(cnt.values())
        
        buckets = [[] for _ in range(max_cnt + 1)]
        for x, c in cnt.items():
            buckets[c].append(x)
        
        ans = []
        for bucket in reversed(buckets):
            ans += bucket
            if len(ans) == k:
                return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;74&lt;/h2&gt;
&lt;p&gt;数组中的第K大元素。&lt;/p&gt;
&lt;p&gt;快速排序的思想+每次丢掉一半。&lt;/p&gt;
&lt;p&gt;Python里写成这种三路归并的方法应该最好理解。&lt;/p&gt;
&lt;p&gt;处理完有&lt;code&gt;[l, lt - 1] &amp;lt; pivot, [lt, gt] = pivot, [gt + 1, r] &amp;gt; pivot&lt;/code&gt;。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def quick_select(self, q, l, r, k) -&amp;gt; int:
        if l &amp;gt;= r:
            return q[l]

        pivot = q[random.randint(l, r)]
        
        lt, i, gt = l, l, r
        while i &amp;lt;= gt:
            if q[i] &amp;lt; pivot:
                q[i], q[lt] = q[lt], q[i]
                lt += 1
                i += 1
            elif q[i] &amp;gt; pivot:
                q[i], q[gt] = q[gt], q[i]
                gt -= 1
            else:
                i += 1

        if k &amp;lt; lt - l:
            return self.quick_select(q, l, lt - 1, k)
        if k &amp;lt;= gt - l:
            return pivot
        return self.quick_select(q, gt + 1, r, k - (gt - l + 1))
        

    def findKthLargest(self, nums: List[int], k: int) -&amp;gt; int:
        n = len(nums)
        return self.quick_select(nums, 0, n - 1, n - k)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;73&lt;/h2&gt;
&lt;p&gt;柱状图重最大的矩形。&lt;/p&gt;
&lt;p&gt;枚举高，单调栈算左右最长能延申到哪。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def largestRectangleArea(self, h: List[int]) -&amp;gt; int:
        ans = 0
        n = len(h)

        stack = []
        l = [-1] * n
        for i in range(n):
            while(stack and h[stack[-1]] &amp;gt;= h[i]):
                stack.pop()
            if stack:
                l[i] = stack[-1]
            stack.append(i)
        
        stack = []
        r = [n] * n
        for i in range(n - 1, -1, -1):
            while(stack and h[stack[-1]] &amp;gt;= h[i]):
                stack.pop()
            if stack:
                r[i] = stack[-1]
            stack.append(i)


        for i in range(n):
            ans = max(ans, h[i] * ((r[i] - 1) - (l[i] + 1) + 1))

        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;72&lt;/h2&gt;
&lt;p&gt;每日温度。&lt;/p&gt;
&lt;p&gt;给定每一天温度，问每一天的下一个更高温度出现在几天后。&lt;/p&gt;
&lt;p&gt;找右边第一个更大的数，单调栈经典应用。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def dailyTemperatures(self, temperatures: List[int]) -&amp;gt; List[int]:
        n = len(temperatures)
        ans = [0] * n
        stack = []

        for i in range(n - 1, -1, -1):
            while stack and temperatures[stack[-1]] &amp;lt;= temperatures[i]:
                stack.pop()
            if stack:
                ans[i] = stack[-1] - i
            stack.append(i)

        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;71&lt;/h2&gt;
&lt;p&gt;字符串解码。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;3[a]2[2[bc]]&lt;/code&gt;解析成&lt;code&gt;aaabcbcbcbc&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;简单递归模拟。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def decodeString(self, s: str) -&amp;gt; str:
        if not s:
            return s

        if s[0].isalpha():
            return s[0] + self.decodeString(s[1:])
        
        i = s.find(&apos;[&apos;)
        n = len(s)
        left = 1
        for j in range(i + 1, n):
            if s[j] == &apos;[&apos;:
                left += 1
            elif s[j] == &apos;]&apos;:
                left -= 1
                if left == 0:
                    return self.decodeString(s[i+1:j] * int(s[:i])) + self.decodeString(s[j+1:])
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>日拱两卒（二）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%BA%8C/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%BA%8C/</guid><description>81 - 90</description><pubDate>Thu, 09 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;90&lt;/h2&gt;
&lt;p&gt;给一个括号序列，问最长的一串合法括号。&lt;/p&gt;
&lt;p&gt;合法的一串括号一定是连续的，而且每个右括号一定是和最近的左括号配对。&lt;/p&gt;
&lt;p&gt;拿栈存左括号，先扫一遍把所有配对的左右括号标记，再扫一遍找最长的一串标记。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def longestValidParentheses(self, s: str) -&amp;gt; int:
        stack = []
        n = len(s)
        v = [False] * n

        for i in range(n):
            if s[i] == &apos;(&apos;:
                stack.append(i)
            elif s[i] == &apos;)&apos; and len(stack):
                left = stack.pop()
                v[left] = v[i] = True

        ans, cnt, i = 0, 0, 0
        while i &amp;lt; n:
            if v[i]:
                cnt += 1
                ans = max(ans, cnt)
            else:
                cnt = 0
            i += 1
        
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;89&lt;/h2&gt;
&lt;p&gt;能否把一个数组里的数字分成两个集合，每个集合的和相等。&lt;/p&gt;
&lt;p&gt;先判奇偶。&lt;/p&gt;
&lt;p&gt;问题变成能否从&lt;code&gt;n&lt;/code&gt;个数里选某些数使得和为&lt;code&gt;m&lt;/code&gt;，是背包问题。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def canPartition(self, nums: List[int]) -&amp;gt; bool:
        s = sum(nums)
        if s &amp;amp; 1:
            return False

        n = len(nums)

        @cache
        def dfs(i, j) -&amp;gt; bool:
            if i == 0:
                return j == 0 or j == nums[0]
            if j &amp;lt; 0:
                return False
            return dfs(i - 1, j) or dfs(i - 1, j - nums[i])

        return dfs(n - 1, s / 2)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;88&lt;/h2&gt;
&lt;p&gt;乘积最大子数组。&lt;/p&gt;
&lt;p&gt;因为这个数组里有负数，所以要同时维护只考虑前&lt;code&gt;i&lt;/code&gt;个数且取第&lt;code&gt;i&lt;/code&gt;个数的最大值和最小值，可能负负得正。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def maxProduct(self, nums: List[int]) -&amp;gt; int:
        n = len(nums)
        f = [float(&apos;-inf&apos;)] * n
        g = [float(&apos;inf&apos;)] * n

        f[0] = nums[0]
        g[0] = nums[0]
        for i in range(1, n):
            f[i] = max(f[i - 1] * nums[i], g[i - 1] * nums[i], nums[i], f[i])
            g[i] = min(g[i - 1] * nums[i], f[i - 1] * nums[i], nums[i], g[i])

        return max(f)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;87&lt;/h2&gt;
&lt;p&gt;最长上升子序列。&lt;/p&gt;
&lt;p&gt;最重要的是理解&lt;code&gt;O(nlogn)&lt;/code&gt;做法的思想。&lt;/p&gt;
&lt;p&gt;先看代码实际意味着做的事情。&lt;/p&gt;
&lt;p&gt;想象数组是一副扑克牌，现在按以下规则分成若干堆牌堆：&lt;/p&gt;
&lt;p&gt;为当前牌找到第一个牌堆顶部牌大于等于它的堆，放上去。&lt;/p&gt;
&lt;p&gt;如果没找到，就以这个牌新开一个堆。&lt;/p&gt;
&lt;p&gt;最后牌堆的数量就是LIS长度。&lt;/p&gt;
&lt;p&gt;为什么两者等价呢？&lt;/p&gt;
&lt;p&gt;每个牌堆，都精确对应了一个“某个长度的上升子序列”的最好结尾。&lt;/p&gt;
&lt;p&gt;第&lt;code&gt;k&lt;/code&gt;个牌堆顶部的牌，是目前为止，能构成&quot;长度为&lt;code&gt;k&lt;/code&gt;的LIS&quot;的最小可能结尾数字。&lt;/p&gt;
&lt;p&gt;具体例子，考虑&lt;code&gt;[3, 1, 5]&lt;/code&gt;。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;新建第一个堆，堆顶是&lt;code&gt;3&lt;/code&gt;，意味着想构成长度为&lt;code&gt;1&lt;/code&gt;的子序列，末尾最小可以是&lt;code&gt;3&lt;/code&gt;。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;1 &amp;lt; 3&lt;/code&gt;，想构成长度为&lt;code&gt;1&lt;/code&gt;的子序列，末尾最小可以是&lt;code&gt;1&lt;/code&gt;。&lt;code&gt;1&lt;/code&gt;比&lt;code&gt;3&lt;/code&gt;更有潜力。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;5&lt;/code&gt;比前两个数都大，无法去优化长度为&lt;code&gt;1&lt;/code&gt;的子序列的末尾，但它带来了长度为&lt;code&gt;2&lt;/code&gt;的子序列的想象空间，新开一个堆。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;核心比喻抗遗忘：转化分牌堆，维护一个有“潜力”的结构。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def lengthOfLIS(self, nums: List[int]) -&amp;gt; int:
        d = []
        n = len(nums)

        for num in nums:
            l, r = 0, len(d)
            while l &amp;lt; r:
                mid = (l + r) &amp;gt;&amp;gt; 1;
                if num &amp;lt;= d[mid]:
                    r = mid
                else:
                    l = mid + 1

            if l &amp;lt; len(d):
                d[l] = num
            else:
                d.append(num)
                    
        return len(d)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;86&lt;/h2&gt;
&lt;p&gt;单词拆分。给定字符串和单词列表，问字符串能不能由列表内若干单词组成。&lt;/p&gt;
&lt;p&gt;如果这里没用&lt;code&gt;set&lt;/code&gt;，用了&lt;code&gt;dict&lt;/code&gt;的话，不要写&lt;code&gt;mp[s[j:i]]&lt;/code&gt;，要写&lt;code&gt;s[j:i] in mp&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;Python对于没有的&lt;code&gt;Key&lt;/code&gt;会直接抛一个异常。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -&amp;gt; bool:
        n = len(s)
        dp = [False] * (n + 1)
        word_set = set(wordDict)
        
        dp[0] = True
        for i in range(1, n + 1):
            for j in range(0, i):
                dp[i] = dp[i] or (dp[j] and s[j:i] in word_set)

        return dp[n]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;85&lt;/h2&gt;
&lt;p&gt;零钱兑换。问某一个总额能不能由若干固定面额的零钱凑出来，每种零钱有无数个。&lt;/p&gt;
&lt;p&gt;记忆化搜索就完了。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def coinChange(self, coins: List[int], amount: int) -&amp;gt; int:
        
        @cache
        def dfs(remains) -&amp;gt; int:
            if remains &amp;lt; 0:
                return float(&quot;inf&quot;)
            if remains == 0:
                return 0
            
            res =  float(&quot;inf&quot;)
            for coin in coins:
                res = min(res, dfs(remains - coin) + 1)
            return res

        res = dfs(amount)
        return res if res != float(&quot;inf&quot;) else -1
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;84&lt;/h2&gt;
&lt;p&gt;一个数最少能由几个完全平方数相加得到。&lt;/p&gt;
&lt;p&gt;简单DP。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def numSquares(self, n: int) -&amp;gt; int:
        squares = set()
        
        i = 1
        while i * i &amp;lt;= n:
            squares.add(i * i)
            i += 1

        @cache
        def dfs(x) -&amp;gt; int:
            if x &amp;lt;= 0:
                return float(&quot;inf&quot;)
            if x in squares:
                return 1
            
            res = float(&quot;inf&quot;)
            for square in squares:
                res = min(res, dfs(x - square) + 1)
            return res


        return dfs(n)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;83&lt;/h2&gt;
&lt;p&gt;打家劫舍。&lt;/p&gt;
&lt;p&gt;不能连着抢两家相邻的。简单DP。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def rob(self, nums: List[int]) -&amp;gt; int:
        n = len(nums)
        dp = [[0] * 2 for _ in range(n)]
        
        dp[0][0] = 0
        dp[0][1] = nums[0]

        for i in range(1, n):
            dp[i][0] = max(dp[i - 1][0], dp[i - 1][1])
            dp[i][1] = dp[i - 1][0] + nums[i]
        
        return max(dp[n - 1][0], dp[n - 1][1])
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;82&lt;/h2&gt;
&lt;p&gt;打印杨辉三角形。&lt;/p&gt;
&lt;p&gt;简单DP。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def generate(self, numRows: int) -&amp;gt; List[List[int]]:
        f = [[1] for _ in range(numRows)]

        for i in range(1, numRows):
            for j in range(1, i + 1):
                f[i].append(f[i - 1][j - 1]) 
                if j &amp;lt; i:
                    f[i][j] += f[i - 1][j]
        
        return f
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;81&lt;/h2&gt;
&lt;p&gt;爬楼梯。&lt;/p&gt;
&lt;p&gt;简单DP。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def climbStairs(self, n: int) -&amp;gt; int:
        dp = [0] * (n + 1)
        dp[0] = dp[1] = 1

        for i in range(2, n + 1):
            dp[i] = dp[i - 1] + dp[i - 2]
        
        return dp[n]
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>2026.07.04</title><link>https://cabbage74.github.io/posts/20260704/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/20260704/</guid><description>23岁生日</description><pubDate>Sat, 04 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;去年22岁感觉自己还很小，但是今年感觉就不一样了。&lt;/p&gt;
&lt;p&gt;因为四舍五入就是25岁，再四舍五入就是30岁...&lt;/p&gt;
&lt;p&gt;也可能是因为身份变了，感觉研究生和本科生的一个区别就是大家都更“独狼”了。&lt;/p&gt;
&lt;p&gt;...&lt;/p&gt;
&lt;p&gt;不过听到本科毕业就工作的同龄人最近结婚、听到印象里还是小学生的一个弟弟今年高考时，我还是会下意识惊讶一下。&lt;/p&gt;
&lt;p&gt;...&lt;/p&gt;
&lt;p&gt;每年的元旦、春节、生日都会让我感到焦虑，因为这样的时间节点总会让我问自己：“相比上一个这样的时间节点，你有什么长进？”&lt;/p&gt;
&lt;p&gt;每次我都给不了自己一个满意的回答。&lt;/p&gt;
&lt;p&gt;在新环境实习也一个月了，目前的工作可以总结为数据业务的后端开发。说实话，能学到东西，但挺无聊。而且有些业务上理解不了的地方同事也理解不了。&lt;/p&gt;
&lt;p&gt;好在最近自身的学习热情还不错吧。&lt;/p&gt;
</content:encoded></item><item><title>日拱两卒（一）</title><link>https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%B8%80/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E6%97%A5%E6%8B%B1%E4%B8%A4%E5%8D%92%E4%B8%80/</guid><description>91 - 100</description><pubDate>Sat, 04 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;Leetcode的考察点和ACM不太一样，之前面试也有几次尴尬写不出的经历。&lt;/p&gt;
&lt;p&gt;从2026.07.04开始把Hot100过一遍，因为前面几题其实很久以前提交过，所以这次倒着刷。&lt;/p&gt;
&lt;p&gt;这轮试着用Python而不是C++！&lt;/p&gt;
&lt;p&gt;每天两题写完差不多正好是9月日常实习。某一天的题太简单的话就多做几道！&lt;/p&gt;
&lt;h2&gt;100&lt;/h2&gt;
&lt;p&gt;有&lt;code&gt;n + 1&lt;/code&gt;个整数，值域是&lt;code&gt;[1, n]&lt;/code&gt;，找出唯一的重复的数。&lt;/p&gt;
&lt;p&gt;假设这个重复的数是&lt;code&gt;x&lt;/code&gt;。定义&lt;code&gt;cnt[i]&lt;/code&gt;为小于等于&lt;code&gt;i&lt;/code&gt;的数的个数。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;cnt[i] &amp;lt;= i&lt;/code&gt; 当 &lt;code&gt;i &amp;lt;= x - 1&lt;/code&gt;时。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;cnt[i] &amp;gt; i&lt;/code&gt; 当 &lt;code&gt;i &amp;gt;= x&lt;/code&gt; 时。&lt;/p&gt;
&lt;p&gt;所以二分出最小的满足&lt;code&gt;cnt[i] &amp;gt; i&lt;/code&gt;的&lt;code&gt;i&lt;/code&gt;就可以了。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def findDuplicate(self, nums: List[int]) -&amp;gt; int:
        l = 1
        r = len(nums) - 1
        while l &amp;lt; r:
            mid = (l + r) &amp;gt;&amp;gt; 1;
            
            cnt = 0
            for num in nums:
                cnt += num &amp;lt;= mid

            if cnt &amp;gt; mid:
                r = mid
            else:
                l = mid + 1
            
        return l 
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;99&lt;/h2&gt;
&lt;p&gt;按字典序找出下一个排列。&lt;/p&gt;
&lt;p&gt;比如&lt;code&gt;[1, 2, 3]&lt;/code&gt;的下一个是&lt;code&gt;[1, 3, 2]&lt;/code&gt;，再下一个是&lt;code&gt;[2, 1, 3]&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;[1, 3, 5, 4, 2]&lt;/code&gt; -&amp;gt; &lt;code&gt;[1, 4, 5, 3, 2]&lt;/code&gt; -&amp;gt; &lt;code&gt;[1, 4, 2, 3, 5]&lt;/code&gt;。这是一个一般化过程。&lt;/p&gt;
&lt;p&gt;从后往前看，如果有一个数破坏了递增，说明这个数移到后面去是可以提升字典序的（因为后面有比它大的数）。&lt;/p&gt;
&lt;p&gt;那么把哪个数和它调换？因为要求下一个排列，肯定是比它大的最小的数。&lt;/p&gt;
&lt;p&gt;换完以后容易发现现在递减的部分可以倒过来，进一步缩小字典序。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def nextPermutation(self, nums: List[int]) -&amp;gt; None:
        n = len(nums)

        i = n - 2
        while i &amp;gt;= 0 and nums[i] &amp;gt;= nums[i + 1]:
            i -= 1

        if i &amp;gt;= 0:
            j = n - 1
            while nums[j] &amp;lt;= nums[i]:
                j -= 1
            nums[i], nums[j] = nums[j], nums[i]

        nums[i+1:] = nums[i+1:][::-1]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;98&lt;/h2&gt;
&lt;p&gt;不用&lt;code&gt;sort&lt;/code&gt;排序一个只含有&lt;code&gt;0, 1, 2&lt;/code&gt;的数组。&lt;/p&gt;
&lt;p&gt;这个问题叫荷兰国旗问题（名字加深印象）！&lt;/p&gt;
&lt;p&gt;&lt;code&gt;p0&lt;/code&gt;维护下一个&lt;code&gt;0&lt;/code&gt;的位置，它的左边都是&lt;code&gt;0&lt;/code&gt;。&lt;code&gt;p2&lt;/code&gt;维护下一个&lt;code&gt;2&lt;/code&gt;的位置，它的右边都是&lt;code&gt;2&lt;/code&gt;。&lt;code&gt;i&lt;/code&gt;是当前检查的位置。&lt;/p&gt;
&lt;p&gt;循环的不变量：&lt;code&gt;[0, p0)&lt;/code&gt;都是 &lt;code&gt;0&lt;/code&gt;，&lt;code&gt;(p2, n - 1]&lt;/code&gt;都是 2，中间&lt;code&gt;[p0, i)&lt;/code&gt;都是 1。&lt;/p&gt;
&lt;p&gt;把&lt;code&gt;2&lt;/code&gt;交换到后面去的时候要再检查一次当前位置，因为被换到前面来的数可能是&lt;code&gt;0&lt;/code&gt;或&lt;code&gt;2&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;在这个算法里，一个数最多会被搬两次。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def sortColors(self, nums: List[int]) -&amp;gt; None:
        p0, i, p2 = 0, 0, len(nums) - 1
        while i &amp;lt;= p2:
            if nums[i] == 0:
                nums[i], nums[p0] = nums[p0], nums[i]
                p0 += 1
                i += 1
            elif nums[i] == 2:
                nums[i], nums[p2] = nums[p2], nums[i]
                p2 -= 1
            else:
                i += 1
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;97&lt;/h2&gt;
&lt;p&gt;求一个数组里的绝对众数。&lt;/p&gt;
&lt;p&gt;擂台法。同一个数字想成同一个门派，同门派就加一战斗力，不同门派就减一战斗力。绝对众数的门派肯定能站到最后。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def majorityElement(self, nums: List[int]) -&amp;gt; int:
        ans, cnt = 0, 0
        for num in nums:
            if cnt == 0:
                ans = num
                cnt += 1
            else:
                cnt += 1 if num == ans else -1
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;96&lt;/h2&gt;
&lt;p&gt;找出只出现一次的数字，保证其他数字都是两次。&lt;/p&gt;
&lt;p&gt;老生常谈的异或。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def singleNumber(self, nums: List[int]) -&amp;gt; int:
        ans = 0
        for num in nums:
            ans = ans ^ num
        return ans
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;95&lt;/h2&gt;
&lt;p&gt;编辑距离。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;dp[n][m]&lt;/code&gt;代表只考虑&lt;code&gt;word1&lt;/code&gt;前&lt;code&gt;n&lt;/code&gt;个字母，&lt;code&gt;word2&lt;/code&gt;前&lt;code&gt;m&lt;/code&gt;个字母的答案。&lt;/p&gt;
&lt;p&gt;第&lt;code&gt;n&lt;/code&gt;个和第&lt;code&gt;m&lt;/code&gt;个字母相等时，只需要考虑&lt;code&gt;dp[n - 1][m - 1]&lt;/code&gt;这个子问题。&lt;/p&gt;
&lt;p&gt;不相等时，插入、删除、替换对应三个子问题。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;@cache&lt;/code&gt;起到记忆化搜索的作用，Python把这个功能内置了。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def minDistance(self, word1: str, word2: str) -&amp;gt; int:
        n = len(word1)
        m = len(word2)

        @cache
        def dfs(x, y):
            if x &amp;lt; 0:
                return y + 1
            if y &amp;lt; 0:
                return x + 1
            if word1[x] == word2[y]:
                return dfs(x - 1, y - 1)
            
            return min(dfs(x - 1, y), dfs(x, y - 1), dfs(x - 1, y - 1)) + 1

        return dfs(n - 1, m - 1)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;94&lt;/h2&gt;
&lt;p&gt;最长公共子序列。&lt;/p&gt;
&lt;p&gt;熟悉Python的多维列表初始化和&lt;code&gt;enumerate&lt;/code&gt;。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def longestCommonSubsequence(self, text1: str, text2: str) -&amp;gt; int:
        n, m = len(text1), len(text2)
        dp = [[0] * (m + 1) for _ in range(n + 1)]

        for i, x in enumerate(text1):
            for j, y in enumerate(text2):
                dp[i + 1][j + 1] = dp[i][j] + 1 if x == y else max(dp[i][j + 1], dp[i + 1][j])

        return dp[n][m]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;93&lt;/h2&gt;
&lt;p&gt;最长回文子串。&lt;/p&gt;
&lt;p&gt;经典的区间DP。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def longestPalindrome(self, s: str) -&amp;gt; str:
        n = len(s)
        dp = [[False] * n for _ in range(n)]

        for i in range(n):
            dp[i][i] = True

        for i in range(n - 1):
            dp[i][i + 1] = True if s[i] == s[i + 1] else False

        for length in range(3, n + 1):
            for l in range(n):
                r = l + length - 1
                if r &amp;gt; n - 1:
                    break
                if s[l] == s[r]:
                    dp[l][r] = dp[l + 1][r - 1]

        ans = 0
        st = 0
        for i in range(n):
            for j in range(i, n):
                if dp[i][j] and j - i + 1 &amp;gt; ans:
                    ans = j - i + 1
                    st = i

        return s[st:st + ans]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;92&lt;/h2&gt;
&lt;p&gt;从左上角走到右下角的最小权重和，只能向下和向右。&lt;/p&gt;
&lt;p&gt;经典的简单DP。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def minPathSum(self, grid: List[List[int]]) -&amp;gt; int:
        n, m = len(grid), len(grid[0])
        dp = [[0] * m for _ in range(n)]

        dp[0][0] = grid[0][0]
        for j in range(1, m):
            dp[0][j] = dp[0][j - 1] + grid[0][j]

        for i in range(1, n):
            dp[i][0] = dp[i - 1][0] + grid[i][0]

        for i in range(1, n):
            for j in range(1, m):
                dp[i][j] = min(dp[i - 1][j], dp[i][j - 1]) + grid[i][j]

        return dp[n - 1][m - 1]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;91&lt;/h2&gt;
&lt;p&gt;从左上角走到右下角的不同路径数，只能向下和向右。&lt;/p&gt;
&lt;p&gt;可以是简单DP。&lt;/p&gt;
&lt;p&gt;也可以是组合数学，&lt;code&gt;m&lt;/code&gt;行&lt;code&gt;n&lt;/code&gt;列，因为只能向下和向右，肯定是走&lt;code&gt;m - 1 + n - 1&lt;/code&gt;步，这些步数里有&lt;code&gt;m - 1&lt;/code&gt;步是向下走。&lt;/p&gt;
&lt;p&gt;数据量小的时候Python有现成的&lt;code&gt;comb&lt;/code&gt;函数，数据量大的时候还是得上预处理阶乘和逆元的写法。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Solution:
    def uniquePaths(self, m: int, n: int) -&amp;gt; int:
        return comb(m + n - 2, m - 1)
&lt;/code&gt;&lt;/pre&gt;
</content:encoded></item><item><title>Python入门（四）</title><link>https://cabbage74.github.io/posts/python%E5%85%A5%E9%97%A8%E5%9B%9B/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/python%E5%85%A5%E9%97%A8%E5%9B%9B/</guid><description>Python独特语法</description><pubDate>Wed, 01 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;集合与迭代&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;numbers = [1, 2, 3, 4, 5]
for number in numbers:
    print(number)
else:
    print(&quot;Finished printing all numbers.&quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python里&lt;code&gt;while&lt;/code&gt;和&lt;code&gt;for&lt;/code&gt;是可以接&lt;code&gt;else&lt;/code&gt;的，循环正常结束就会执行，如果循环内部因异常终止的话就不执行。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;letters = [&quot;a&quot;, &quot;b&quot;, &quot;c&quot;]

first_iterator = letters.__iter__()
second_iterator = letters.__iter__()

print(type(first_iterator)) # &amp;lt;class &apos;list_iterator&apos;&amp;gt;

print(first_iterator.__next__()) # a
print(first_iterator.__next__()) # b
print(second_iterator.__next__()) # a
print(first_iterator.__next__()) # c
try:
    print(first_iterator.__next__()) # Raises StopIteration
except StopIteration:
    print(&quot;StopIteration raised for first_iterator&quot;)

iterator = iter(letters)
print(next(iterator)) # a
print(next(iterator)) # b
print(next(iterator)) # c
print(next(iterator, &quot;No more&quot;)) # No more
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;可以直接用&lt;code&gt;__iter__&lt;/code&gt;和&lt;code&gt;__next__&lt;/code&gt;来获取和使用迭代器。&lt;/p&gt;
&lt;p&gt;但总是应该使用Python提供的公共API。功能、类型检查、错误信息都会更强大。比如&lt;code&gt;next()&lt;/code&gt;可以提供第二个参数，在没有下一个元素时不抛异常，而是返回默认值。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class Queue:
    def __init__(self):
        self._queue = []
    
    def enqueue(self, item):
        self._queue.append(item)
    
    def __iter__(self):
        return QueueIterator(self._queue)
    
    def __len__(self):
        return len(self._queue)
    
class QueueIterator:
    def __init__(self, queue):
        self._queue = queue
        self._index = 0
    
    def __iter__(self):
        return self
    
    def __next__(self):
        if self._index &amp;lt; len(self._queue):
            item = self._queue[self._index]
            self._index += 1
            return item
        else:
            raise StopIteration
        

queue = Queue()
queue.enqueue(1)
queue.enqueue(2)
for item in queue:
    print(item)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;需要自定义可迭代类时，一个简单的写法如上。&lt;/p&gt;
&lt;h2&gt;生成器和推导式&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;import time

slow_list = [1, time.sleep(1), 2, time.sleep(1), 3]
# Wait for 2 seconds

print(slow_list) # [1, None, 2, None, 3]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;迭代器的思想本质是惰性求值，当要使用值的时候再进行计算，而不是定义值时就计算。&lt;/p&gt;
&lt;p&gt;上面的例子模拟了一个在定义时进行耗时计算的列表。打印语句需要等待两秒。&lt;/p&gt;
&lt;p&gt;如果这里的计算极端耗资源，可能卡死，可能Out of Memory。在Python里处理这种情况的最好方法就是惰性求值。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from itertools import product

def get_license_plate():
    for letter in product(&apos;ABCDEFGHIJKLMNOPQRSTUVWXYZ&apos;, repeat=3):
        for number in product(&apos;0123456789&apos;, repeat=3):
            yield &apos;&apos;.join(letter) + &apos;&apos;.join(number)

license_plate_generator = get_license_plate()
print(next(license_plate_generator)) # AAA000
print(next(license_plate_generator)) # AAA001
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;迭代器的一个强大替代品是生成器。含有&lt;code&gt;yield&lt;/code&gt;语句的函数就是生成器函数。调用它时，会返回一个生成器对象。&lt;/p&gt;
&lt;p&gt;每次调用&lt;code&gt;next&lt;/code&gt;，就会从上次暂停的地方开始执行，直到又碰到&lt;code&gt;yield&lt;/code&gt;，暂停，返回值。&lt;/p&gt;
&lt;p&gt;当生成器函数终止时，不管是执行到末尾，还是提前&lt;code&gt;return&lt;/code&gt;，都会触发异常&lt;code&gt;StopIteration&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;可以将迭代器看成一种协议，要求对象实现&lt;code&gt;__iter__&lt;/code&gt;和&lt;code&gt;__next__&lt;/code&gt;。而生成器&lt;code&gt;Generator&lt;/code&gt;对象实现了这个协议，是迭代器的子集，对代码编写者来说用起来更方便。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;Generator&lt;/code&gt;对象内部维护了一个&lt;code&gt;frame&lt;/code&gt;，存储了指令指针、局部变量、栈。调用&lt;code&gt;next()&lt;/code&gt;会恢复&lt;code&gt;frame&lt;/code&gt;从上次停下的字节码位置继续执行。&lt;code&gt;yield&lt;/code&gt;完又会冻结&lt;code&gt;frame&lt;/code&gt;。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from random import randint, choice

colors = [&quot;red&quot;, &quot;green&quot;, &quot;blue&quot;, &quot;yellow&quot;, &quot;purple&quot;]
vehicles = [&quot;car&quot;, &quot;bike&quot;, &quot;truck&quot;, &quot;bus&quot;, &quot;scooter&quot;]

# 生成一个有2到10辆随机颜色的摩托车的车队
def biker_gang():
    for _ in range(randint(2, 10)):
        color = choice(colors)
        yield f&quot;{color} motorcycle&quot;

# 百分之二的概率生成摩托车队
def traffic():
    while True:
        if randint(1, 50) == 50:
            yield from biker_gang()
            continue
        vehicle = choice(vehicles)
        color = choice(colors)
        yield f&quot;{color} {vehicle}&quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;yield from&lt;/code&gt;会把执行流转交给另一个生成器。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# def license_plates():
#     for num in range(10):
#         yield f&apos;ABC {num:03}&apos;

# license_plates = license_plates()

license_plates = (
    f&apos;ABC {num:03}&apos; for num in range(10)
)

for plate in license_plates:
    print(plate)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;没被注释掉的版本是生成器表达式写法，是语法糖。写起来像把&lt;code&gt;for&lt;/code&gt;倒过来写。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;g = (x * 2 for x in range(5))

l = [x * 2 for x in range(5)]

s = {x * 2 for x in range(5)}

d = {x: x * 2 for x in range(5)}

print(g)  # &amp;lt;generator object &amp;lt;genexpr&amp;gt; at 0x1002eab50&amp;gt;
print(l)  # [0, 2, 4, 6, 8]
print(s)  # {0, 2, 4, 6, 8}
print(d)  # {0: 0, 1: 2, 2: 4, 3: 6, 4: 8}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;把生成器表达式的括号变一下就变成了推导式。推导式只是语法糖，也没有惰性求值。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def echo():
    while True:
        received = yield
        print(f&quot;收到: {received}&quot;)

gen = echo()
gen.send(None)
gen.send(&quot;hello&quot;)  # 收到: hello
gen.send(&quot;world&quot;)  # 收到: world
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;yield&lt;/code&gt;出现在等式右边时也可以接收值，一开始PC在函数开头，Python规定需要先&lt;code&gt;send(None)&lt;/code&gt;一次把PC移到yield位置。&lt;/p&gt;
&lt;p&gt;后续&lt;code&gt;hello&lt;/code&gt;会作为&lt;code&gt;received&lt;/code&gt;的接受值然后继续运行直到下一个&lt;code&gt;yield&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;当然这是简单协程的写法，现代Python基本没人用了。&lt;/p&gt;
</content:encoded></item><item><title>React数据驱动页面</title><link>https://cabbage74.github.io/posts/react%E6%95%B0%E6%8D%AE%E9%A9%B1%E5%8A%A8%E9%A1%B5%E9%9D%A2/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/react%E6%95%B0%E6%8D%AE%E9%A9%B1%E5%8A%A8%E9%A1%B5%E9%9D%A2/</guid><description>React的数据驱动页面思想</description><pubDate>Wed, 01 Jul 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;上一节“组件根据运行时输入来渲染”已经能让人感觉到数据驱动页面的思想。&lt;/p&gt;
&lt;p&gt;这一节更加深入一下这个概念。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import Nav from &quot;./Nav.jsx&quot;;
import PageHeading from &quot;./PageHeading.jsx&quot;;
import AnimatedCardGrid from &quot;./AnimatedCardGrid.jsx&quot;;

export default function HomePage({ current, onNavigate }) {
  return (
    &amp;lt;AnimatedCardGrid className=&quot;dashboard-grid&quot;&amp;gt;
      ...
      &amp;lt;article className=&quot;hero-stage panel-full&quot;&amp;gt;
        &amp;lt;Nav current={current} onNavigate={onNavigate} /&amp;gt;
        &amp;lt;PageHeading title=&quot;关于我&quot; subtitle=&quot;项目，创意，灵感，心得，我的作品&quot; /&amp;gt;
      &amp;lt;/article&amp;gt;
      ...
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;上一节HomePage.jsx把个人主页封装成了一个组件，但里面有些数据仍然处于数据和页面没有分开的状态。&lt;/p&gt;
&lt;p&gt;假如想把“关于我”改成别的文字，要在这个复杂的结构里找到再修改。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// 网站要显示的&quot;内容&quot;，全都集中在这里。
// 想改标题、改文案、加作品？只动这个文件，组件代码一个字都不用碰。
// 这就是&quot;数据与界面分离&quot;：组件只管&quot;怎么显示&quot;，site.js 只管&quot;显示什么&quot;。

// 现在这些值写死在文件里，是因为还没有接入后端。
// 等接入后端，可以改成从网络接口实时取——而组件那边照样一个字都不用动。

export const home = {
  heroTitle: &quot;关于我&quot;,
  heroSubtitle: &quot;项目，创意，灵感，心得，我的作品&quot;,
  featuredWork: {
    kicker: &quot;作品&quot;,
    title: &quot;文字实验室&quot;,
    copy: &quot;拼音和情绪，挖掘中文里的细节&quot;,
    linkLabel: &quot;打开作品&quot;,
  },
  identity: {
    motto: &quot;已识乾坤大，尤怜草木青&quot;,
    learning: &quot;零到全栈&quot;,
  },
};

export const textLab = {
  heroTitle: &quot;文字实验室&quot;,
  heroSubtitle: &quot;拼音和情绪，挖掘中文里的细节&quot;,
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;通过将网站要显示的所有文字全部集中管理，能实现两者的分离。&lt;/p&gt;
&lt;p&gt;即使还没有接入后端也是有好处的。假设网站新增选取语言的功能，可以根据用户当前所选语言来维护site.zh.js、site.en.js、site.jp.js等。比在同一个组件里写大量if、else是方便很多的。&lt;/p&gt;
&lt;p&gt;组件目前的输入都是由调用者“喂”的，比如PageHeading的输入由HomePage从上层往下传。&lt;/p&gt;
&lt;p&gt;其实组件本身也可以生产数据。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/%E6%95%B0%E6%8D%AE%E9%A9%B1%E5%8A%A8%E9%A1%B5%E9%9D%A21.png&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;假设要给InputCard新加一个功能，根据输入显示目前已经输入了多少个字。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { useState } from &quot;react&quot;;

// 文字实验室的&quot;输入区&quot;卡片。
// 这一节它多了一个&quot;会变的值&quot;：text——这就是 state。
//   · text 是这个组件自己揣着的值，出生时带着一段默认文字；
//   · 用户每打一个字、删一个字，text 就变；
//   · text 一变，下面那行&quot;已输入 N 字&quot;当场跟着跳——
//     你连一句&quot;找到那个元素再去改它&quot;都不用写，React 自己刷。
export default function InputCard() {
  const [text, setText] = useState(&quot;今天的风很轻，适合把脑海里的想法慢慢写下来。&quot;);

  return (
    &amp;lt;article className=&quot;panel panel-half lab-panel card&quot;&amp;gt;
      &amp;lt;div className=&quot;panel-heading&quot;&amp;gt;
        &amp;lt;p className=&quot;section-kicker&quot;&amp;gt;输入区&amp;lt;/p&amp;gt;
        &amp;lt;h3&amp;gt;贴一段中文&amp;lt;/h3&amp;gt;
      &amp;lt;/div&amp;gt;
      &amp;lt;form className=&quot;lab-form&quot; onSubmit={(e) =&amp;gt; e.preventDefault()}&amp;gt;
        &amp;lt;label htmlFor=&quot;text-input&quot;&amp;gt;文本内容&amp;lt;/label&amp;gt;
        &amp;lt;textarea
          id=&quot;text-input&quot;
          rows=&quot;8&quot;
          placeholder=&quot;例如：生活没有标准答案，但每一天都值得认真感受。&quot;
          value={text}
          onChange={(e) =&amp;gt; setText(e.target.value)}
        /&amp;gt;
        {/* state 现身：text 一变，这行数字自动跟着变 */}
        &amp;lt;p className=&quot;lab-count&quot;&amp;gt;已输入 {text.length} 字&amp;lt;/p&amp;gt;
        &amp;lt;button className=&quot;primary-button&quot; type=&quot;button&quot;&amp;gt;开始分析&amp;lt;/button&amp;gt;
      &amp;lt;/form&amp;gt;
    &amp;lt;/article&amp;gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;State就有点像函数的局部变量。不过函数每次被重新调用局部变量会被重置，而State会被React记住，记忆边界是当前页面会话（一刷新就忘记了）。&lt;/p&gt;
&lt;p&gt;上一节App.jsx其实已经用到了State来记住目前处于HomePage页面还是TextLab页面，起到路由作用。&lt;/p&gt;
&lt;p&gt;而因为State一刷新就会被忘记，所以上一节的代码存在问题，只要一刷新就会回到HomePage页面。而且因为本质上两个页面都挂在index.html，它们的URL都是完全一样的。&lt;/p&gt;
&lt;p&gt;解决方法是可以把路由信息存到URL里去。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import HomePage from &quot;./components/HomePage.jsx&quot;;
import TextLabPage from &quot;./components/TextLabPage.jsx&quot;;
import { useRoute } from &quot;./router/useRoute.js&quot;;

export default function App() {
  // 上一节中，&quot;现在在哪一页&quot;记在内存（useState）里——一刷新就被打回首页。
  // 这一节换成 useRoute：把&quot;在哪一页&quot;读自 / 写入浏览器地址栏。
  const { path, navigate } = useRoute();

  // 看地址栏决定显示哪一页：/text-lab → 文字实验室；其它（含 /）→ 个人主页
  const current = path.startsWith(&quot;/text-lab&quot;) ? &quot;textlab&quot; : &quot;home&quot;;

  // 点导航 → 改地址栏（而不再是改内存里的值）
  const onNavigate = (key) =&amp;gt; navigate(key === &quot;textlab&quot; ? &quot;/text-lab&quot; : &quot;/&quot;);

  return (
    &amp;lt;div className=&quot;app-shell&quot;&amp;gt;
      &amp;lt;div className=&quot;page-shell&quot;&amp;gt;
        &amp;lt;main className=&quot;page-content&quot;&amp;gt;
          {current === &quot;home&quot;
            ? &amp;lt;HomePage current={current} onNavigate={onNavigate} /&amp;gt;
            : &amp;lt;TextLabPage current={current} onNavigate={onNavigate} /&amp;gt;}
        &amp;lt;/main&amp;gt;
      &amp;lt;/div&amp;gt;
    &amp;lt;/div&amp;gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;import { useEffect, useState } from &quot;react&quot;;

// 一个非常小的&quot;路由&quot;，干三件事：
//   - 读浏览器地址栏的 path，决定显示哪一页（一刷新还在这一页）
//   - 提供一个 navigate(path)，点导航时把&quot;在哪一页&quot;写进地址栏
//   - 用户按浏览器前进 / 后退，界面也跟着变
//
// 关键就在于：上一节把&quot;在哪一页&quot;记在内存里（一刷新就丢）；
// 这一版把它换成&quot;写在网址里&quot;——网址刷新不丢、能整条复制、能前进后退。
//
// 这一版&quot;够用但糙&quot;：自己手写、要兼顾的边角不少。真实项目里大家不手搓，
// 而是用现成的库（叫 react-router）；再往后 Next.js 会用&quot;文件夹=路由&quot;把它整个替掉。
export function useRoute() {
  const [path, setPath] = useState(window.location.pathname);

  useEffect(() =&amp;gt; {
    const handler = () =&amp;gt; setPath(window.location.pathname);
    window.addEventListener(&quot;popstate&quot;, handler);
    return () =&amp;gt; window.removeEventListener(&quot;popstate&quot;, handler);
  }, []);

  function navigate(to) {
    if (to === path) return;
    window.history.pushState({}, &quot;&quot;, to);
    setPath(to);
  }

  return { path, navigate };
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;总结地说，React将前端开发抽象成面向组件以后，界面的显示由“值”决定。&lt;/p&gt;
&lt;p&gt;这些值可以是组件内部的State、可以是URL、可以是父亲大组件传进来的输入、可以是site.js里集中定义的模版、可以是后端提供的值。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;├── README.md
├── index.html
├── node_modules
├── package-lock.json
├── package.json
├── src
│   ├── App.jsx
│   ├── components
│   │   ├── AnimatedCardGrid.jsx
│   │   ├── HomePage.jsx
│   │   ├── ...
│   ├── css
│   │   ├── cards.css
│   │   ├── hero.css
│   │   ├── ...
│   ├── data
│   │   └── site.js
│   ├── main.jsx
│   └── router
│       └── useRoute.js
└── vite.config.js
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这是加入site.js和useRoute.js以后的React项目结构。&lt;/p&gt;
</content:encoded></item><item><title>React组件化</title><link>https://cabbage74.github.io/posts/react%E7%BB%84%E4%BB%B6%E5%8C%96/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/react%E7%BB%84%E4%BB%B6%E5%8C%96/</guid><description>React的组件化思想</description><pubDate>Tue, 30 Jun 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;上一节里，构建工具承担了“翻译官”的角色。将开发者写起来舒服的代码“翻译”成浏览器看起来舒服的代码。&lt;/p&gt;
&lt;p&gt;构建工具可以“翻译”，意味着我们可以写出规则更加不同的代码，只要最后让构建工具“翻译”成浏览器看得懂的html/css/js就行了。&lt;/p&gt;
&lt;p&gt;React是最火的前端框架，核心思想是把复杂页面拆成可复用的组件，通过数据驱动组件更新。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;npm install react react-dom
npm install -D @vitejs/plugin-react
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;NPM安装react相关依赖、能让Vite知道怎么翻译react的插件。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { defineConfig } from &apos;vite&apos;
import react from &apos;@vitejs/plugin-react&apos;

export default defineConfig({
  plugins: [react()],
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;安装完还得编写Vite的配置文件vite.config.js，真正地“插”上插件。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/%E7%BB%84%E4%BB%B6%E5%8C%961.jpg&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;!doctype html&amp;gt;
&amp;lt;html lang=&quot;zh-CN&quot;&amp;gt;
  &amp;lt;head&amp;gt;
    &amp;lt;meta charset=&quot;UTF-8&quot; /&amp;gt;
    &amp;lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot; /&amp;gt;
    &amp;lt;title&amp;gt;Zero to Tech · 文字实验室&amp;lt;/title&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/reset.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/variables.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/layout.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/hero.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/nav.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/cards.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/lab.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/responsive.css&quot; /&amp;gt;
  &amp;lt;/head&amp;gt;
  &amp;lt;body&amp;gt;
    &amp;lt;div class=&quot;app-shell&quot;&amp;gt;
      &amp;lt;div class=&quot;page-shell&quot;&amp;gt;
        &amp;lt;main class=&quot;page-content&quot;&amp;gt;
          &amp;lt;section class=&quot;dashboard-grid&quot;&amp;gt;
            &amp;lt;article class=&quot;hero-stage panel-full&quot;&amp;gt;
              &amp;lt;div class=&quot;hero-topline&quot;&amp;gt;
                &amp;lt;p class=&quot;brand-eyebrow&quot;&amp;gt;zero to tech&amp;lt;/p&amp;gt;
                &amp;lt;nav class=&quot;inline-links hero-nav&quot;&amp;gt;
                  &amp;lt;a class=&quot;nav-link&quot; href=&quot;index.html&quot;&amp;gt;个人主页&amp;lt;/a&amp;gt;
                  &amp;lt;a class=&quot;nav-link&quot; href=&quot;text-lab.html&quot;&amp;gt;文字实验室&amp;lt;/a&amp;gt;
                &amp;lt;/nav&amp;gt;
              &amp;lt;/div&amp;gt;
              &amp;lt;div class=&quot;hero-copy&quot;&amp;gt;
                &amp;lt;h1 class=&quot;hero-display&quot;&amp;gt;文字实验室&amp;lt;/h1&amp;gt;
                &amp;lt;p class=&quot;hero-subtitle&quot;&amp;gt;拼音和情绪，挖掘中文里的细节&amp;lt;/p&amp;gt;
              &amp;lt;/div&amp;gt;
            &amp;lt;/article&amp;gt;

            &amp;lt;article class=&quot;panel panel-half lab-panel card&quot;&amp;gt;
              &amp;lt;div class=&quot;panel-heading&quot;&amp;gt;
                &amp;lt;p class=&quot;section-kicker&quot;&amp;gt;输入区&amp;lt;/p&amp;gt;
                &amp;lt;h3&amp;gt;贴一段中文&amp;lt;/h3&amp;gt;
              &amp;lt;/div&amp;gt;
              &amp;lt;form class=&quot;lab-form&quot;&amp;gt;
                &amp;lt;label for=&quot;text-input&quot;&amp;gt;文本内容&amp;lt;/label&amp;gt;
                &amp;lt;textarea
                  id=&quot;text-input&quot;
                  rows=&quot;8&quot;
                  placeholder=&quot;例如：生活没有标准答案，但每一天都值得认真感受。&quot;
                &amp;gt;今天的风很轻，适合把脑海里的想法慢慢写下来。&amp;lt;/textarea&amp;gt;
                &amp;lt;button class=&quot;primary-button&quot; type=&quot;button&quot;&amp;gt;开始分析&amp;lt;/button&amp;gt;
              &amp;lt;/form&amp;gt;
            &amp;lt;/article&amp;gt;

            &amp;lt;article class=&quot;panel panel-half lab-panel result-panel card&quot;&amp;gt;
              &amp;lt;div class=&quot;panel-heading&quot;&amp;gt;
                &amp;lt;p class=&quot;section-kicker&quot;&amp;gt;结果区&amp;lt;/p&amp;gt;
                &amp;lt;h3&amp;gt;分析结果&amp;lt;/h3&amp;gt;
              &amp;lt;/div&amp;gt;
              &amp;lt;div class=&quot;result-stack&quot;&amp;gt;
                &amp;lt;div class=&quot;result-item&quot;&amp;gt;
                  &amp;lt;span&amp;gt;原文&amp;lt;/span&amp;gt;
                  &amp;lt;p&amp;gt;今天的风很轻，适合把脑海里的想法慢慢写下来。&amp;lt;/p&amp;gt;
                &amp;lt;/div&amp;gt;
                &amp;lt;div class=&quot;result-item&quot;&amp;gt;
                  &amp;lt;span&amp;gt;拼音&amp;lt;/span&amp;gt;
                  &amp;lt;p&amp;gt;jīn tiān de fēng hěn qīng …&amp;lt;/p&amp;gt;
                &amp;lt;/div&amp;gt;
                &amp;lt;div class=&quot;result-grid&quot;&amp;gt;
                  &amp;lt;div class=&quot;result-badge&quot;&amp;gt;
                    &amp;lt;span&amp;gt;情感分数&amp;lt;/span&amp;gt;
                    &amp;lt;strong data-score&amp;gt;0.86&amp;lt;/strong&amp;gt;
                  &amp;lt;/div&amp;gt;
                  &amp;lt;div class=&quot;result-badge&quot;&amp;gt;
                    &amp;lt;span&amp;gt;情感判断&amp;lt;/span&amp;gt;
                    &amp;lt;strong&amp;gt;偏积极&amp;lt;/strong&amp;gt;
                  &amp;lt;/div&amp;gt;
                &amp;lt;/div&amp;gt;
              &amp;lt;/div&amp;gt;
            &amp;lt;/article&amp;gt;

          &amp;lt;/section&amp;gt;
        &amp;lt;/main&amp;gt;
      &amp;lt;/div&amp;gt;
    &amp;lt;/div&amp;gt;

    &amp;lt;script type=&quot;module&quot; src=&quot;js/main.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;对于文字实验室text-lab.html这个文件，现在是vanilla的写法。html把页面结构写的很长，而且要写新的一页还是得写这么长。&lt;/p&gt;
&lt;p&gt;其实可以按导航Nav、输入卡片InputCard等拆成可复用的组件。很多页面可以复用导航Nav。&lt;/p&gt;
&lt;p&gt;html/css/js就像是后端的controller/service/repository按技术切分。React就像是后端按业务切分服务一样切分出组件。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// Nav 原本在 index.html 和 text-lab.html 各抄了一遍。
// 现在它是一个组件，整个项目只有这一份。
// 想加链接、改样式？只改这一处，两个页面自动一起跟着变。
export default function Nav({ current, onNavigate }) {
  const items = [
    { key: &quot;home&quot;,    label: &quot;个人主页&quot; },
    { key: &quot;textlab&quot;, label: &quot;文字实验室&quot; },
  ];

  return (
    &amp;lt;div className=&quot;hero-topline&quot;&amp;gt;
      &amp;lt;p className=&quot;brand-eyebrow&quot;&amp;gt;zero to tech&amp;lt;/p&amp;gt;
      &amp;lt;nav className=&quot;inline-links hero-nav&quot;&amp;gt;
        {items.map((it) =&amp;gt; (
          &amp;lt;a
            key={it.key}
            href=&quot;#&quot;
            className={&quot;nav-link&quot; + (current === it.key ? &quot; active&quot; : &quot;&quot;)}
            onClick={(e) =&amp;gt; { e.preventDefault(); onNavigate(it.key); }}
          &amp;gt;
            {it.label}
          &amp;lt;/a&amp;gt;
        ))}
      &amp;lt;/nav&amp;gt;
    &amp;lt;/div&amp;gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;以导航Nav.jsx为例。一个jsx文件一般对应一个组件，一个组件就像是一个函数，输出一段由&lt;strong&gt;运行时输入&lt;/strong&gt;决定的类似html的结构。这个结构经过React和Vite最终会变成真的html。容易想象每个组件都是一个动态的DOM子树，由React来动态地创建、修改、销毁。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import Nav from &quot;./Nav.jsx&quot;;
import PageHeading from &quot;./PageHeading.jsx&quot;;
import AnimatedCardGrid from &quot;./AnimatedCardGrid.jsx&quot;;
import InputCard from &quot;./InputCard.jsx&quot;;
import ResultCard from &quot;./ResultCard.jsx&quot;;

// 整页就是几块积木拼起来的：导航、标题、输入卡、结果卡。
// TextLabPage 这个&quot;大组件&quot;，把这些小组件摆在一起，拼成一整页。
export default function TextLabPage({ current, onNavigate }) {
  return (
    &amp;lt;AnimatedCardGrid className=&quot;dashboard-grid&quot;&amp;gt;
      &amp;lt;article className=&quot;hero-stage panel-full&quot;&amp;gt;
        &amp;lt;Nav current={current} onNavigate={onNavigate} /&amp;gt;
        &amp;lt;PageHeading title=&quot;文字实验室&quot; subtitle=&quot;拼音和情绪，挖掘中文里的细节&quot; /&amp;gt;
      &amp;lt;/article&amp;gt;

      &amp;lt;InputCard /&amp;gt;
      &amp;lt;ResultCard /&amp;gt;
    &amp;lt;/AnimatedCardGrid&amp;gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;再看含有Nav的页面TextLabPage.jsx。其实和Nav没什么区别，Nav像是小积木，TextLabPage像是大积木。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/%E7%BB%84%E4%BB%B6%E5%8C%962.jpg&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Vanilla写法里，不同的页面一般就是不同的html。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { useState } from &quot;react&quot;;
import HomePage from &quot;./components/HomePage.jsx&quot;;
import TextLabPage from &quot;./components/TextLabPage.jsx&quot;;

export default function App() {
  // page 是个&quot;状态&quot;——它一变，下面的界面就跟着重新渲染。
  // 这一节先把它当&quot;框架的规则&quot;用、不展开
  const [page, setPage] = useState(&quot;home&quot;);

  return (
    &amp;lt;div className=&quot;app-shell&quot;&amp;gt;
      &amp;lt;div className=&quot;page-shell&quot;&amp;gt;
        &amp;lt;main className=&quot;page-content&quot;&amp;gt;
          {page === &quot;home&quot;
            ? &amp;lt;HomePage current={page} onNavigate={setPage} /&amp;gt;
            : &amp;lt;TextLabPage current={page} onNavigate={setPage} /&amp;gt;}
        &amp;lt;/main&amp;gt;
      &amp;lt;/div&amp;gt;
    &amp;lt;/div&amp;gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;React里每个页面都是一个组件，可以把它们合成一个最大的组件App.jsx。切换页面不再是切换html，而是切换组件。写完App.jsx就写完了最大的一个组件。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { StrictMode } from &quot;react&quot;;
import { createRoot } from &quot;react-dom/client&quot;;
import App from &quot;./App.jsx&quot;;

import &quot;./css/reset.css&quot;;
import &quot;./css/variables.css&quot;;
import &quot;./css/layout.css&quot;;
import &quot;./css/hero.css&quot;;
import &quot;./css/nav.css&quot;;
import &quot;./css/cards.css&quot;;
import &quot;./css/lab.css&quot;;
import &quot;./css/responsive.css&quot;;

createRoot(document.getElementById(&quot;root&quot;)).render(
  &amp;lt;StrictMode&amp;gt;
    &amp;lt;App /&amp;gt;
  &amp;lt;/StrictMode&amp;gt;,
);
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;!doctype html&amp;gt;
&amp;lt;html lang=&quot;zh-CN&quot;&amp;gt;
  &amp;lt;head&amp;gt;
    &amp;lt;meta charset=&quot;UTF-8&quot; /&amp;gt;
    &amp;lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot; /&amp;gt;
    &amp;lt;title&amp;gt;Zero to Tech&amp;lt;/title&amp;gt;
  &amp;lt;/head&amp;gt;
  &amp;lt;body&amp;gt;
    &amp;lt;div id=&quot;root&quot;&amp;gt;&amp;lt;/div&amp;gt;
    &amp;lt;script type=&quot;module&quot; src=&quot;/src/main.jsx&quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;把index.html和main.jsx结合起来看，就知道组件是怎么和html连接起来的了。&lt;/p&gt;
&lt;p&gt;React项目里，只有一个空的div，dom树一开始是空的。main.jsx这个脚本告诉React要渲染root这个div。&lt;/p&gt;
&lt;p&gt;容易发现这种React项目里只有一棵DOM树。&lt;/p&gt;
&lt;p&gt;传统Vanilla写法：每个 HTML 文件被加载时，浏览器直接解析该 HTML，构建出整棵 DOM 树。切换页面时，旧 DOM 树被完全销毁，浏览器加载新 HTML，构建一棵全新的 DOM 树。&lt;/p&gt;
&lt;p&gt;React写法：整个应用只有一个 HTML 文件。里面有个容器，比如 root div。React接管这个div，然后动态地在这个div内部创建、更新、销毁 DOM 节点，来模拟“页面切换”。浏览器只加载一次 HTML，后续的“页面”切换，都是 React 在这个 root div 里面变魔术。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;├── README.md
├── index.html
├── node_modules
├── package-lock.json
├── package.json
├── src
│   ├── App.jsx
│   ├── components
│   │   ├── AnimatedCardGrid.jsx
│   │   ├── HomePage.jsx
│   │   ├── InputCard.jsx
│   │   ├── Nav.jsx
│   │   ├── ...
│   ├── css
│   │   ├── cards.css
│   │   ├── hero.css
│   │   ├── ...
│   └── main.jsx
└── vite.config.js
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这是一个React项目的经典结构。&lt;/p&gt;
</content:encoded></item><item><title>HBase + ElasticSearch</title><link>https://cabbage74.github.io/posts/hbase--elasticsearch/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/hbase--elasticsearch/</guid><description>研究一下这个组合的读写特性</description><pubDate>Mon, 29 Jun 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;实习业务部分和这套相关，有必要研究一下读写特性。&lt;/p&gt;
&lt;h2&gt;HBase&lt;/h2&gt;
&lt;p&gt;HBase的设计是面向这样的场景：PB级数据、追求对单行数据或小范围数据的&lt;strong&gt;毫秒级&lt;/strong&gt;访问。是TP系统。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/hbase-architecture.png&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;HBase 的数据模型是一张巨大的、排序的、稀疏的映射表（Sorted Map），行键（RowKey）按字典序排序。为了分布式存储，表会被自动切分成很多个 Region。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Region：就是一片连续的行键范围，是 HBase 中分布式和负载均衡的最小单元。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;每个 Region 会交给一个 RegionServer 管理。一个 RegionServer 可以管理很多个 Region。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;RegionServer 就是真正的工人，负责处理它管辖的 Region 上的所有读写请求。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;Region (行键范围)
                 |
        +--------+--------+
        |                 |
     Store A            Store B    (对应列族)
        |                 |
  +-----+-----+     +-----+-----+
  |           |     |           |
MemStore   HFile   MemStore   HFile ...
(内存)     (磁盘)   (内存)     (磁盘)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;每个工人（RegionServer）管理抽屉（Region）时，内部又有精细分工。一个 Region 包含多个 Store，每个 Store 对应一个列族。&lt;/p&gt;
&lt;p&gt;每个 Store 内部有：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;MemStore（内存缓存）：一个有序的内存区域，所有写入操作先写一份到 HLog（预写日志），然后再写这里，返回成功。这是 HBase 写速度极快的原因——顺序写WAL+内存写入。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;HFile（磁盘文件）：当 MemStore 大到一定程度，整个刷写到 HDFS 上变成一个不可变的 HFile。读数据时，需要合并 MemStore 和多个 HFile 中的结果。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;HLog（Write-Ahead Log）：存于 HDFS，是安全的保障。在数据刷到 HFile 前，若机器宕机，可以从 HLog 重放恢复数据。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;HFile对应LSM-Tree里的SSTable，Memstore显然就是Memtable。HBase里每一个Store对应一棵LSM-Tree。&lt;/p&gt;
&lt;p&gt;LSM-Tree的K是(rowkey, family, qualifier, timestamp, type)，V是cell value。&lt;/p&gt;
&lt;p&gt;type指的是put/delete。&lt;/p&gt;
&lt;p&gt;上面的HDFS只是一个例子，其实S3之类的都可以，但文件格式都是HFile。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;写入流程：&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Client 问 ZooKeeper：“我要写表 T，行键 R 的元数据存在哪？” ZooKeeper 告诉他去找哪个 RegionServer（存有 &lt;code&gt;hbase:meta&lt;/code&gt; 表的 RegionServer）。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Client 从 &lt;code&gt;meta&lt;/code&gt; 表查到，目标行键 R 属于哪个 Region，由哪个 RegionServer 管理。这个位置信息会缓存起来。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Client 直接联系那个 RegionServer，发出写请求。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;RegionServer 先将写入记录到 HLog，然后写入对应 Region 的 MemStore。一旦两者都完成，就返回写入成功。&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;读取流程：&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;同样的元数据定位，Client 直接找到目标 RegionServer。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;RegionServer 要在该行所在的 Region 里，合并 MemStore 和所有相关的 HFile 数据，按时间戳或版本取出最新的，返回给 Client。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;读操作是强一致的：一定能读到之前已写入的数据。&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;HMaster不参与读写，只管负载均衡、故障转移等。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/hbase-table.png&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;整张表是按RowKey的字典序排序的。&lt;/p&gt;
&lt;p&gt;每个表一开始只有一个Region，随着不断增大会分裂。&lt;/p&gt;
&lt;h2&gt;ElasticSearch&lt;/h2&gt;
&lt;p&gt;从HBase的结构能看出HBase肯定很擅长下面的场景：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;点查。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;小范围顺序查：如果落在少数几个Region里。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;高吞吐随机写：不同Store是不同的LSM-Tree，可以并发写。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;支持单行事务：一行肯定由一个Region管理，一个Region肯定由一个Region Server管理，意味着一行的修改肯定在一个进程内（但也只支持单行事务，跨行意味着可能跨Region）。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也能看出它不擅长：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;全表扫描。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;复杂聚合等AP负载（HBase没有计算引擎，本身其实不支持SQL）。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;不基于RowKey的范围查询：LSM-Tree的Key失效了。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;多表JOIN。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;所以HBase经常和ES结合使用，解决HBase不擅长的“不基于RowKey的复杂条件查询”。&lt;/p&gt;
&lt;p&gt;使我们能够在“海量数据下，快速找到符合条件的那些 RowKey”。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;解决“不基于 RowKey 查询”的问题：&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;HBase 的痛：如果 RowKey 是 &lt;code&gt;user_id&lt;/code&gt;，你要查 &lt;code&gt;age &amp;gt; 18 and city = &apos;Beijing&apos;&lt;/code&gt; 的所有用户，HBase 只能全表扫描。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;ES 的解决方案：将 &lt;code&gt;age&lt;/code&gt;、&lt;code&gt;city&lt;/code&gt; 等需要查询的字段同步到 ES。ES 会为这些字段建立倒排索引。查询时，ES 能快速返回符合条件的 &lt;code&gt;user_id&lt;/code&gt; 列表。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;最终流程：应用 -&amp;gt; 向 ES 发起复杂条件查询 -&amp;gt; ES 返回一批 RowKey -&amp;gt; 应用拿着 RowKey 到 HBase 做批量精确查询 -&amp;gt; 返回完整数据。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;解决“实时聚合分析”的需求：&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;HBase 的痛：做 &lt;code&gt;GROUP BY&lt;/code&gt;, &lt;code&gt;COUNT&lt;/code&gt;, &lt;code&gt;AVG&lt;/code&gt; 等聚合分析性能极差。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;ES 的解决方案：ES 有强大的聚合框架（Aggregations），可以秒级完成按城市统计用户数、按时间段统计 PV 等分析。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;工作流通常长这样：&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;数据写入：一条新数据来了，应用层会双写（或通过数据同步工具）：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;全量写入 HBase：把这条数据的全部字段，按精心设计的 RowKey（如 &lt;code&gt;user_id&lt;/code&gt;）存进去。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;索引写入 ES：只把需要被查询的字段（如 &lt;code&gt;name&lt;/code&gt;, &lt;code&gt;age&lt;/code&gt;, &lt;code&gt;city&lt;/code&gt;, &lt;code&gt;tags&lt;/code&gt;）和 HBase 的 RowKey，一起写入 ES。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;数据查询：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;用户想查“在北京的、标签包含‘篮球’的、所有用户”。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;查询请求先到 ES，ES 检索索引，返回所有满足条件的 &lt;code&gt;user_id&lt;/code&gt; 列表。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;服务端拿到 &lt;code&gt;user_id&lt;/code&gt; 列表，去 HBase 批量 &lt;code&gt;GET&lt;/code&gt; 这些 RowKey 对应的完整数据。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;将完整结果返回给用户。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
</content:encoded></item><item><title>NPM与构建工具</title><link>https://cabbage74.github.io/posts/npm%E4%B8%8E%E6%9E%84%E5%BB%BA%E5%B7%A5%E5%85%B7/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/npm%E4%B8%8E%E6%9E%84%E5%BB%BA%E5%B7%A5%E5%85%B7/</guid><description>了解Node、NPM、Vite</description><pubDate>Mon, 29 Jun 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;上节的模块化只解决了前端开发中一小部分问题。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;!doctype html&amp;gt;
&amp;lt;html lang=&quot;zh-CN&quot;&amp;gt;
  &amp;lt;head&amp;gt;
    ...
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/reset.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/variables.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/layout.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/hero.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/nav.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/cards.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/lab.css&quot; /&amp;gt;
    &amp;lt;link rel=&quot;stylesheet&quot; href=&quot;css/responsive.css&quot; /&amp;gt;
  &amp;lt;/head&amp;gt;
  &amp;lt;body&amp;gt;
    ...
    &amp;lt;script type=&quot;module&quot; src=&quot;js/main.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;延续上一节模块化改造完的index.html代码。&lt;/p&gt;
&lt;p&gt;目前还有几个可以进一步优化的问题：&lt;/p&gt;
&lt;p&gt;一是引入模块化写法以后一定要走HTTP、HTTPS，调试起来不方便。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/NPM1.png&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;二是要请求的文件太多，需要排队。虽然拆分代码方便程序员调试维护，但不方便浏览器请求。&lt;/p&gt;
&lt;p&gt;三是浏览器会缓存旧文件，比如这里的anime库走的是memory cache。刷新页面可能不会很及时地体现背后代码的变动。一个笨方法是每次改完代码就把文件名也手动修改一下，让浏览器不走缓存，但显然不方便。&lt;/p&gt;
&lt;p&gt;构建工具可以帮助解决这三个问题，Vite是现在最流行的一个。&lt;/p&gt;
&lt;p&gt;Vite是用TypeScript写跑在Node.js上的，Node.js是一套让js能在浏览器外面跑的环境。&lt;/p&gt;
&lt;p&gt;npm是Node.js的包管理器，类似Cargo之于Rust。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;npm init -y
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;用npm初始化项目，-y是告诉npm全部走默认，跳过交互式的提问&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;name&quot;: &quot;zero-to-tech-4-1&quot;,
  &quot;version&quot;: &quot;1.0.0&quot;,
  &quot;scripts&quot;: {
    &quot;test&quot;: &quot;echo \&quot;Error: no test specified\&quot; &amp;amp;&amp;amp; exit 1&quot;
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;初始化完会多一个package.json，有很多其实没什么用的字段，这里都手动删掉了。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;npm install -D vite
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;-D代表装的是开发依赖，不进最终的网站。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;name&quot;: &quot;zero-to-tech-4-1&quot;,
  &quot;version&quot;: &quot;1.0.0&quot;,
  &quot;scripts&quot;: {
    &quot;test&quot;: &quot;echo \&quot;Error: no test specified\&quot; &amp;amp;&amp;amp; exit 1&quot;
  },
  &quot;devDependencies&quot;: {
    &quot;vite&quot;: &quot;^8.1.0&quot;
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;package.json多了一条依赖。&lt;/p&gt;
&lt;p&gt;而且装完以后多了一个node_modules文件夹，里面有Vite和Vite自身依赖的其他包的源代码，.bin文件夹里有可执行文件。&lt;/p&gt;
&lt;p&gt;还多一个package-lock.json，里面有每一个包的具体版本，是为了锁死更深层的依赖。&lt;/p&gt;
&lt;p&gt;和Cargo.toml、Cargo.lock的设计是一样的。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;./node_modules/.bin/vite
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;此时执行Vite的可执行文件，Vite就能起一个服务器用于开发预览，而且是热更新不用自己刷新的。解决了文章开头提到的第一个问题。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;./node_modules/.bin/vite build
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;./images/NPM2.png&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;执行build构建命令，多了一个dist文件夹，里面就是构建产物。这个产物是实际放在服务器的代码。&lt;/p&gt;
&lt;p&gt;Vite构建的核心就是保持代码语义，但把代码打包成最适合浏览器使用的形态。可以看到css和js都各自被合成了一个文件，解决了文章开头提到的第二个问题。文件名最后有一串哈希指纹，解决了文章开头提到的第三个问题。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;./node_modules/.bin/vite preview
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这个命令会起一个服务器，可以预览dist产物。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;name&quot;: &quot;zero-to-tech-4-1&quot;,
  &quot;version&quot;: &quot;1.0.0&quot;,
  &quot;scripts&quot;: {
    &quot;test&quot;: &quot;echo \&quot;Error: no test specified\&quot; &amp;amp;&amp;amp; exit 1&quot;,
    &quot;dev&quot;: &quot;vite&quot;,             // npm run dev*
    &quot;build&quot;: &quot;vite build&quot;,     // npm run build*
    &quot;preview&quot;: &quot;vite preview&quot;  // npm run preview*
  },
  &quot;devDependencies&quot;: {
    &quot;vite&quot;: &quot;^8.1.0&quot;
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;前面使用Vite的命令都不是很方便，普遍做法是在package.json自定义命令。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { animate, stagger } from &quot;https://cdn.jsdelivr.net/npm/animejs@4/+esm&quot;;
...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;现在对于anime第三方库的import依然是走cdn服务，为了可靠还是应该让npm把它装成本地依赖。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;npm install animejs
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;import { animate, stagger } from &quot;animejs&quot;;
...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Vite会将animejs指向node_modules下的animejs。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;name&quot;: &quot;zero-to-tech-4-1&quot;,
  &quot;version&quot;: &quot;1.0.0&quot;,
  &quot;scripts&quot;: {
    &quot;test&quot;: &quot;echo \&quot;Error: no test specified\&quot; &amp;amp;&amp;amp; exit 1&quot;,
    &quot;dev&quot;: &quot;vite&quot;,
    &quot;build&quot;: &quot;vite build&quot;,
    &quot;preview&quot;: &quot;vite preview&quot;
  },
  &quot;devDependencies&quot;: {
    &quot;vite&quot;: &quot;^8.1.0&quot;
  },
  &quot;dependencies&quot;: {
    &quot;animejs&quot;: &quot;^4.5.0&quot;
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;顺便package.json确实能看出来-D install和普通install的区别。&lt;/p&gt;
</content:encoded></item><item><title>Python入门（三）</title><link>https://cabbage74.github.io/posts/python%E5%85%A5%E9%97%A8%E4%B8%89/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/python%E5%85%A5%E9%97%A8%E4%B8%89/</guid><description>Python独特语法</description><pubDate>Sun, 28 Jun 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;类&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;class A:
    def __new__(cls, *args, **kwargs):
        return super().__new__(cls)

    def __init__(self, value):
        self.value = value

    def display(self):
        print(f&quot;Value: {self.value}&quot;)

    def __del__(self):
        print(f&quot;Deleting instance with value: {self.value}&quot;)

a = A(10)
a.display() # Value: 10

b = a
del a
b.display() # Value: 10

# Deleting instance with value: 10
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;别的语言的构造函数被Python分成了两块。&lt;code&gt;__new__&lt;/code&gt;负责内存分配，这个内存是固定大小的，只放对象的骨架。这块固定的内存里有一个空字典&lt;code&gt;__dict__={}&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;__init__&lt;/code&gt;负责初始化属性，在这里第一次定义的变量就是放在&lt;code&gt;__dict__&lt;/code&gt;指向的地方。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;self.value = 10&lt;/code&gt;本质是&lt;code&gt;self.__dict__[&apos;value&apos;] = 10&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;__del__&lt;/code&gt;是析构函数，引用计数为零的时候就会被调用。&lt;/p&gt;
&lt;p&gt;手动调用&lt;code&gt;del&lt;/code&gt;只会让引用计数减一，不会强制回收内存里这块值。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class A:
    name = &quot;Class A&quot;

    def __init__(self, value):
        self.value = value

    version = 1.0
    _speed = 100
    __hidden = &quot;This is a hidden attribute&quot;

    def display(self):
        print(f&quot;Value: {self.value}&quot;)

    
print(A.name)  # Class A
print(A.version)  # 1.0
print(A._speed)  # 100
print(A._A__hidden)  # This is a hidden attribute
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;不在方法里的属性都是类属性。&lt;/p&gt;
&lt;p&gt;在属性前面加一个下划线是约定“不要访问我”，但硬要访问也可以。&lt;/p&gt;
&lt;p&gt;加两个下划线是让编译器改名，但这个改名的模式是可预测的。一般用于防止子类意外覆盖父类的同名属性。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class A:
    name = &quot;Class A&quot;

    def __init__(self, value):
        self.value = value

    @classmethod
    def get_name(cls):
        return cls.name

    @staticmethod
    def add(a, b):
        return a + b

    @property
    def info(self):
        return f&quot;{self.__class__.__name__}(value={self.value})&quot;


print(A.get_name())       # Class A
print(A.add(1, 2))        # 3

a = A(10)
print(a.info)             # A(value=10)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;@classmethod&lt;/code&gt;修饰的是类方法，第一个参数是类本身，所以通常只访问类属性。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;@staticmethod&lt;/code&gt;修饰的是静态方法，类似工具函数。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;@property&lt;/code&gt;修饰的让方法能像属性一样被访问。&lt;/p&gt;
&lt;p&gt;这三个装饰器的原理和普通的函数套函数语法糖不一样，是基于描述符协议实现的。经过我和AI的讨论，我决定姑且把它们记作Python的特殊实现，描述符协议我不打算深入了解。&lt;/p&gt;
&lt;p&gt;一般情况下，Python的装饰器有两个用法，一个就是普通的函数套函数语法糖，一个是给函数注入一些元信息方便框架进行扫描。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;class A:
    def __init__(self, value):
        self.value = value

    def __str__(self):
        return f&quot;A(value={self.value})&quot;

    def __lt__(self, other):
        return self.value &amp;lt; other.value
 
    def __call__(self, x):
        return self.value + x


a = A(10)
print(str(a))           # A(value=10)
print(a &amp;lt; A(20))        # True
print(a(5))             # 15
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python里以两个下划线开头和结尾的是特殊方法，大概有100多个。这里我挑了三个。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;__call__&lt;/code&gt;很有意思，让对象能被调用。&lt;code&gt;def&lt;/code&gt;本质就是创建了一个&lt;code&gt;function&lt;/code&gt;的实例对象，而&lt;code&gt;function&lt;/code&gt;类实现了&lt;code&gt;__call__&lt;/code&gt;。&lt;/p&gt;
&lt;h2&gt;错误和异常&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;def make_guess():
    guess = int(input(&quot;Guess a number between 1 and 10: &quot;)) # fifty
    return guess

guess = make_guess()

# Traceback (most recent call last):
#   File &quot;/Users/cabbage/python-lab/main.py&quot;, line 5, in &amp;lt;module&amp;gt;
#     guess = make_guess()
#   File &quot;/Users/cabbage/python-lab/main.py&quot;, line 2, in make_guess
#     guess = int(input(&quot;Guess a number between 1 and 10: &quot;))
# ValueError: invalid literal for int() with base 10: &apos;fifty&apos;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python的异常自底向上读，是由近向远的调用栈。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def divide(a, b):
    try:
        result = a / b
    except ZeroDivisionError as e:
        raise ValueError(&quot;b不能等于0&quot;) from e
    else:
        print(f&quot;result = {result}&quot;)
        return result
    finally:
        print(&quot;divide结束&quot;)


divide(10, 2)

try:
    divide(10, 0)
except ValueError as e:
    print(e)          # b不能等于0
    print(e.__cause__) # division by zero
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;try&lt;/code&gt;里放可能出错的代码，&lt;code&gt;except&lt;/code&gt;负责捕获异常，&lt;code&gt;else&lt;/code&gt;只会在没报错的时候执行，&lt;code&gt;finally&lt;/code&gt;无论有没有异常都会执行（只要配对的&lt;code&gt;try&lt;/code&gt;执行了就会执行，不管有没有&lt;code&gt;return&lt;/code&gt;或&lt;code&gt;raise&lt;/code&gt;。不过进程被杀或者解释器崩溃的话还是会执行不了）。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;raise ... from e&lt;/code&gt;会把新的异常和原来的异常串起来，形成异常链。如果不用&lt;code&gt;raise from&lt;/code&gt;只是简单&lt;code&gt;raise&lt;/code&gt;有可能丢失原始错误的上下文。&lt;/p&gt;
</content:encoded></item><item><title>Python入门（二）</title><link>https://cabbage74.github.io/posts/python%E5%85%A5%E9%97%A8%E4%BA%8C/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/python%E5%85%A5%E9%97%A8%E4%BA%8C/</guid><description>Python独特语法</description><pubDate>Mon, 22 Jun 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;这篇记录阅读过程中看到的Python里让我感觉比较意外、反直觉的独特语法。&lt;/p&gt;
&lt;h2&gt;作用域&lt;/h2&gt;
&lt;p&gt;Python里只有函数和推导式定义了作用域的结构。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def spam():
    message = &quot;Spam&quot;
    word = &quot;Spam&quot;
    for _ in range(10):
        separator = &quot;,&quot;
        message += separator + word
    message += separator
    message += &quot;spam!&quot;
    return message
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;循环是没有作用域的，所以&lt;code&gt;separator&lt;/code&gt;可以在循环外面用。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;high_score = 10

def score(new_score):
    high_score = new_score

score(100)
print(high_score) # 10
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;如果在函数内部定义和全局变量同名的变量，会shadow掉（如果不定义，只是使用的话不会）。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;high_score = 10

def score(new_score):
    global high_score
    high_score = new_score

score(100)
print(high_score) # 100
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;显式地用&lt;code&gt;global&lt;/code&gt;关键字能解决这个问题。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def score():
    val = 1
    def score2():
        nonlocal val
        val = 2
    score2()
    print(val) # 2

score()    
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;类似&lt;code&gt;global&lt;/code&gt;，在内部函数要修改外部函数的变量时，用&lt;code&gt;nonlocal&lt;/code&gt;关键字，只是读取的话不需要。&lt;/p&gt;
&lt;p&gt;存在多层嵌套的函数时，&lt;code&gt;nonlocal&lt;/code&gt;的查找顺序是由里向外就近原则。&lt;/p&gt;
&lt;h2&gt;不可变值与可变值&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;a = 1
b = a
print(b is a) # True
b += 1
print(b is a) # False
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python里整数、浮点数、字符串、元组这类值是不可变的，意味着修改的时候其实是创建一个新内存空间，在里面写新值。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;c = [1, 2, 3]
d = c
print(d is c) # True
d += [4, 5]
print(d is c) # True
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;可变类型比如列表，就是原地更新。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def find_lowest(numbers):
    numbers.sort()
    return numbers[0]

a = [3, 1, 2]
find_lowest(a)
print(a)  # [1, 2, 3]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python函数传参的语义是“赋值”传递，等于多定义一个变量指向这个值，所以传可变对象的时候函数内变动会对函数外有影响。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import copy

original = [[1, 2, 3], [4, 5, 6]]

# 普通赋值：只是起了个别名，指向同一个对象
ref = original

# 浅拷贝：外层列表是新的，内层列表仍是原对象的引用
shallow = copy.copy(original)

# 深拷贝：外层和内层列表都是全新的独立副本
deep = copy.deepcopy(original)

# 修改原对象的内层列表
original[0][0] = 999

print(f&quot;original: {original}&quot;)   # [[999, 2, 3], [4, 5, 6]]
print(f&quot;ref:      {ref}&quot;)        # [[999, 2, 3], [4, 5, 6]] ← 完全一样！
print(f&quot;shallow:  {shallow}&quot;)    # [[999, 2, 3], [4, 5, 6]] ← 受影响！
print(f&quot;deep:     {deep}&quot;)       # [[1, 2, 3], [4, 5, 6]]   ← 不受影响


original2 = [[1, 2, 3], [4, 5, 6]]
ref2 = original2                      # 普通赋值
shallow2 = copy.copy(original2)       # 浅拷贝
deep2 = copy.deepcopy(original2)      # 深拷贝

# 修改外层列表
original2.append([7, 8, 9])           

print(f&quot;original2: {original2}&quot;)      # [[1,2,3], [4,5,6], [7,8,9]]
print(f&quot;ref2:      {ref2}&quot;)           # [[1,2,3], [4,5,6], [7,8,9]] ← 一样！
print(f&quot;shallow2:  {shallow2}&quot;)       # [[1,2,3], [4,5,6]]          ← 不受影响！
print(f&quot;deep2:     {deep2}&quot;)          # [[1,2,3], [4,5,6]]          ← 不受影响

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;上面这个程序能清楚表现普通赋值、浅拷贝、深拷贝的区别。&lt;/p&gt;
&lt;h2&gt;函数签名&lt;/h2&gt;
&lt;p&gt;Python的函数签名应该是我目前见过的语言里最花里胡哨的了。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def fib(series=[1,1]):
    series.append(series[-1] + series[-2])
    return series

print(fib()) # [1, 1, 2]
print(fib()) # [1, 1, 2, 3]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;:::tip
默认参数值只在函数定义（&lt;code&gt;def&lt;/code&gt;语句执行）时计算一次，不是调用时，所以永远不要在默认参数放一个可变值。
:::&lt;/p&gt;
&lt;p&gt;这个坑&lt;code&gt;Momenta&lt;/code&gt;面试的时候其实被问了，当时不会。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def fib(series=None):
    if series is None:
        series = [1, 1]
    series.append(series[-1] + series[-2])
    return series

print(fib()) # [1, 1, 2]
print(fib()) # [1, 1, 2]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;正确的写法是这样的。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def f(a, b=2, *args, c, d=4, e, **kwargs):
    print(f&quot;a: {a}, b: {b}, args: {args}, c: {c}, d: {d}, e: {e}, kwargs: {kwargs}&quot;)

f(1, 2, 3, 4, c=5, e=6, f=7, g=8)
# a: 1, b: 2, args: (3, 4), c: 5, d: 4, e: 6, kwargs: {&apos;f&apos;: 7, &apos;g&apos;: 8}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python参数可以分为位置参数和关键字参数。&lt;/p&gt;
&lt;p&gt;默认参数和这两个概念是正交的，位置参数和关键字参数都可以有默认值。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;*args&lt;/code&gt;会收走所有多余的位置参数，&lt;code&gt;**kwargs&lt;/code&gt;会收走所有多余的关键字参数。&lt;/p&gt;
&lt;p&gt;有默认值的位置参数一定要放在位置参数的最后。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;*args&lt;/code&gt;后面的参数一定都是关键字参数。&lt;/p&gt;
&lt;h2&gt;闭包&lt;/h2&gt;
&lt;p&gt;闭包 = 函数 + 它诞生时引用到的外部环境。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;funcs = []
for i in range(3):
    funcs.append(lambda: print(i))

funcs[0]()  # 2
funcs[1]()  # 2
funcs[2]()  # 2

for i in range(3):
    funcs.append(lambda i=i: print(i))

funcs[3]()  # 0
funcs[4]()  # 1
funcs[5]()  # 2
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;闭包捕获的是变量本身，不是变量指向的值。&lt;/p&gt;
&lt;p&gt;相当于C++的&lt;code&gt;[&amp;amp;]&lt;/code&gt;捕获，Python没提供现成的&lt;code&gt;[=]&lt;/code&gt;捕获&lt;/p&gt;
&lt;h2&gt;Lambda&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;lambda x, y: x + y
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;相比普通函数更方便作为表达式内嵌&lt;/p&gt;
&lt;h2&gt;装饰器&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;def bold(func):
    def wrapper():
        return f&quot;&amp;lt;b&amp;gt;{func()}&amp;lt;/b&amp;gt;&quot;
    return wrapper

def italic(func):
    def wrapper():
        return f&quot;&amp;lt;i&amp;gt;{func()}&amp;lt;/i&amp;gt;&quot;
    return wrapper

@bold
@italic
def hello():
    return &quot;你好&quot;

print(hello())  # &amp;lt;b&amp;gt;&amp;lt;i&amp;gt;你好&amp;lt;/i&amp;gt;&amp;lt;/b&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;函数套函数的语法糖。&lt;/p&gt;
&lt;p&gt;要定义一个装饰器，就要定义一个函数，这个函数接收一个函数，返回一个函数。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def log(func):
    def wrapper(*args, **kwargs):
        print(&quot;调用中...&quot;)
        return func(*args, **kwargs)
    return wrapper

@log
def greet():
    &quot;&quot;&quot;问好&quot;&quot;&quot;
    print(&quot;你好&quot;)

print(greet.__name__)  # wrapper
print(greet.__doc__)   # None  
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这样的装饰器会让函数本身的重要元信息失效。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from functools import wraps

def log(func):
    @wraps(func)
    def wrapper(*args, **kwargs):
        print(&quot;调用中...&quot;)
        return func(*args, **kwargs)
    return wrapper

@log
def greet():
    &quot;&quot;&quot;问好&quot;&quot;&quot;
    print(&quot;你好&quot;)

print(greet.__name__)  # greet
print(greet.__doc__)   # 问好 
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;可以加这样一个装饰器解决。&lt;/p&gt;
</content:encoded></item><item><title>Python入门（一）</title><link>https://cabbage74.github.io/posts/python%E5%85%A5%E9%97%A8%E4%B8%80/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/python%E5%85%A5%E9%97%A8%E4%B8%80/</guid><description>包与虚拟环境</description><pubDate>Sat, 20 Jun 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;由于实习工作需要用到很多Python，并且之前一直拿Python当脚本语言用，基础不扎实。&lt;/p&gt;
&lt;p&gt;这个系列记录《Python编程从新手到高手》的阅读过程，希望厘清之前没能力弄懂和没意愿弄懂的知识。&lt;/p&gt;
&lt;h2&gt;包与虚拟环境&lt;/h2&gt;
&lt;p&gt;包就是Python里的第三方库。&lt;/p&gt;
&lt;p&gt;一个系统下不同项目的包可能有版本冲突，可以通过虚拟环境解决。相当于给每个项目配一个沙盒。&lt;/p&gt;
&lt;p&gt;:::tip
不过虚拟环境在实现上和项目是解耦的，所以其实可以创造一些系统级别的通用虚拟环境。
:::&lt;/p&gt;
&lt;h3&gt;这里讨论虚拟环境的使用&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;python3 -m venv venv
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;该命令在当前目录创建一个虚拟环境，第一个venv表示调用Python提供的venv工具，第二个venv表示相对路径。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;source venv/bin/activate
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;该命令激活虚拟环境。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;deactivate
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;该命令退出虚拟环境。&lt;/p&gt;
&lt;h3&gt;这里讨论虚拟环境的原理&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;我的项目/
├── venv/              ← 虚拟环境根目录
│   ├── bin/           ← Linux/macOS 下的可执行文件
│   │   ├── python     ← Python解释器的符号链接
│   │   ├── pip        ← 独立的包管理器
│   │   └── activate   ← 激活脚本
│   ├── lib/
│   │   └── python3.x/
│   │       └── site-packages/  ← 第三方包安装位置（每个虚拟环境独有）
│   └── pyvenv.cfg     ← 配置文件
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这是创建虚拟环境后一个比较经典的venv文件夹构成。&lt;/p&gt;
&lt;p&gt;激活脚本做了以下事情：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;保存当前旧PATH。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;修改PATH：把虚拟环境路径放到PATH最前面。比如原本的PATH是&lt;code&gt;/usr/local/bin:/usr/bin:...&lt;/code&gt;，那么修改后就是&lt;code&gt;/Users/cabbage/python-lab/venv/bin:/usr/local/bin:/usr/bin:...&lt;/code&gt;。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;设置&lt;code&gt;VIRTUAL_ENV&lt;/code&gt;环境变量&lt;code&gt;VIRTUAL_ENV=/Users/cabbage/python-lab/venv&lt;/code&gt;。&lt;code&gt;VIRTUAL_ENV&lt;/code&gt;是Python生态的一个约定俗成变量，下游工具都认它。比如pip看到这个变量就知道包要装到venv的site-packages里，而不是系统级别的site-packages里。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;修改提示符前缀 (venv)。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;定义deactivate函数用于恢复。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;为什么激活脚本好像只是简单改个PATH就实现了隔离呢？&lt;/p&gt;
&lt;p&gt;关键在于 &lt;code&gt;pyvenv.cfg&lt;/code&gt; 文件。Python 解释器启动时会做一件事：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;拿到自己的路径 &lt;code&gt;sys.executable&lt;/code&gt;（比如 &lt;code&gt;.../venv/bin/python&lt;/code&gt;）&lt;/li&gt;
&lt;li&gt;从这个路径向上查找 &lt;code&gt;pyvenv.cfg&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;找到了，就读取配置，把 &lt;code&gt;sys.path&lt;/code&gt; 里的系统 &lt;code&gt;site-packages&lt;/code&gt; 替换成 venv 的 &lt;code&gt;site-packages&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;sys.path&lt;/code&gt; 是 Python 内部的一个列表，控制 &lt;code&gt;import&lt;/code&gt; 时去哪里找模块——
和 shell 的 &lt;code&gt;PATH&lt;/code&gt; 原理一样，只是找的是 &lt;code&gt;.py&lt;/code&gt; 文件而非可执行文件。&lt;/p&gt;
&lt;p&gt;所以隔离不是靠&lt;code&gt;PATH&lt;/code&gt;实现的，&lt;code&gt;PATH&lt;/code&gt;只负责让 shell 找到 venv 里的
python 和 pip。真正的包隔离靠的是 Python 启动时自己读了 &lt;code&gt;pyvenv.cfg&lt;/code&gt;
然后改了 &lt;code&gt;sys.path&lt;/code&gt;。&lt;/p&gt;
&lt;h2&gt;虚拟环境相关的Python生态产品&lt;/h2&gt;
&lt;p&gt;venv只解决了一个问题：在一个Python版本下隔离包。现实中的需求比这复杂得多，这些工具就是来填补其他空白的。&lt;/p&gt;
&lt;h3&gt;conda&lt;/h3&gt;
&lt;p&gt;还能管理Python版本以及一些Python包底层依赖的C库和Fortran库。&lt;/p&gt;
&lt;p&gt;在机器学习领域使用很多。&lt;/p&gt;
&lt;h3&gt;poetry&lt;/h3&gt;
&lt;p&gt;npm/cargo风格的Python包管理。&lt;/p&gt;
&lt;h3&gt;uv&lt;/h3&gt;
&lt;p&gt;和poetry类似，因为是用Rust写的，性能很好。&lt;/p&gt;
</content:encoded></item><item><title>现代前端基础：模块化</title><link>https://cabbage74.github.io/posts/%E7%8E%B0%E4%BB%A3%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80%E6%A8%A1%E5%9D%97%E5%8C%96/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/%E7%8E%B0%E4%BB%A3%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80%E6%A8%A1%E5%9D%97%E5%8C%96/</guid><description>JS的模块化写法</description><pubDate>Sun, 14 Jun 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;在B站看到了一个不错的课程，&lt;a href=&quot;https://space.bilibili.com/427191943/lists?sid=8088328&amp;amp;spm_id_from=333.788.0.0&quot;&gt;AI时代的全栈开发&lt;/a&gt;。&lt;/p&gt;
&lt;p&gt;作为远古人，我对前端的理解还停留在HTML/CSS/JS三件套（其实这三件套也不熟）。所以现在看AI用现代前端的方法生成代码的时候，经常有两眼一抹黑的感觉。&lt;/p&gt;
&lt;p&gt;这个课程讲的还挺清楚的。打算借此入门一下现代前端。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;!doctype html&amp;gt;
&amp;lt;html lang=&quot;zh-CN&quot;&amp;gt;
  &amp;lt;head&amp;gt;
    ...
  &amp;lt;/head&amp;gt;
  &amp;lt;body&amp;gt;
    ...
    &amp;lt;script src=&quot;https://cdn.jsdelivr.net/npm/animejs@4/lib/anime.iife.min.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
    &amp;lt;script src=&quot;js/cards.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;(function () {
  anime.animate(&quot;.card&quot;, {
    opacity: [0, 1],
    translateY: [24, 0],
    delay: anime.stagger(120),
    duration: 700,
    ease: &quot;outBack&quot;,
  });
})();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;假设有这样一个index.html（最简单的原生html/css/js）。引入了两个script，一个是第三方anime库，一个是本地的cards.js负责给当前页面所有card加上闪烁效果。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/%E6%A8%A1%E5%9D%97%E5%8C%961.png&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;cards.js里用到了anime.stagger这个函数，依赖第三方anime库。也就是说一定要先导入anime库，再导入cards.js，这里代码层面的先后导入顺序需要程序员靠人脑记住。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/%E6%A8%A1%E5%9D%97%E5%8C%962.png&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;window是浏览器环境下的全局顶层对象，一个独立的html对应一个window。anime.stagger是第三方库往当前页面的window挂的名字。容易想到引入的第三方库多的时候，不同的第三方库容易撞名字，导致全局污染。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./images/%E6%A8%A1%E5%9D%97%E5%8C%963.png&quot; alt=&quot;Image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;现代前端在ES6标准后引入import/export标准，用模块化的方法解决这两个问题。&lt;/p&gt;
&lt;p&gt;每个JS文件写的东西默认只有自己看得见，想让别人看得见需要显式export，想用别人的需要显式import。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { animate, stagger } from &quot;https://cdn.jsdelivr.net/npm/animejs@4/+esm&quot;;

export function initCardsAnim() {
  animate(&quot;.card&quot;, {
    opacity: [0, 1],
    translateY: [24, 0],
    delay: stagger(120),
    duration: 700,
    ease: &quot;outBack&quot;,
  });
}
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;import { initCardsAnim } from &quot;./cards.js&quot;;

initCardsAnim();
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;!doctype html&amp;gt;
&amp;lt;html lang=&quot;zh-CN&quot;&amp;gt;
  &amp;lt;head&amp;gt;
    ...
  &amp;lt;/head&amp;gt;
  &amp;lt;body&amp;gt;
    ...
    &amp;lt;script type=&quot;module&quot; src=&quot;js/main.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;将cards.js改造成模块化版本，新建main.js作为该页面所有js的总开关。可以发现导入顺序问题、全局名称污染问题就解决了。&lt;/p&gt;
&lt;p&gt;不过引入模块化写法以后，本地打开文件会报跨域错误，浏览器出于安全考虑，规定模块化文件一定要走HTTP或者HTTPS服务。&lt;/p&gt;
</content:encoded></item><item><title>2026.06.12</title><link>https://cabbage74.github.io/posts/20260612/</link><guid isPermaLink="true">https://cabbage74.github.io/posts/20260612/</guid><description>迁移</description><pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;前几天在小红书刷到了别人用Astro搭建的静态网站，让我想起了自己巨久没有维护的博客。&lt;/p&gt;
&lt;p&gt;还记得大二那天跟着网上的各种教程，用Hexo+Next搭，遇到了很多版本不一致导致的Bug。应该前前后后花了有两到三个下午才搞成全部满意的样子，那个时候的ChatGPT远没有如今强大。&lt;/p&gt;
&lt;p&gt;昨天我让Claude Code + DeepSeek用Astro重新搭建一下，几分钟就搞定了...&lt;/p&gt;
&lt;p&gt;不知道是该开心还是伤心...&lt;/p&gt;
</content:encoded></item></channel></rss>